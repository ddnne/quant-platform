# packages/edge

CF-adjacent Python helpers and local MCP (import names unchanged).

- `cf_platform` — Python SoT mirror for CF Premium coverage / validate / natural_key
- `mcp_servers` — local stdio quant_data MCP (dev/offline)

Cloudflare Workers stay under `platform/workers/` (path frozen for wrangler).
