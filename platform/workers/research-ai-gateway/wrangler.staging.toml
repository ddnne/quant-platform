# STAGING STUB — not a production config. Do not wrangler deploy this file
# against the production Cloudflare account.
#
# HUMAN: create a physically separate Cloudflare account, Worker name, and
# secrets. Do not copy production binding IDs into this file.
# Do not create those resources from this lane.
# GATEWAY_TOKEN service-binding remains HOLD (do not fake unspoofable binding).

name = "quant-platform-research-ai-gateway-staging"
main = "src/index.ts"
compatibility_date = "2026-08-01"
workers_dev = false
preview_urls = false

# Bindings omitted on purpose. Production Worker name
# quant-platform-research-ai-gateway must not be reused.
#
# After HUMAN creates staging resources:
#   [ai] binding = "AI" on the staging Worker only
#   BUDGET_LEDGER Durable Object class on the staging Worker only
#   GATEWAY_TOKEN secret on the staging Worker only (never production)
