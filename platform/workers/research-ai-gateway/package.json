{
  "name": "quant-platform-research-ai-gateway",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Strict Workers AI gateway. Research mass-eval must not bind AI.",
  "scripts": {
    "deploy": "wrangler deploy --config=wrangler.toml --env=production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts && vitest run --config vitest.harness.config.ts",
    "build": "wrangler deploy --dry-run --config=wrangler.toml --env=\"\" --outdir .wrangler-dry-run",
    "types": "wrangler types --config=wrangler.toml --env=\"\" --include-runtime false"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.0.0",
    "@cloudflare/workers-types": "5.20260825.1",
    "typescript": "5.9.2",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  }
}
