{
  "name": "quant-platform-ci-aggregate",
  "private": true,
  "version": "0.1.0",
  "description": "Internal CI gate: six lane receipts → GitHub commit status. Not a research API.",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir .wrangler-dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5.5.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.120.1"
  }
}
