{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://quant-platform.invalid/specs/authorities/trader_webauthn_assertion.schema.json",
  "title": "Exact-four human-present Trader WebAuthn assertion",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "environment",
    "challenge_id",
    "challenge_digest",
    "exact_four_authorization_digest",
    "credential_id_base64url",
    "authenticator_data_base64url",
    "client_data_json_base64url",
    "signature_base64url",
    "rp_id",
    "origin",
    "user_present",
    "user_verified",
    "sign_count",
    "asserted_at",
    "one_use_key",
    "assertion_digest"
  ],
  "properties": {
    "schema_version": {"const": "trader-webauthn-assertion/v1"},
    "environment": {"enum": ["staging", "production"]},
    "challenge_id": {"$ref": "#/$defs/uuid4"},
    "challenge_digest": {"$ref": "#/$defs/sha256"},
    "exact_four_authorization_digest": {"$ref": "#/$defs/sha256"},
    "credential_id_base64url": {"$ref": "#/$defs/base64url"},
    "authenticator_data_base64url": {"$ref": "#/$defs/base64url"},
    "client_data_json_base64url": {"$ref": "#/$defs/base64url"},
    "signature_base64url": {"$ref": "#/$defs/base64url"},
    "rp_id": {"enum": ["staging.quant-platform.local", "quant-platform.local"]},
    "origin": {
      "enum": [
        "https://staging.quant-platform.local",
        "https://quant-platform.local"
      ]
    },
    "user_present": {"const": true},
    "user_verified": {"const": true},
    "sign_count": {"type": "integer", "minimum": 0},
    "asserted_at": {"type": "string", "format": "date-time"},
    "one_use_key": {"$ref": "#/$defs/sha256"},
    "assertion_digest": {"$ref": "#/$defs/sha256"}
  },
  "allOf": [
    {
      "if": {"properties": {"environment": {"const": "staging"}}, "required": ["environment"]},
      "then": {
        "properties": {
          "rp_id": {"const": "staging.quant-platform.local"},
          "origin": {"const": "https://staging.quant-platform.local"}
        }
      },
      "else": {
        "properties": {
          "rp_id": {"const": "quant-platform.local"},
          "origin": {"const": "https://quant-platform.local"}
        }
      }
    }
  ],
  "$defs": {
    "uuid4": {
      "type": "string",
      "pattern": "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "base64url": {"type": "string", "pattern": "^[A-Za-z0-9_-]{2,8192}$"},
    "sha256": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"}
  }
}
