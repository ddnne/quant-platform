logic_id: surprise_xs_eqf_eql
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  Surprise CS rank when EqAR fell versus the last prior print AND EqAR is low versus its PIT median. Skip missing PIT prints (no invent).
signal_definition: >
  Surprise CS rank when EqAR fell versus the last prior print AND EqAR is low versus its PIT median. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: surprise_xs_eqf_eql
  side: orig
  gates: eq_ar_falling,eq_ar_low
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
