logic_id: event_epsu_liq_uncr
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  PEAD when EPS expanded versus the last prior print AND the name is liquid versus the ADV screen AND margin is uncrowded versus its PIT median. Skip missing PIT prints (no invent).
signal_definition: >
  PEAD when EPS expanded versus the last prior print AND the name is liquid versus the ADV screen AND margin is uncrowded versus its PIT median. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: event_epsu_liq_uncr
  side: orig
  gates: eps_up,liq_high,uncrowded_margin
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
