logic_id: surprise_xs_epsu_eqr_lsur
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  PEAD when EPS expanded versus the last prior print AND EqAR rose versus the last prior print AND the earnings surprise is large. Skip missing PIT prints (no invent).
signal_definition: >
  PEAD when EPS expanded versus the last prior print AND EqAR rose versus the last prior print AND the earnings surprise is large. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: surprise_xs_epsu_eqr_lsur
  side: orig
  gates: eps_up,eq_ar_rising,large_surprise
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
