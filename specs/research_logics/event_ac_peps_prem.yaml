logic_id: event_ac_peps_prem
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  PEAD when the print is after close AND EPS is positive AND pre-event momentum is positive. Skip missing PIT prints (no invent).
signal_definition: >
  PEAD when the print is after close AND EPS is positive AND pre-event momentum is positive. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: event_ac_peps_prem
  side: orig
  gates: afterclose,positive_eps,pre_mom
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
