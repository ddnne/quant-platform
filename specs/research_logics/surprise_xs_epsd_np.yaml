logic_id: surprise_xs_epsd_np
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  Surprise CS rank when EPS contracted versus the last prior print AND net profit is negative. Skip missing PIT prints (no invent).
signal_definition: >
  Surprise CS rank when EPS contracted versus the last prior print AND net profit is negative. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: surprise_xs_epsd_np
  side: orig
  gates: eps_down,np_negative
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
