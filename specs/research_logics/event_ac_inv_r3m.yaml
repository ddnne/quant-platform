logic_id: event_ac_inv_r3m
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  PEAD when disclosure is after the close AND the repo curve inverted AND the 3m repo rate declined versus its PIT median. Skip missing PIT prints (no invent).
signal_definition: >
  PEAD when disclosure is after the close AND the repo curve inverted AND the 3m repo rate declined versus its PIT median. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
  - jsda_tokyo_repo_rates
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: event_ac_inv_r3m
  side: orig
  gates: afterclose,invert_curve,repo_3m_down
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
