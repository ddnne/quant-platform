logic_id: surprise_xs_crd_eqh_lsur
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  PEAD when margin is crowded versus its PIT median AND EqAR is high versus its PIT median AND the earnings surprise is large versus its PIT median. Skip missing PIT prints (no invent).
signal_definition: >
  PEAD when margin is crowded versus its PIT median AND EqAR is high versus its PIT median AND the earnings surprise is large versus its PIT median. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: surprise_xs_crd_eqh_lsur
  side: orig
  gates: crowded_margin,eq_ar_high,large_surprise
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
