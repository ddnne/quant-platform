logic_id: surprise_xs_epsu_eqh_taup
headline: false
generation_enabled: false
promote_as_main: false
go: false
main_pool: true
thesis: >
  PEAD when EPS expanded versus the last prior print AND EqAR is high versus its PIT median AND total assets expanded versus the last prior print. Skip missing PIT prints (no invent).
signal_definition: >
  PEAD when EPS expanded versus the last prior print AND EqAR is high versus its PIT median AND total assets expanded versus the last prior print. Skip missing PIT prints (no invent).
datasets:
  - equities_bars_daily
  - fins_summary
  - markets_calendar
params:
  post_hold_days: 5
  hold_days: 10
  momentum_n: 5
  min_hist: 20
  mode: surprise_xs_epsu_eqh_taup
  side: orig
  gates: eps_up,eq_ar_high,ta_up
  cs_gate: None
  entry_shift: 0
  hold_tail_days: 0
evaluator: research.unique_logic.event_combos.evaluate_combo_daily_mtm
