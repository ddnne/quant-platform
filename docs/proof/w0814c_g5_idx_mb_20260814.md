# W3-G5 indices_bars_daily residual + markets_breakdown residual seal (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (SUCCESS `raw_row_count>0` required)  
**Worker pass ≠ Coverage COMPLETE**  
**prefix:** `w0814c_g5_*` · general pool · **did not kill** peers (w0814b_g5_deriv paced_runner left alive)

**Base tip at start:** `4164545` / live residual SoT tip `be57976`  
**Live verified:** 2026-08-14 (JST) / ~2026-08-14T05:19Z UTC  
**Projection:** **FRESH** `projgen-463803f95e864c0591f1205c65d63bb4` age=0

## Objective

| track | dataset | action | params |
|-------|---------|--------|--------|
| **G5-idx** | `indices_bars_daily` | residual acq + real-raw seal | workers **2**, general-rpm **495**, max-jobs **100** |
| **G5-mb** | `markets_breakdown` | residual seal only | pre-2015 empty **DEFER** |

**Forbidden held:** no Mass; no empty COMPLETE; no invent receipt without raw; no peer kill.

---

## PRE (remote D1 `quant-ingest` + local)

Artifacts: `.glm-logs/w0814c_g5/PRE.json`

| metric | value |
|--------|------:|
| **idx COMPLETE** | **129** / 224 |
| idx PARTIAL | **95** |
| **mb COMPLETE** | **137** / 164 |
| mb PARTIAL | **27** (`2013-01…2015-03`) |
| platform segment COMPLETE total | **1727** |
| raw_retention_manifests | **11284** |
| empty COMPLETE | **0** |

Dry-run residual (planner, local COMPLETE skip):

| dataset | plan_jobs | queued (max-jobs) |
|---------|----------:|------------------:|
| `indices_bars_daily` | **95** | **95** (cap 100) |

---

## 1. Residual backfill (`cf_premium_backfill`) — idx

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily \
  --from-date 2008-01-01 --to-date 2026-08-13 \
  --execute --workers 2 --general-rpm 495 --max-jobs 100 \
  --sleep-on-retry 3.0 \
  --plan-out  .glm-logs/cf-backfill/w0814c_g5_idx_exec_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814c_g5_idx_exec_queue.json \
  --state-out .glm-logs/cf-backfill/w0814c_g5_idx_exec_state.jsonl
```

| field | value |
|-------|------:|
| plan / queued / executed | **95 / 95 / 95** |
| wave1 **pass / fail** | **67 / 28** |
| host POST rpm | **7.62** (window ~12.0 min) |
| http_429_count | **0** |
| fail reasons | worker_error 500×26 / 503×1 / D1 long-running import×1 |

### Retry fails (`w0814c_g5` retry_fails)

| field | value |
|-------|------:|
| fail segs | **28** |
| **pass / fail** | **28 / 0** |
| host POST rpm | **4.02** |
| empty shells | `2008-01…04` densify `rowsInserted=0` |

Worker pass densifies R2 only — COMPLETE requires raw + structured + signed SUCCESS.

---

## 2. Seal months with **real raw only** (idx)

Path (empty-raw ban held):

1. Remote `raw_retention_manifests` COMPLETE + `row_count>0`  
2. `wrangler r2 object get quant-raw/raw/{dataset}/{run_id}/…` → local raw JSON  
3. `normalize_generic` + `SqliteStore.upsert` (reject empty)  
4. signed SUCCESS receipts → `restore_local_complete_from_receipt`  
5. Fail-closed `publish_ops_projection.py --apply-remote`

Helpers (local, not committed): `.glm-logs/w0814c_g5/seal_from_r2_idx.py` (parallel workers=6) + `seal_lock_retry.py` (serial).

| wave | role | n ready | note |
|------|------|--------:|------|
| seal main | residual band densify | **69/77** | 9× `database is locked` under parallel upsert |
| lock-retry serial | 8 lock-fails | **8/8** | `2017-03…06`, `2018-05`, `2019-01…02`, `2022-08` |
| **total sealed+issued** | | **91** | promote logs ×91 |

### Sealed / COMPLETE this wave (**+91**)

Continuous `2017-03…2023-12` + tip holes  
`2024-06`, `2024-11`, `2025-01…03`, `2025-06`, `2025-08`, `2025-11`, `2026-01`.

Receipt run_ids span **902141…902280** (issue waves; peer races may interleave IDs outside pure sequential).

- empty-raw skips: **0** on issued set  
- residual PARTIAL left: **`2008-01…04`** only (API empty shells)

### Local ledger after issue+promote

| dataset | PRE COMPLETE | POST COMPLETE | Δ |
|---------|-------------:|--------------:|--:|
| `indices_bars_daily` | **129** | **220** | **+91** |
| `markets_breakdown` | **137** | **137** | **0** |
| empty COMPLETE | 0 | **0** | 0 |

---

## 3. markets_breakdown residual seal (pre-2015 DEFER)

Probe: `.glm-logs/w0814c_g5/mb_residual_probe.py`  
Floor/ceil: **2013-01…2015-03** · min full-month rows **20000**

| field | value |
|-------|------:|
| residual PARTIAL | **27** (`2013-01…2015-03`) |
| sealable | **[]** |
| thin only | `2015-03` max_rows **3628** (`DEFER_thin_partial_month`) |
| empty/shell months | **26** |
| **verdict** | **DEFER_pre2015_empty** |

Honesty: source floor ~**2015-03-26**; prior COMPLETE island starts **2015-04**. No empty COMPLETE invented.

---

## 4. Publish (fail-closed) + reeval

```text
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
# complete_count_guard ok local=1867 remote=1790 force=False
# remote projection applied

.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily --today 2026-08-14 --freshness-days 7
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset markets_breakdown --today 2026-08-14 --freshness-days 7
.venv/bin/python scripts/ops_reeval_freshness.py
```

| dataset | observed_start | observed_end | status | C8 |
|---------|----------------|--------------|--------|-----|
| `indices_bars_daily` | **2008-05-01** | **2026-08-13** | PARTIAL | **pass lag 1** |
| `markets_breakdown` | **2015-03-26** | **2026-08-13** | PARTIAL | **pass lag 1** |

Neither dataset is dataset-level COMPLETE.  
Freshness: **FRESH** `projgen-463803f95e864c0591f1205c65d63bb4` age=0 · `coverage_segments_untouched=1` · mass=NO-GO.

---

## POST (remote D1 live verify)

| metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **idx COMPLETE segs** | **129** | **220** | **+91** |
| idx PARTIAL | 95 | **4** | −91 |
| **mb COMPLETE segs** | **137** | **137** | **0** |
| mb PARTIAL | 27 | **27** | 0 |
| platform COMPLETE segs | **1727** | **1867** | **+140** (G5 idx **+91** + concurrent peers) |
| raw_retention_manifests | **11284** | **11697** | +413 (multi-track) |
| empty COMPLETE | 0 | **0** | 0 |
| projection | — | **FRESH** `projgen-463803f9…` age=0 | |

### idx residual DEFER (4)

`2008-01`, `2008-02`, `2008-03`, `2008-04` — acq pass with **empty** `rowsInserted=0`. Honest empty-raw ban: **no COMPLETE**.

### mb residual DEFER (27)

`2013-01…2015-03` — pre-source / empty shells / thin `2015-03`. **DEFER**.

---

## Acceptance

| gate | result |
|------|--------|
| idx residual acq max-jobs **100**, workers=2, rpm=495 | **PASS** (wave1 67p/28f + retry **28/28**; 0×429) |
| seal with real raw only | **PASS** (idx **+91**) |
| idx COMPLETE PRE **129** → POST **220** | **PASS** |
| mb residual seal pre-2015 | **PASS** (probe sealable **0**; DEFER held) |
| mb COMPLETE PRE **137** → POST **137** | **PASS** (+0 honest) |
| no empty COMPLETE / no Mass / Phase7 OFF / peers not killed | **PASS** |
| reeval C8 pass + FRESH age=0 | **PASS** |
| fail-closed publish (local ≥ remote, no force) | **PASS** |

---

## Explicit non-claims

- No dataset-level COMPLETE for idx (220/224) or mb (137/164).  
- No Mass / READY / Phase7.  
- No COMPLETE without raw; no empty `{"data":[]}` seals.  
- Worker pass ≠ COMPLETE (2008-01…04 empty shells prove the distinction).  
- Concurrent peers also advanced platform COMPLETE; **G5-attributed seals = idx +91 / mb +0**.

---

## Artifacts

| path | role |
|------|------|
| `.glm-logs/cf-backfill/w0814c_g5_idx_exec_*` | idx residual execute |
| `.glm-logs/cf-backfill/w0814c_g5_idx_retry_state.jsonl` | fail retry 28/28 |
| `.glm-logs/w0814c_g5/PRE.json` / `POST.json` | PRE/POST snaps |
| `.glm-logs/w0814c_g5/seal_from_r2_idx.py` | R2→struct seal |
| `.glm-logs/w0814c_g5/idx/*` | seal maps/results |
| `.glm-logs/w0814c_g5/mb_residual_probe.json` | pre-2015 DEFER probe |
| `.glm-logs/w0814c_g5/publish.log` | fail-closed apply-remote |
| `.glm-logs/w0814c_g5/reeval_*.log` / `freshness.log` | reeval |

## Operator repro

```bash
# residual acq
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily \
  --from-date 2008-01-01 --to-date 2026-08-13 \
  --execute --workers 2 --general-rpm 495 --max-jobs 100 \
  --plan-out  .glm-logs/cf-backfill/w0814c_g5_idx_exec_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814c_g5_idx_exec_queue.json \
  --state-out .glm-logs/cf-backfill/w0814c_g5_idx_exec_state.jsonl

# after real-raw mirror + struct upsert
.venv/bin/python scripts/issue_receipts_parallel.py \
  --datasets indices_bars_daily --segment-id YYYY-MM \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset indices_bars_daily --segment-id YYYY-MM

.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily --today 2026-08-14
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset markets_breakdown --today 2026-08-14
.venv/bin/python scripts/ops_reeval_freshness.py
```
