# G3 = T3 `indices_bars_daily_topix` residual close (2026-08-14)

**Mass / READY / Phase7:** **NO-GO / OFF**  
**empty COMPLETE:** **0** (SUCCESS `raw_row_count>0` required)  
**Worker pass ≠ Coverage COMPLETE** (held)

**Base tip at start:** `83fe7c0` (origin/main, ff-only up to date)

## Objective

| track | dataset | action |
|-------|---------|--------|
| **T3 / G3** | `indices_bars_daily_topix` | residual month backfill + real-raw seal **+N** (COMPLETE was stuck at **32**) |

Prefixes: `w0713_t3_topix_*`  
Params: workers **2**, general-rpm **495**, max-jobs **0** (full residual)

**Forbidden held:** no Mass; no empty COMPLETE; no invent receipt without raw.

---

## PRE (remote D1 `quant-ingest`)

Artifact: `.glm-logs/w0713-t3-topix/PRE_20260813T221659Z.json`

| metric | value |
|--------|------:|
| topix `coverage_segments` COMPLETE | **32** |
| topix PARTIAL | **192** |
| topix raw manifests n / COMPLETE | 1391 / 1390 |
| SUCCESS receipts `raw_row_count>0` n / sum_raw | 1349 / 30_857 |
| `observed_start` / `observed_end` | **2008-01-01** / 2026-08-12 |
| dataset status | PARTIAL |
| platform segment COMPLETE total | **585** |
| `raw_retention_manifests` total | **7940** |

COMPLETE months PRE: **2024-01 … 2026-08** only (32). History **2008-01 … 2023-12** = PARTIAL (192).

Dry-run residual (planner, local COMPLETE skip): **192** month jobs.

---

## 1. Backfill residual months

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily_topix \
  --from-date 2008-01-01 --to-date 2026-08-13 \
  --execute --workers 2 --general-rpm 495 --max-jobs 0 \
  --sleep-on-retry 3.0 \
  --plan-out .glm-logs/cf-backfill/w0713_t3_topix_exec_plan.json \
  --queue-out .glm-logs/cf-backfill/w0713_t3_topix_exec_queue.json \
  --state-out .glm-logs/cf-backfill/w0713_t3_topix_exec_state.jsonl
```

| field | value |
|-------|------:|
| plan / queued / executed | **192 / 192 / 192** |
| pass / fail | **192 / 0** |
| host POST rpm | **54.28** (window ~211 s) |
| http_429_count | **0** |
| window | 2026-08-13T22:17:13Z → 22:20:45Z |
| pid | 81713 |

Worker pass only — **no** COMPLETE inflation at this step.

Artifacts: `.glm-logs/cf-backfill/w0713_t3_topix_exec_{plan,queue,state,run.log}`

---

## 2. Seal months with **real raw only**

Path (empty-raw ban held):

1. Remote `raw_retention_manifests` COMPLETE + `row_count>0` (prefer newest run per month).
2. `wrangler r2 object get quant-raw/raw/indices_bars_daily_topix/{run_id}/…` →  
   `data/raw/jquants/2026/08/14/indices_bars_daily_topix_from=_to=_from_r2_run*.json`
3. `normalize_generic` + `SqliteStore.upsert(jquants_records)` (reject `{"data":[]}`).
4. `scripts/issue_receipts_parallel.py --struct-hint --limit 48 --order asc` → signed SUCCESS.
5. Fail-closed `publish_ops_projection.py --apply-remote` (no `--force-apply-remote`).

R2 mirror helper (local, not committed): `.glm-logs/w0713-t3-topix/seal_from_r2.py`  
Seal map: `.glm-logs/w0713-t3-topix/seal_map.json` (48 months)  
Seal result: **ready=48 / 48** (all non-empty pages).

### Sealed this wave (**+48**)

| field | value |
|-------|------|
| segment_ids | **2008-05 … 2012-04** (48 calendar months) |
| receipt `run_id` | **900662 – 900709** |
| structured_row_count / month | ~18–23 (TOPIX daily bars) |
| raw_row_count == structured | **yes** (script policy) |
| eligibility | `TRUSTED_COLLECTION` / `SignedReceiptAuthority` / `dev-receipt-v1` |
| empty-raw skips | **0** |

Local ledger after issue+refresh: topix COMPLETE **80/224** (= 32 prior + 48).  
empty COMPLETE (no SUCCESS nz receipt): **0**.

### Publish (fail-closed)

```text
complete_count_guard ok local=652 remote=585 force=False
remote projection applied
```

Note: local total COMPLETE **652** includes concurrent peer seals already present in the research mirror at publish time; **this track’s topix delta is +48** (32→80). Remote total COMPLETE after apply: **652**.

---

## 3. Reeval topix

```text
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily_topix --today 2026-08-14
.venv/bin/python scripts/ops_reeval_freshness.py
```

| field | PRE | POST |
|-------|-----|------|
| **observed_start** | **2008-01-01** | **2008-01-01** |
| observed_end | 2026-08-12 | **2026-08-14** |
| status | PARTIAL | PARTIAL (dataset **not** COMPLETE) |
| C8 | pass lag 1 | **pass lag 0** |
| topix COMPLETE segs | **32** | **80** |
| receipt nz n / sum_raw | 1349 / 30_857 | **1537 / 34_690** |

Projection freshness: **FRESH** `projgen-d896e57219cf49e5834d4999e4baee30` age=0  
(`ops_reeval_freshness`; coverage_segments untouched by freshness path).

---

## POST (remote D1 live verify)

| metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| topix COMPLETE segs | **32** | **80** | **+48** |
| topix PARTIAL segs | 192 | 144 | −48 |
| topix raw manifests n / c | 1391 / 1390 | 1583 / 1582 | +192 / +192 |
| SUCCESS nz receipts n / sum_raw | 1349 / 30_857 | 1537 / 34_690 | +188 / +3_833 |
| observed_end | 2026-08-12 | **2026-08-14** | +2d |
| empty COMPLETE | 0 | **0** | 0 |
| platform COMPLETE segs | 585 | **652** | +67 (topix +48 + peers in same publish) |

Dataset-level COMPLETE still only **markets_calendar** + **jsda_tokyo_repo_rates** (unchanged).  
topix dataset status remains **PARTIAL**.

---

## Acceptance

| gate | result |
|------|--------|
| residual backfill 192/192 pass, workers=2, rpm=495 | **PASS** |
| seal with real raw only (empty-raw ban) | **PASS** (+48) |
| topix COMPLETE PRE **32** → POST **80** | **PASS** |
| no empty COMPLETE / no Mass / Phase7 OFF | **PASS** |
| reeval observed_end + C8 pass lag 0 | **PASS** |
| fail-closed publish (local ≥ remote) | **PASS** |

---

## Explicit non-claims / DEFER

| item | status |
|------|--------|
| Dataset-level COMPLETE for topix | **not** claimed (80/224 segs) |
| Remaining history months **2008-01…04**, **2012-05…2023-12** | **DEFER** — more R2 raw exists; next seal wave |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| Worker pass as COMPLETE | **forbidden** |

---

## Artifacts

| path | role |
|------|------|
| `.glm-logs/cf-backfill/w0713_t3_topix_exec_*` | residual execute |
| `.glm-logs/w0713-t3-topix/PRE_*.json` | PRE snap |
| `.glm-logs/w0713-t3-topix/seal_*` | R2 mirror + result |
| `.glm-logs/w0713-t3-topix/issue_run.log` | signed receipts +48 |
| `.glm-logs/w0713-t3-topix/publish2.log` | fail-closed apply-remote |
| `.glm-logs/w0713-t3-topix/reeval.log` | observed_* reeval |
| `.glm-logs/w0713-t3-topix/freshness.log` | FRESH reclock |

## Operator repro

```bash
# dry residual
.venv/bin/python scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily_topix \
  --from-date 2008-01-01 --to-date 2026-08-13 \
  --workers 2 --general-rpm 495 \
  --plan-out .glm-logs/cf-backfill/w0713_t3_topix_plan.json \
  --queue-out .glm-logs/cf-backfill/w0713_t3_topix_queue_dry.json

# after real-raw mirror + struct upsert
.venv/bin/python scripts/issue_receipts_parallel.py \
  --datasets indices_bars_daily_topix \
  --struct-hint --limit 48 --workers 4 --order asc --dry-run --json-summary

.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily_topix --today 2026-08-14
```
