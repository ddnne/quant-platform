# W23-G4 — w0815p ops (continuous collection: receipts + projection + LIVE+DEFER maintain) (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**force-apply:** **not** used (fail-closed guard held)  
**peers killed:** **0** (G1 JSDA tip, G3 seal harvest, tip densify / bars backfill, late peer `w0815q_g6_ops` left alone)  
**Worker pass ≠ COMPLETE:** held  
**DEFER densify re-run:** **not** done (policy)  
**G4 role:** ready-seal issue gap-fill (**no dual** with live issue PIDs / peer ops_loop) + fail-closed publish/`ops_reeval_freshness` + residual SoT live-sync + **LIVE+DEFER inventory maintain** + throughput snap + `tokyo_repo_rows` + git push

**Live verified:** 2026-08-15 (JST) / ~2026-08-15T01:38Z UTC  
**Wave start HEAD:** `541a33f8f4c9082c633e7d301ccd76394838a2c2` (see `.glm-logs/w0815p_g4_ops/WAVE_START.txt`)  
**Projection:** **FRESH** `projgen-fc3440c70b8747f6a3509a9ad83c485c`  
**Artifacts:** `.glm-logs/w0815p_g4_ops/` (`ops_loop.py`, `ops_loop.stdout`, `PRE_remote.json`, `POST_remote.json`, `FINAL_metrics.json`, `publish_final.log`, `reeval_final.log`, `tokyo_repo_rows.json`)

## PRE / POST (remote D1 `quant-ingest`)

| Metric | PRE (G4 start ~01:16Z) | POST (~01:38Z) | Δ |
|--------|-----------------------:|---------------:|--:|
| `raw_retention_manifests` | **15064** | **15101** | **+37** |
| Segment COMPLETE total | **3457** | **3457** | **0** |
| Dataset COMPLETE | **11** | **11** | held |
| Dataset STALE | **0** | **0** | 0 |
| empty COMPLETE | **0** | **0** | held |
| FRESH generation | `projgen-f0c85058…` (PRE) | `projgen-fc3440c7…` | reclocked ×N |
| `jsda_otc` COMPLETE | **72** | **72** | held |
| `fins_summary` COMPLETE / PARTIAL | **218** / **6** | **218** / **6** | held (D10) |
| `tokyo_repo_rows` (easy) | local **30303** / D1 hot **252** | local **30303** / D1 hot **252** | held (plane split; hot tip already published) |

### Dataset COMPLETE set (remote POST)

| dataset | status |
|---------|--------|
| `markets_calendar` | **COMPLETE** (held) |
| `jsda_tokyo_repo_rates` | **COMPLETE** (held) |
| `jsda_corporate_bond_transactions` | **COMPLETE** (held) |
| `edinet_major_shareholders` | **COMPLETE** (held) |
| `equities_investor_types` | **COMPLETE** (held) |
| `markets_margin_alert` | **COMPLETE** (held) |
| `markets_margin_interest` | **COMPLETE** (held) |
| `markets_short_ratio` | **COMPLETE** (held) |
| `derivatives_bars_daily_futures` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options_225` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options` | **COMPLETE** (held; **W15-G1**) |

### Key segment COMPLETE (remote PRE→POST)

| dataset | PRE | POST | Δ | note |
|---------|----:|-----:|--:|------|
| `jsda_otc_bond_reference_prices` | **72** | **72** | **0** | tip held; archive still DEFER D5 |
| `fins_summary` | **218** | **218** | **0** | residual PARTIAL **6** DEFER D10 held |
| platform total | **3457** | **3457** | **0** | G4 owned issue **0**; ready gap empty |

## T1 — issue ready seals (no dual)

Continuous `ops_loop` (cycles **14**/14; sleep 90s; ~**01:16–01:38Z** window ≈ **22 min**) scanned seal/reagg results for `status=ready` without local COMPLETE.  
**DEFER skip:** `equities_earnings_calendar`, `equities_bars_daily_am` (tip-Date / today-mode).  
**empty-raw ban:** `raw_rows>0` gate held.  
**full-month filter:** options issue only from reagg-ready full_month paths (week-partial not issued; options already COMPLETE 164).

| Action | Detail |
|--------|--------|
| Dual-issue / dual-ops gate | skip issue while peer **ops_loop** alive **or** any `issue_receipts*` / `issue_restore*` / `issue_as_ready` alive |
| Peer ops_loop overlap | **0** cycles mid-window (`peer_ops_cycles=0`); late final-gate saw peer `w0815q_g6_ops` alive — final publish still ran fail-closed (local==remote) |
| G4 direct issue+restore | **0** (ready_without_complete **0** all 14 cycles) |
| Fail / aborted | **0** |
| Peer concurrent | w0815p G1 JSDA tip curl; G3 seal harvest R2 scan; tip densify raw **+**; late bars backfill + peer `w0815q_g6_ops` (left alone) |

### G4-owned issue list

| dataset | segments | n |
|---------|----------|--:|
| *(none — ready gap empty)* | | **0** |
| **TOTAL** | | **0** |

Empty-raw ban held. No `cf_premium_backfill` launched by G4. No DEFER densify.  
Session raw **+37** absorbed via publish reclock only (not G4-owned COMPLETE issue).

## T2 — publish fail-closed + ops_reeval_freshness (keep age low)

```text
publish_ops_projection --apply-remote
  complete_count_guard ok local>=remote force=False → applied
  c1 local=3457 remote=3457 → applied → projgen-9db2fc46…
  mid-window: publish skip (no delta); peer reclock gens observed
    (750be8a1… / 4f5ea492…)
  c13/c14 + final: local=3457 remote=3457 → applied
  final → projgen-fc3440c70b8747f6a3509a9ad83c485c
  no --force

ops_reeval_freshness ×N
  coverage_segments_untouched=1 mass=NO-GO
```

Fail-closed: publish only when **local COMPLETE ≥ remote**. `--force-apply-remote` **not** used.  
Projection age kept low via continuous reclock (PRE `f0c85058…` → final `fc3440c7…`).

## T3 — residual SoT live-sync + DEFER inventory maintain

- Updated `docs/phase62_residual_status.md` LIVE header/snapshot to remote POST **3457** / raw **15101** / Dataset COMPLETE **11** / FRESH `projgen-fc3440c7…` / OTC **72**
- **DEFER inventory D1–D10 held** (**no** densify re-run); re-verify remote POST **2026-08-15T01:38Z**:
  - D1 topix/idx PARTIAL `2008-01…04` (4+4) re-verified
  - D3 breakdown PARTIAL `2013-01…2015-03` (27) re-verified
  - D5 OTC archive beyond tip **72** still site-timeout DEFER (tip held **72**)
  - D9 short_sale PARTIAL `2013-01…10` (10) empty pre-history re-verified
  - D10 fins_summary PARTIAL `2008-01…06` (6) empty pre-history re-verified
  - D2/D4/D6 empty families kept (no invent COMPLETE)
- `tokyo_repo_rows` easy check: local `jsda_repo_rates` **30303**; D1 hot tip **252** (`as_of_date>=2026-07-01`) held
- This proof: `docs/proof/w0815p_g4_ops_20260815.md`

## T4 — throughput (raw Δ, COMPLETE Δ, dataset COMPLETE, tokyo_repo_rows)

| Track | host POST/min | n | note |
|-------|--------------:|--:|------|
| w0815 G1 general W1 (rpm~495 catalog) | **76.15** | 386 | 18p/368f 429-storm (historical) |
| w0815 G1 general W3 deriv+edinet | **5.03** | 341 | 201p/140f |
| w0815 G2 fins W1 (separate pool) | **49.58** | 79 | 0p/79f abort |
| w0815d G1 options week densify | **2.53** | 120 | 120p/0f (session carry) |
| w0815e G1 options residual week | **2.53** | 178 | 178p/0f (session carry) |
| w0815f G1 options residual week | **2.59** | 126 | 126p/0f (session carry) |
| G4 issue rate | — | **0** | ready gap empty (no acq) |
| Session raw Δ | — | **+37** | 15064→15101 (peer tip densify / concurrent acq) |
| Session COMPLETE seg Δ | — | **0** | 3457→3457 |
| Dataset COMPLETE count | — | **11** | held |
| `tokyo_repo_rows` | — | local **30303** / D1 hot **252** | plane split held |

**Note:** general catalog target **~495** rpm (W1 burst); fins use **separate** rate pool / lower sustained rpm.

## T5 — last_run monitor (no peer kill)

| Track | state | Action |
|-------|-------|--------|
| w0815p_g1_jsda tip | **ALIVE** mid (curl retries) | leave alone |
| w0815p_g3_seal harvest | **ALIVE** mid → **DONE**/quiet | leave alone; dual-issue N/A (ready gap 0) |
| tip densify / bars backfill | peer scope raw **+** | leave alone; densify **not** re-run by G4 |
| w0815q_g6_ops ops_loop | **ALIVE** late final gate | leave alone; no kill; final publish fail-closed only |

Resume rule held: **only if dead AND residual non-DEFER**. No dual-resume. No peer kill.

## T6 — git push each batch

Proof + residual LIVE sync + DEFER maintain committed and pushed to `origin/main` (see tip SHA below).

## Leak-check (漏れなし)

| Check | Result |
|-------|--------|
| empty COMPLETE | **0** |
| `--force-apply-remote` | **not** used |
| peers killed | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| dual-issue while peer issue/ops alive | **N/A** for issue (ready gap empty all 14 cycles) |
| DEFER densify re-run | **not** done |
| week-partial options COMPLETE mint | **not** done (full-month filter; options already COMPLETE) |
| G4 sole claim of COMPLETE Δ | **no** — session COMPLETE Δ **0**; G4 owned issue **0** |
| G4 sole claim of Dataset COMPLETE 11 | **no** — held from prior waves (options via W15-G1) |
| G4 sole claim of raw +37 | **no** — peer densify/acq |

## Explicit non-claims

- Dataset COMPLETE for fins/JSDA OTC **not** claimed (still PARTIAL except the eleven listed).
- G4 sole owner of raw densify **not** claimed.
- New historical acq by G4 **not** launched (no DEFER densify).
- DEFER residuals (D1–D10) **not** re-densified.
- fins_summary residual `2008-01…06` **not** sealed (empty pre-history DEFER formal).
- short_sale residual `2013-01…10` **not** sealed (empty pre-history DEFER formal).

## Operator repro

```bash
# continuous ops loop (ready-seal issue gap-fill + fail-closed publish)
.venv/bin/python -u .glm-logs/w0815p_g4_ops/ops_loop.py

# manual single segment (ready only; only if no peer issue / peer ops_loop alive)
.venv/bin/python scripts/issue_receipts_parallel.py \
  --dataset fins_summary --segment-id 2026-04 \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset fins_summary --segment-id 2026-04

# fail-closed publish + FRESH reclock
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```

## origin/main tip

`3c5f2c9c7b98dbb27a4dc6b522f442faa74676a1` — proof `w0815p_g4_ops_20260815.md` + residual LIVE sync (POST COMPLETE **3457** / raw **15101** / G4 issue **+0** / session COMPLETE **+0** / Dataset COMPLETE **11** / OTC **72** / empty **0** / Mass **NO-GO** / tokyo_repo local **30303** / D1 hot **252**).

## Orchestrator FINAL metrics

```json
{
  "wave": "W23-G4",
  "tag": "w0815p_g4_ops",
  "pre_complete": 3457,
  "post_complete": 3457,
  "d_complete": 0,
  "pre_raw_n": 15064,
  "post_raw_n": 15101,
  "d_raw": 37,
  "dataset_complete": 11,
  "empty_complete": 0,
  "issued_n": 0,
  "force_used": false,
  "peers_killed": 0,
  "mass_ready_phase7": "NO-GO / OFF",
  "last_gen": "projgen-fc3440c70b8747f6a3509a9ad83c485c",
  "tokyo_repo_rows": {
    "local_jsda_repo_rates": 30303,
    "d1_hot": 252,
    "d1_hot_filter": "as_of_date>=2026-07-01"
  }
}
```
