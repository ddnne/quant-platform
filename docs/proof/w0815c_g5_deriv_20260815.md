# W11-G5 derivatives — w0815c_g5_deriv residual seal (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (POST emptyish probe)  
**empty-raw ban:** held (`row_count>0` + usable pages / sealpage only)  
**peer kill ban:** **honored** — did **not** kill live W9-G1 `cf_premium_backfill` (rpm 350) or G1 `seal_from_r2` while they were acquiring/sealing  
**prefix:** `w0815c_g5_deriv_*` · **seal-only** while peer acq alive → issue → restore → publish → densify residual after peer acq died  
**PRE residual tip (task):** futures **~59** / options_225 **~72** / options **~127**  
**PRE SHA:** `6ea6662a162404d6072682dcc6c71714f51e392f`

## Goal

1. Peer W9-G1 densifying derivatives at **rpm 350** — if alive, **seal-only** from new raw; do **not** dual full acq.  
2. Seal **window_ok** months → issue SUCCESS receipts → restore COMPLETE.  
3. If acq dead, densify residual **workers 3 / general-rpm 150 / max-jobs 80**.  
4. publish + proof + **push**.

## PRE (remote D1 @ wave start)

| Metric | Value |
|--------|------:|
| Segment COMPLETE (global remote) | **3016** |
| `derivatives_bars_daily_futures` COMPLETE | **108** (PARTIAL **55**, UNKNOWN **1**) |
| `derivatives_bars_daily_options` COMPLETE | **37** (PARTIAL **127**) |
| `derivatives_bars_daily_options_225` COMPLETE | **92** (PARTIAL **72**) |
| Peer acq | **ALIVE** `w0815_g1_general` w=6 rpm=350 (pid 92649) |
| Peer seal | **ALIVE** G1 `seal_from_r2` (o225 2018 mid-flight) |

Artifacts: `.glm-logs/w0815c_g5_deriv/PRE_*.json`, `PRE_sha.txt`, `PEER_ACQ.txt`, `PEER_SEAL.txt`

## Peer policy (seal-only first)

```text
Peer densify ALIVE @ general-rpm 350 → dual full acq FORBIDDEN
Path: inventory window_ok unsealed nz COMPLETE manifests → R2 seal → issue → restore
```

Later in-session peer acq **exited** → residual densify launched (see below). Did **not** kill peer processes.

## Inventory (window_ok unsealed with nz raw)

| dataset | window_ok unsealed (inventory) | notes |
|---------|-------------------------------:|-------|
| `derivatives_bars_daily_futures` | **32** | 2013-09…2017 gaps; 2018 mostly sealed by peers |
| `derivatives_bars_daily_options_225` | **69** | 2013-01…2017 residual; 2018 under G1 seal map |
| `derivatives_bars_daily_options` | **12** | only monthly window_ok with nz raw (peer densifying more) |
| **total** | **113** | |

Artifacts: `inventory_window_ok.json`, `all_unsealed_window_ok.json`

## Seal execute (2 waves)

`seal_from_r2.py` — R2 pages → local raw (+ sealpage when ≥~24.5MB) → `normalize_generic` + `SqliteStore.upsert`. Empty-raw ban held. Parallel page fetch 8-way; serial upsert with lock retry (G1 seal contention).

| wave | map | selected | ready | skip/error |
|------|-----|---------:|------:|-----------:|
| wave1 | residual not in G1 map | **48** | **48** | **0** |
| wave2 | remaining inventory | **48** | **48** | **0** |
| **total** | | **96** | **96** | **0** |

### Seal ranges (this G5 unique map)

| dataset | segments | n |
|---------|----------|--:|
| `derivatives_bars_daily_futures` | **2013-09…2014-12**, **2015-01**, **2015-12**, **2016-01**, **2017-01…12** | **31** |
| `derivatives_bars_daily_options_225` | **2013-01…2017-05** | **53** |
| `derivatives_bars_daily_options` | **2013-01…09**, **2014-01…03** | **12** |
| **seal total** | | **96** |

Also finished issue/restore for G1-ready leftovers (futures **2018-11/12**, o225 **2018** island) without dual acq.

## Issue + restore

```bash
.venv/bin/python scripts/issue_receipts_parallel.py --dataset … --segment-id … --no-refresh
.venv/bin/python scripts/restore_local_complete_from_receipt.py --dataset … --segment-id …
```

| pass | ready non-COMPLETE | issued ok | restored COMPLETE |
|------|-------------------:|----------:|------------------:|
| early (futures 2018-11/12, o225 2018-02/06) | 4 | 4 | 4 |
| wave1 fast issue | **27** | **27** | **27** |
| final (wave2 o225 + futures tail) | **39** | **38** (+1 no_seg already COMPLETE) | **39** |
| **todo_ready after final** | **0** | | |

Eligibility: `TRUSTED_COLLECTION` / `SignedReceiptAuthority` / signed SUCCESS only.  
Empty-raw ban held. No COMPLETE without local raw + structured + SUCCESS receipt.

## Densify residual (after peer acq died)

Dry residual: **143** jobs (`futures 22` / `options 114` / `o225 7`) at general pool.

```bash
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets derivatives_bars_daily_futures,derivatives_bars_daily_options_225,derivatives_bars_daily_options \
  --from-date 2008-05-01 --execute \
  --workers 3 --general-rpm 150 --sleep-on-retry 4.0 --max-jobs 80 \
  --plan-out .glm-logs/w0815c_g5_deriv/plan_residual_exec.json \
  --queue-out .glm-logs/w0815c_g5_deriv/queue_residual_exec.json \
  --state-out .glm-logs/w0815c_g5_deriv/state_residual.jsonl
```

| field | value |
|-------|------:|
| workers | **3** |
| general-rpm | **150** |
| max-jobs | **80** |
| dry residual | **143** |
| early state (at proof) | futures **4 pass** (acq still running) |

Worker pass ≠ Coverage COMPLETE — densify left alive for next residual seal wave.

## Publish (fail-closed)

```bash
.venv/bin/python scripts/publish_ops_projection.py --apply-remote
```

```text
complete_count_guard ok local=3252 remote=3230 force=False
remote projection applied (13015 queries)
```

## Reeval

```bash
for ds in derivatives_bars_daily_futures derivatives_bars_daily_options derivatives_bars_daily_options_225; do
  .venv/bin/python scripts/ops_reeval_observed_window.py --dataset "$ds" --today 2026-08-15
done
.venv/bin/python scripts/ops_reeval_freshness.py
```

| dataset | status | observed_start → end | C8 |
|---------|--------|----------------------|-----|
| futures | **PARTIAL** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |
| options | **PARTIAL** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |
| options_225 | **PARTIAL** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |

Notes:

- All three observed_start advanced to **`2013-01-04`** via SUCCESS receipts with `raw_row_count>0`.  
- Datasets remain **PARTIAL** (month gaps remain; not dataset-level COMPLETE).  
- Freshness **FRESH** `projgen-21724a1adfd942408b289c1376f53ef3`.  
- `coverage_segments_untouched=1`; Mass **NO-GO**.  
- No segment rewrite. No COMPLETE claim from reeval alone.

## POST (remote D1 live verify)

| item | PRE (wave) | POST | Δ |
|------|----------:|-----:|--:|
| Segment COMPLETE total | **3016** | **3252** | **+236** (this G5 seals + concurrent peers) |
| `derivatives_bars_daily_futures` COMPLETE | **108** | **142** | **+34** |
| `derivatives_bars_daily_options` COMPLETE | **37** | **50** | **+13** |
| `derivatives_bars_daily_options_225` COMPLETE | **92** | **157** | **+65** |
| **trio COMPLETE** | **237** | **349** | **+112** |
| nz COMPLETE manifests (trio) | 214/229/241 | 215/252/242 | densify + peers |
| empty COMPLETE | — | **0** | |

### POST COMPLETE ranges (remote)

| dataset | COMPLETE lo…hi | n | residual PARTIAL |
|---------|----------------|--:|-----------------:|
| futures | **2013-01…2026-08** | **142** | **22** |
| options_225 | **2013-01…2026-08** | **157** | **7** |
| options | **2013-01…2026-08** | **50** | **114** |

Honesty: global COMPLETE **+236** includes concurrent peer seals (G1 densify/seal, other waves). This G5 sealed **96** window_ok months and issued/restored them (plus G1 leftover 2018 island finish). Trio unique remote Δ **+112**.

## Forbidden / honesty

- Did **not** launch Mass / READY / Phase7 ON.  
- Did **not** invent empty COMPLETE (`{"data":[]}` ban / emptyish **0**).  
- Did **not** dual full acq while peer W9-G1 rpm-350 densify was alive.  
- Did **not** kill long-running peer acq/seal.  
- Worker pass ≠ Coverage COMPLETE (acq densify left running; COMPLETE only via seal+receipt path).  
- options residual still large (**114** PARTIAL) — only **12** window_ok monthly raw available at inventory; densify continues.  
- o225 residual down **72 → 7**; futures **~59 → 22**.

## Explicit non-claims / DEFER

| Item | Why |
|------|-----|
| options mid-history continuous COMPLETE | **DEFER** — densify residual (114) + next seal waves |
| futures remaining **22** PARTIAL | **DEFER** — densify max-jobs 80 running; seal after window_ok raw |
| o225 remaining **7** PARTIAL | **DEFER** — densify + seal next wave |
| COMPLETE without raw / empty `{"data":[]}` | **Forbidden** |
| Mass / READY / Phase7 | **NO-GO / OFF** |

## Residual pointers

- Dry residual at densify launch: **futures 22 / o225 7 / options 114 = 143** jobs.  
- Live densify: w=3 rpm=150 max-jobs=80 state `.glm-logs/w0815c_g5_deriv/state_residual.jsonl`.  
- Next: seal new window_ok months from densify raw (especially options); do not dual-acq if another general-pool peer returns at high rpm.

## Operator artifacts

| path | role |
|------|------|
| `.glm-logs/w0815c_g5_deriv/seal_from_r2.py` | R2→local seal (sealpage for large months) |
| `.glm-logs/w0815c_g5_deriv/seal_map.json` / `seal_map_wave2.json` | wave1/2 selected maps |
| `.glm-logs/w0815c_g5_deriv/seal_result.jsonl` | **96** ready seals |
| `.glm-logs/w0815c_g5_deriv/issue_restore_fast.py` | issue+restore non-COMPLETE ready |
| `.glm-logs/w0815c_g5_deriv/plan_residual*.json` | residual densify plan |
| `.glm-logs/w0815c_g5_deriv/state_residual.jsonl` | densify state |
| `.glm-logs/w0815c_g5_deriv/publish.log` | fail-closed publish |
| `.glm-logs/w0815c_g5_deriv/reeval_*.log` | observed window reeval |
| `.glm-logs/w0815c_g5_deriv/freshness.log` | freshness reeval |

## POST SHA

`ff01bacef387445fa4163aa1ca238e20658258b1` (at proof write; may advance with proof commit)
