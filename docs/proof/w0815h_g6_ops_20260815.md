# W16-G6 — w0815h ops (continuous collection: receipts + projection + DEFER maintain) (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**force-apply:** **not** used (fail-closed guard held)  
**peers killed:** **0** (G1 options `issue_restore_fast` + `issue_receipts` drain, G2 options waiters, short_sale residual left alive)  
**Worker pass ≠ COMPLETE:** held  
**DEFER densify re-run:** **not** done (policy)  
**G6 role:** ready-seal issue gap-fill (full-month only; no dual with live issue PIDs) + fail-closed publish/`ops_reeval_freshness` + residual SoT live-sync + DEFER table maintain + throughput snap

**Live verified:** 2026-08-15 (JST) / ~2026-08-14T23:22Z UTC  
**Projection:** **FRESH** `projgen-6899460e4e994e1d8662f02cc342f4e9`  
**Artifacts:** `.glm-logs/w0815h_g6_ops/` (`ops_loop.py`, `ops_loop.stdout`, `PRE_*`, `POST_*`, `FINAL_metrics.json`, `publish_final.log`, `reeval_final.log`)

## PRE / POST (remote D1 `quant-ingest`)

| Metric | PRE (G6 start ~22:59Z) | POST (~23:22Z) | Δ |
|--------|-----------------------:|---------------:|--:|
| `raw_retention_manifests` | **14910** | **14953** | **+43** |
| Segment COMPLETE total | **3391** | **3409** | **+18** |
| Dataset COMPLETE | **10** | **10** | 0 (held) |
| Dataset STALE | **0** | **0** | 0 |
| empty COMPLETE | **0** | **0** | held |
| FRESH generation | `projgen-462e902c…` (PRE) | `projgen-6899460e…` | reclocked ×N |

### Dataset COMPLETE set (remote POST)

| dataset | status |
|---------|--------|
| `markets_calendar` | **COMPLETE** (held) |
| `jsda_tokyo_repo_rates` | **COMPLETE** (held) |
| `jsda_corporate_bond_transactions` | **COMPLETE** (held) |
| `edinet_major_shareholders` | **COMPLETE** (held) |
| `equities_investor_types` | **COMPLETE** (held) |
| `markets_margin_alert` | **COMPLETE** (held) |
| `markets_margin_interest` | **COMPLETE** (held) |
| `markets_short_ratio` | **COMPLETE** (held) |
| `derivatives_bars_daily_futures` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options_225` | **COMPLETE** (held; **W13-G3**) |

### Key segment COMPLETE (remote PRE→POST)

| dataset | PRE | POST | Δ | note |
|---------|----:|-----:|--:|------|
| `derivatives_bars_daily_options` | **142** | **157** | **+15** | peer **W15-G1** `issue_restore_fast` / `issue_receipts` drain (G6 dual-gate → **0** owned issue) |
| `fins_summary` | **198** | **200** | **+2** | peer fins tip (G6 **not** sole claim) |
| `jsda_otc_bond_reference_prices` | **48** | **49** | **+1** | peer JSDA tip (G6 **not** sole claim) |
| platform total | **3391** | **3409** | **+18** | concurrent peers + G6 publish; G6 **not** sole owner |

## T11 — issue ready seals (no dual with live issue PIDs)

Continuous `ops_loop` (cycles **12**/12; sleep 90s; ~**22 min** window 22:59–23:21Z) scanned seal/reagg results for `status=ready` without local COMPLETE.  
**DEFER skip:** `equities_earnings_calendar`, `equities_bars_daily_am` (tip-Date / today-mode).  
**empty-raw ban:** `raw_rows>0` gate held.  
**full-month filter:** options issue only from G1 `reagg_seal_result.jsonl` with `full_month=true` (week-partial sealpages **not** issued as COMPLETE).

| Action | Detail |
|--------|--------|
| Dual-issue gate | skip when any `issue_receipts_parallel` / `issue_driver` / `issue_promote` / `issue_restore_fast` / `issue_as_ready` alive |
| G6 direct issue+restore | **0** (gate held **all 12 cycles** — peer G1 `issue_restore_fast` + serial `issue_receipts` drain continuous) |
| Fail / aborted | **0** |
| Peer concurrent | G1 options full-month issue drain (`2022-06…` → `2022-12…` mid-session); G2 options waiters; short_sale residual |
| Cycles w/ peer issue | dual gate held (skip while G1 issue workers alive) |

### G6-owned issue list

| dataset | segments | n |
|---------|----------|--:|
| *(none — dual-gate)* | | **0** |
| **TOTAL** | | **0** |

Empty-raw ban held. No `cf_premium_backfill` launched by G6. No DEFER densify.  
Full-month ready inventory at PRE: **13** missing local COMPLETE (`2022-06…2023-06`); peer drained **~6** during G6 window; residual ready_without_complete **7** at loop end (still peer-owned).

## T12 — publish fail-closed + ops_reeval_freshness

```text
publish_ops_projection --apply-remote
  complete_count_guard ok local>=remote force=False → applied
  (×N incl. final local=3409 remote=3409)
  no --force

ops_reeval_freshness ×N
  … → projgen-6899460e4e994e1d8662f02cc342f4e9
  coverage_segments_untouched=1 mass=NO-GO
```

Fail-closed: publish only when **local COMPLETE ≥ remote**. `--force-apply-remote` **not** used.  
Projection age kept low via continuous reclock during peer COMPLETE growth (loop gens: `46019108…` → `bcd6c9c5…` → `b82f9a96…` → `29701801…` → `12790b97…` → `4afccecf…` → `b63aadf8…` → final `6899460e…`).

## T13 — residual SoT live-sync + DEFER table maintain

- Updated `docs/phase62_residual_status.md` LIVE header/snapshot to remote POST **3409** / raw **14953** / Dataset COMPLETE **10** / FRESH `projgen-6899460e…`
- DEFER inventory **D1–D9** held (**no** densify re-run); D5 tip note OTC **49**
- This proof: `docs/proof/w0815h_g6_ops_20260815.md`

## T14 — throughput (raw Δ, COMPLETE Δ, dataset COMPLETE, general ~495 / fins separate)

| Track | host POST/min | n | note |
|-------|--------------:|--:|------|
| w0815 G1 general W1 (rpm~495 catalog) | **76.15** | 386 | 18p/368f 429-storm (historical) |
| w0815 G1 general W3 deriv+edinet | **5.03** | 341 | 201p/140f |
| w0815 G2 fins W1 (separate pool) | **49.58** | 79 | 0p/79f abort |
| w0815d G1 options week densify | **2.53** | 120 | 120p/0f (session carry) |
| w0815e G1 options residual week | **2.53** | 178 | 178p/0f (session carry) |
| w0815f G1 options residual week | **2.59** | 126 | 126p/0f (session carry) |
| G6 issue rate | — | **0** | dual-gate skip entire window (peer issue_restore_fast alive) |
| Session raw Δ | — | **+43** | 14910→14953 |
| Session COMPLETE seg Δ | — | **+18** | 3391→3409 |
| Dataset COMPLETE count | — | **10** | held |

**Note:** general catalog target **~495** rpm (W1 burst); fins use **separate** rate pool / lower sustained rpm.

## T15 — last_run monitor (no peer kill)

| Track | state | Action |
|-------|-------|--------|
| w0815g_g1_options issue_restore_fast + issue_receipts | **ALIVE** / drain | leave alone; dual-issue skip while their issue alive |
| w0815h_g2_options post_when_ready waiters | **ALIVE** | leave alone |
| w0815h_g1_short_sale residual | peer scope | leave alone |

Resume rule held: **only if dead AND residual non-DEFER**. No dual-resume. No peer kill.

## T16 — git push each batch

Proof + residual LIVE sync committed and pushed to `origin/main` (see tip SHA below).

## Leak-check (漏れなし)

| Check | Result |
|-------|--------|
| empty COMPLETE | **0** |
| `--force-apply-remote` | **not** used |
| peers killed | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| dual-issue while peer issue alive | **skipped** (gate; all 12 cycles) |
| DEFER densify re-run | **not** done |
| week-partial options COMPLETE mint | **not** done (full-month filter) |
| G6 sole claim of COMPLETE +18 | **no** — concurrent G1/G2/JSDA peers; G6 owned issue **0** |
| G6 sole claim of Dataset COMPLETE | **no** — held **10** (prior waves) |

## Explicit non-claims

- Dataset COMPLETE for options/fins/JSDA OTC **not** claimed (still PARTIAL except the ten listed).
- G6 sole owner of raw densify **not** claimed.
- New historical acq by G6 **not** launched.
- DEFER residuals (D1–D9) **not** re-densified.
- G1 options full-month issue campaign / remaining residual months **not** claimed as G6-owned (dual-gate skip; peer owned).
- G6 owned issue **0** — dual-issue policy held over opportunistic drain.

## Operator repro

```bash
# continuous ops loop (full-month ready-seal issue gap-fill + fail-closed publish)
.venv/bin/python -u .glm-logs/w0815h_g6_ops/ops_loop.py

# manual single segment (full-month reagg-ready only; only if no peer issue alive)
.venv/bin/python scripts/issue_receipts_parallel.py \
  --dataset derivatives_bars_daily_options --segment-id 2022-12 \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset derivatives_bars_daily_options --segment-id 2022-12

# fail-closed publish + FRESH reclock
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```

## origin/main tip

`a8b4645152a39140c74938004bd47ea9afe43480` — proof `w0815h_g6_ops_20260815.md` + residual LIVE sync (POST COMPLETE **3409** / raw **14953** / G6 issue **+0** dual-gate / session COMPLETE **+18** / Dataset COMPLETE **10**).

## Orchestrator FINAL metrics

```json
{
  "wave": "W16-G6",
  "tag": "w0815h_g6_ops",
  "pre_complete": 3391,
  "post_complete": 3409,
  "d_complete": 18,
  "pre_raw_n": 14910,
  "post_raw_n": 14953,
  "d_raw": 43,
  "dataset_complete": 10,
  "empty_complete": 0,
  "g6_issued": 0,
  "force_used": false,
  "peers_killed": 0,
  "mass": "NO-GO",
  "phase7": "OFF",
  "active_gen": "projgen-6899460e4e994e1d8662f02cc342f4e9",
  "options_complete": {"pre": 142, "post": 157, "d": 15},
  "fins_summary": {"pre": 198, "post": 200, "d": 2},
  "jsda_otc": {"pre": 48, "post": 49, "d": 1}
}
```
