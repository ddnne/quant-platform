# W8-G10 — `equities_master` residual ~94 PARTIAL (w0814h_g10_master) — 2026-08-14

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**empty-raw ban:** held (no COMPLETE without window-ok Date ∈ segment month)  
**Worker pass ≠ Coverage COMPLETE**  
**kill peers:** **none** (bars / fins / edinet / deriv / misc seal left alone)  
**prefix:** `w0814h_g10_master_*` · CF D1 SoT

## Goal

1. PRE remote/local master COMPLETE/PARTIAL inventory.  
2. Identify residual months with R2 raw or sealable window (prefer post-2008 first).  
3. `cf_premium_backfill` densify residual (workers 1–2, general RPM ~80–100; leave pool for T4 bars peers).  
4. Seal only months with honest in-window raw (`raw_row_count>0` **and** page `Date` ∈ segment month).  
5. Pre-2008 empty/misdated → **DEFER with proof** (do not invent COMPLETE).  
6. freshness + proof + residual SoT + **git push**.

## PRE / POST

| Metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **Local** `equities_master` COMPLETE | **220** | **220** | **0** |
| **Remote** `equities_master` COMPLETE | **220** | **220** | **0** |
| Local / remote master PARTIAL | **94** / **94** | **94** / **94** | 0 |
| master TOTAL segs | **314** | **314** | 0 |
| empty COMPLETE | **0** | **0** | 0 |
| COMPLETE span | `2008-05`…`2026-08` continuous | same | 0 holes |
| post-2008 PARTIAL holes | **[]** | **[]** | — |
| Remote master `raw_retention_manifests` n / row_count>0 | **447 / 372** | **472 / 395** | +25 / +23 densify |

Projection after reeval freshness: **FRESH** `projgen-92b097b80d464e1082cc4eb4413efbce`  
(`coverage_segments_untouched=1`; mass=**NO-GO**).

Observed window reeval (`ops_reeval_observed_window.py --dataset equities_master --today 2026-08-14`):

| field | value |
|-------|------|
| status | **PARTIAL** |
| observed_start | **`2006-08-13`** (receipt-derived; not segment COMPLETE) |
| observed_end | **`2026-08-13`** |
| C8 | **pass** lag **1** |

### Residual taxonomy (94 PARTIAL — all pre-2008-05)

| band | n | segments | action |
|------|--:|----------|--------|
| Planner residual (misdate band) | **21** | `2006-08`…`2008-04` | densify + window gate → **DEFER** |
| Pre-plan / outside contract history | **73** | `2000-07`…`2006-07` | **DEFER** (no planner jobs) |
| Post-2008 holes | **0** | — | n/a (highest seal ROI already sealed) |

Artifacts: `.glm-logs/w0814h_g10_master/pre_status.json`, `defer_list.json`, `post_status.json`.

## 1) `cf_premium_backfill` residual densify

Planner residual after COMPLETE=220 is exactly **21** months (`2006-08`…`2008-04`).  
Share general pool lightly (bars peers priority T4).

### Wave 1 (partial — tool kill mid-run; not peer kill)

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_master \
  --execute --workers 1 --general-rpm 100 --max-jobs 0 \
  --sleep-on-retry 8.0 \
  --plan-out  .glm-logs/cf-backfill/w0814h_g10_master_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814h_g10_master_queue.json \
  --state-out .glm-logs/cf-backfill/w0814h_g10_master_state.jsonl
```

| field | value |
|------:|------:|
| executed (before interrupt) | **3** |
| pass / fail | **2 / 1** |
| fail class | HTTP **400** subscription edge on `2006-08` |

### Wave 2 (full residual; natural exit)

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_master \
  --from-date 2006-08-01 --to-date 2008-04-30 \
  --execute --workers 1 --general-rpm 80 --max-jobs 0 \
  --sleep-on-retry 12.0 \
  --plan-out  .glm-logs/cf-backfill/w0814h_g10_master_plan2.json \
  --queue-out .glm-logs/cf-backfill/w0814h_g10_master_queue2.json \
  --state-out .glm-logs/cf-backfill/w0814h_g10_master_state2.jsonl
```

| field | value |
|-------|------:|
| plan jobs / executed | **21 / 21** |
| **pass** | **20** |
| **fail** | **1** (`2006-08` HTTP **400** subscription: plan `from=2006-08-13` vs sub start `2006-08-14`) |
| host envelope HTTP 429 | **0** |
| host POST/min | **1.28** (window ~940s; gentle vs bars peers) |

**Note:** Several worker-pass jobs report `rowsInserted=0` or small inserts — worker pass still **≠** Coverage COMPLETE. New R2 runs landed (e.g. **11587**, **11602**, **11703–11715** band) with non-zero `row_count` manifests.

## 2) Seal gate (window-ok only)

Probe path: newest R2 manifests for residual months → first non-empty page → require `Date` ∈ segment month.

| class | n | action |
|-------|--:|--------|
| **window-ok** (Date ∈ segment month) | **0** | none to seal |
| **window-bad** (Date stuck on **`2008-05-07`**) | **21/21** months (prior + newest densify) | **DEFER** — not sealed |
| empty raw | **0** among densify band (manifests claim thousands of rows) | — |
| sealed ready / issue / restore | **0** | — |

**Honesty:** Even **new** densify runs (e.g. `2007-03` run **11587**, `2007-04` run **11602**, `2006-09` run **11713**, `2008-04` run **11707**) have page bodies dated **`2008-05-07` only** — same misdate class as G2/G4. Non-zero `row_count` is **not** in-window evidence.

Artifacts:

- `.glm-logs/w0814h_g10_master/post_densify_probe.json`
- `.glm-logs/w0814h_g10_master/window_ok.json` (empty)
- `.glm-logs/w0814h_g10_master/window_bad_newest.json`
- `.glm-logs/w0814h_g10_master/residual_best.json`
- prior G4 corroboration: `.glm-logs/w0814-g4-master/window_bad.json`

## 3) Receipts / publish

| step | result |
|------|--------|
| issue_receipts / restore | **skipped / N/A** — window_ok **0**; empty-raw / misdate ban held |
| full `publish_ops_projection --apply-remote` | **not required** — COMPLETE Δ **0** (fail-closed path unused) |
| `ops_reeval_observed_window.py` | **OK** status PARTIAL; C8 pass lag1 |
| `ops_reeval_freshness.py` | **OK** FRESH `projgen-92b097b80d464e1082cc4eb4413efbce`; segs untouched; Mass **NO-GO** |

## Explicit non-claims / DEFER

| Item | Status |
|------|--------|
| COMPLETE without usable in-window raw | **Forbidden** — held |
| `2006-08`…`2008-04` (21) | **DEFER** — misdated `2008-05-07` bodies (incl. post-densify R2) |
| `2000-07`…`2006-07` (73) | **DEFER** — outside BackfillPlanner residual / no honest raw |
| `2006-08` densify | **OPEN optional** only as subscription-edge fix (`from≥2006-08-14`); still likely misdate |
| Post-2008 residual holes | **none** — continuous COMPLETE `2008-05`…`2026-08` |
| Dataset-level COMPLETE for `equities_master` | still **PARTIAL** (history hole pre-2008-05) |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| Peer acq/issue killed? | **no** |

## Verdict

| Check | Result |
|-------|--------|
| master COMPLETE PRE→POST | **220 → 220 (+0)** local = remote |
| residual plan densify | **PASS** (21 jobs; 20p/1f; 0× host 429) |
| raw-required seal only / misdated skip | **PASS** (window_ok **0**, sealed **0**) |
| empty COMPLETE | **0 PASS** |
| no peer kill | **PASS** |
| C8 master | **pass** lag **1** |
| Mass / Phase7 | **NO-GO / OFF PASS** |
| Overall W8-G10 master residual | **PASS** (honest +0; 94 PARTIAL remain DEFER with evidence) |

## Operator repro

```bash
# residual plan (expect 21 = 2006-08…2008-04 while COMPLETE=220)
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_master --workers 1 --general-rpm 80 --max-jobs 0 \
  --plan-out .glm-logs/cf-backfill/w0814h_g10_master_plan2.json \
  --queue-out .glm-logs/cf-backfill/w0814h_g10_master_queue2.json

# densify (gentle general pool)
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_master --from-date 2006-08-01 --to-date 2008-04-30 \
  --execute --workers 1 --general-rpm 80 --sleep-on-retry 12.0 \
  --plan-out .glm-logs/cf-backfill/w0814h_g10_master_plan2.json \
  --queue-out .glm-logs/cf-backfill/w0814h_g10_master_queue2.json \
  --state-out .glm-logs/cf-backfill/w0814h_g10_master_state2.jsonl

# reeval (no COMPLETE claim)
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset equities_master --today 2026-08-14 --freshness-days 7
.venv/bin/python scripts/ops_reeval_freshness.py
```
