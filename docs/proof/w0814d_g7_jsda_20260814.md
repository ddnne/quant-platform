# W4-G7 — JSDA OTC tip/recent **+1** (raw only) (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (OTC sealed days only; no empty shell)  
**empty-raw ban:** held (only official `market.jsda.or.jp` CSV bytes; no invent)  
**Worker pass ≠ COMPLETE:** held (direct HTTP → local raw → parse/upsert → identity-matched `SignedReceiptAuthority` → ledger)  
**corporate+repo:** **COMPLETE skip** (verify only; no re-seal)

**Live verified:** 2026-08-14 (JST) / ~2026-08-14T06:18Z UTC  
**Projection:** **FRESH** `projgen-87dcba7c676648d397431ccf69ee5cfd` age=0 (`coverage_segments_untouched=1`)

## Summary

| Metric | PRE | POST | This W4-G7 |
|--------|----:|-----:|------------|
| JSDA OTC COMPLETE segs | **17** / 8779 | **18** / 8779 | **+1** |
| JSDA corporate | **12/12 COMPLETE** | **12/12 COMPLETE** | skip (verify only) |
| JSDA tokyo repo | **COMPLETE** 1/1 | **COMPLETE** 1/1 | skip (verify only) |
| empty COMPLETE (OTC) | **0** | **0** | held |
| Platform COMPLETE (local publish) | **2034** remote pre-guard | **2035** remote | G7 OTC **+1** |

## Path

1. `git pull --ff-only origin main` (already up to date @ `f9bf2e1…`).
2. Local direct probe of `market.jsda.or.jp`:
   - Index briefly **HTTP 200** (62_006 B, ~0.15s).
   - Tip/recent window opened for one day: **`S260721` HTTP 200** (2_153_826 B).
   - **`S260718` HTTP 404** (non-publication / missing official file — not sealed).
   - **`S260717` max-time partial** then **connect timeout (000)** on retry → discarded partial; **DEFER** remaining tip/recent.
   - Subsequent index/data probes: **connect timeout (000)** → site flake **DEFER**.
3. Corporate + tokyo repo: coverage already **dataset COMPLETE** → **skip** seal path (verify only).
4. Operator seal (`.glm-logs/w0814d_g7_jsda/`):
   - `parse_otc_reference_csv` (headerless positional) + `normalize_otc_reference_prices` + upsert
   - Inventory `expected_items` set to structured count + full inventory `expected_scope` (identity match for `official_archive_day`)
   - Identity-matched `SignedReceiptAuthority` SUCCESS (`expected_scope` / `expected_items` == required)
   - `refresh_coverage_ledger(conn, db, datasets=['jsda_otc_bond_reference_prices'], today='2026-08-14')` → OTC COMPLETE **17→18**
5. R2 retention put (content-addressed):
   - `raw/jsda/jsda_otc_bond_reference_prices/file_S260721.csv/2f197264…9637ec.csv` (verified get + sha256 match)
6. `publish_ops_projection.py --apply-remote` (fail-closed guard: local **2035** ≥ remote **2034**)  
   `ops_reeval_freshness.py` → FRESH age=0; `coverage_segments_untouched=1`

## OTC sealed this wave (**+1**)

| segment_id | raw | rows | receipt run_id | digest (sha256) |
|------------|-----|-----:|---------------:|-----------------|
| **2026-07-21** | `S260721.csv` | **12327 / 12327** | **902424** | `2f197264…699637ec` |

Eligibility: `TRUSTED_COLLECTION` / `SignedReceiptAuthority` / `dev-receipt-v1`.  
Fetched via: `direct_jsda_http+local_raw` (site window) + R2 put for retention.

### POST OTC COMPLETE (**18**)

`2026-07-21`, `2026-07-22`, `2026-07-23`, `2026-07-24`, `2026-07-27`, `2026-07-28`, `2026-07-29`, `2026-07-30`, `2026-07-31`, `2026-08-03`, `2026-08-04`, `2026-08-05`, `2026-08-06`, `2026-08-07`, `2026-08-10`, `2026-08-12`, `2026-08-13`, `2026-08-14`.  
Dataset still **PARTIAL** (history target 2002-08-02; **8779** required days).

## Corporate / Repo (COMPLETE skip)

- **Corporate:** segment COMPLETE **12/12** (`2015`…`2026`); dataset **COMPLETE** — unchanged (no seal).
- **Tokyo repo:** dataset **COMPLETE**, segment `jsda-era-timeseries` **COMPLETE** — unchanged (no seal).

## DEFER (honest)

| Item | Why |
|------|-----|
| OTC further tip/recent (e.g. `2026-07-17` and earlier July) | Local data-file **connect timeout (000)** after one successful day; partial `S260717` discarded. **No raw → no COMPLETE**. |
| OTC full archive (~8761 PARTIAL remain) | Site flake + no full archive crawl this wave. |
| Non-trading / missing official days (e.g. `2026-07-18` 404) | Not sealed as COMPLETE |
| COMPLETE without raw / empty shell | **Forbidden** (empty COMPLETE **0** on OTC) |
| Mass / READY / Phase7 | **NO-GO / OFF** |

## Publish (fail-closed)

```text
complete_count_guard ok local=2035 remote=2034 force=False
remote projection applied
ops_reeval_freshness → projgen-87dcba7c676648d397431ccf69ee5cfd FRESH age=0
OK coverage_segments_untouched=1 mass=NO-GO
```

Remote D1 post-check: OTC COMPLETE **18** / PARTIAL **8761**; total COMPLETE **2035**; corp+repo datasets COMPLETE; OTC dataset PARTIAL.

No `--force-apply-remote`.

## Explicit non-claims

- OTC dataset-level COMPLETE **not** claimed (still PARTIAL vs 8779-day inventory).
- Platform Mass / READY / Phase7 **not** claimed.
- Corporate/repo seals **not** re-run this wave (already COMPLETE).

## Operator repro

```bash
# tip raw (when market.jsda.or.jp accepts connections)
mkdir -p data/raw/jsda/jsda_otc_bond_reference_prices/2026-07-21
curl -o data/raw/jsda/jsda_otc_bond_reference_prices/2026-07-21/S260721.csv \
  https://market.jsda.or.jp/shijyo/saiken/baibai/baisanchi/files/2026/S260721.csv

# seal: parse/normalize/upsert + inventory expected_items=N + full expected_scope + SignedReceiptAuthority
# then refresh_coverage_ledger(conn, db, datasets=['jsda_otc_bond_reference_prices'], today='2026-08-14')
# see .glm-logs/w0814d_g7_jsda/

.venv/bin/python scripts/publish_ops_projection.py --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```
