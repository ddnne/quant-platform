# W25-G5 — w0815r ops (continuous collection: receipts + projection + LIVE+DEFER maintain) (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**force-apply:** **not** used (fail-closed guard held)  
**peers killed:** **0** (peer `w0815q_g6_ops` ops_loop cycles 1–3; concurrent `w0815r_g4_edinet_otc` / `w0815r_g3_bars` left alone)  
**Worker pass ≠ COMPLETE:** held  
**DEFER densify re-run:** **not** done (policy)  
**G5 role:** ready-seal issue gap-fill (**no dual** with live issue PIDs / peer ops_loop) + fail-closed publish/`ops_reeval_freshness` + **last_run monitor** + residual SoT live-sync + **LIVE+DEFER inventory maintain** + throughput snap + **CF-SoT language confirm** + git push

**Live verified:** 2026-08-15 (JST) / ~2026-08-15T02:18Z UTC  
**Wave start HEAD:** `c95a59839825e1f473b5509c4de0b174822de06c` (see `.glm-logs/w0815r_g5_ops/WAVE_START.txt`)  
**Projection:** **FRESH** `projgen-9ee87879ce1843a0aa89f4801b8b7549`  
**Artifacts:** `.glm-logs/w0815r_g5_ops/` (`ops_loop.py`, `ops_loop.stdout`, `PRE_remote.json`, `POST_remote.json`, `FINAL_metrics.json`, `publish_final.log`, `reeval_final.log`, `last_run_monitor*.json`, `tokyo_repo_rows.json`)

## PRE / POST (remote D1 `quant-ingest`)

| Metric | PRE (G5 start ~01:56Z) | POST (~02:18Z) | Δ |
|--------|-----------------------:|---------------:|--:|
| `raw_retention_manifests` | **15102** | **15125** | **+23** |
| Segment COMPLETE total | **3457** | **3457** | **0** |
| Dataset COMPLETE | **11** | **11** | held |
| Dataset STALE | **0** | **0** | 0 |
| empty COMPLETE | **0** | **0** | held |
| FRESH generation | `projgen-659d1b2b…` (PRE) | `projgen-9ee87879…` | reclocked ×N |
| `jsda_otc` COMPLETE | **72** | **72** | held |
| `fins_summary` COMPLETE / PARTIAL | **218** / **6** | **218** / **6** | held (D10) |
| `tokyo_repo_rows` (easy) | local **30303** / D1 hot **252** | local **30303** / D1 hot **252** | held (plane split; hot tip already published) |

### Dataset COMPLETE set (remote POST)

| dataset | status |
|---------|--------|
| `markets_calendar` | **COMPLETE** (held) |
| `jsda_tokyo_repo_rates` | **COMPLETE** (held) |
| `jsda_corporate_bond_transactions` | **COMPLETE** (held) |
| `edinet_major_shareholders` | **COMPLETE** (held) |
| `equities_investor_types` | **COMPLETE** (held) |
| `markets_margin_alert` | **COMPLETE** (held) |
| `markets_margin_interest` | **COMPLETE** (held) |
| `markets_short_ratio` | **COMPLETE** (held) |
| `derivatives_bars_daily_futures` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options_225` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options` | **COMPLETE** (held; **W15-G1**) |

### Key segment COMPLETE (remote PRE→POST)

| dataset | PRE | POST | Δ | note |
|---------|----:|-----:|--:|------|
| `jsda_otc_bond_reference_prices` | **72** | **72** | **0** | tip held; archive still DEFER D5 |
| `fins_summary` | **218** | **218** | **0** | residual PARTIAL **6** DEFER D10 held |
| platform total | **3457** | **3457** | **0** | G5 owned issue **0**; ready gap empty |

## T11 — issue ready seals (no dual)

Continuous `ops_loop` (cycles **14**/14; sleep 90s; ~**01:56–02:18Z** window ≈ **22 min**) scanned seal/reagg results for `status=ready` without local COMPLETE.  
**DEFER skip:** `equities_earnings_calendar`, `equities_bars_daily_am` (tip-Date / today-mode).  
**empty-raw ban:** `raw_rows>0` gate held.  
**full-month filter:** options issue only from reagg-ready full_month paths (week-partial not issued; options already COMPLETE 164).

| Action | Detail |
|--------|--------|
| Dual-issue / dual-ops gate | skip issue while peer **ops_loop** alive **or** any `issue_receipts*` / `issue_restore*` / `issue_as_ready` alive |
| Peer ops_loop overlap | **3** cycles (`peer_ops_cycles=3` — `w0815q_g6_ops` alive c1–c3; dead c4+) |
| G5 direct issue+restore | **0** (ready_without_complete **0** all 14 cycles) |
| Fail / aborted | **0** |
| Peer concurrent | `w0815q_g6_ops` (early); `w0815r_g4_edinet_otc` curl probes; `w0815r_g3_bars` D1 q (left alone) |

### G5-owned issue list

| dataset | segments | n |
|---------|----------|--:|
| *(none — ready gap empty)* | | **0** |
| **TOTAL** | | **0** |

Empty-raw ban held. No `cf_premium_backfill` launched by G5. No DEFER densify.  
Session raw **+23** absorbed via publish reclock only (not G5-owned COMPLETE issue).

## T12 — publish fail-closed + ops_reeval_freshness (keep age low)

```text
publish_ops_projection --apply-remote
  complete_count_guard ok local>=remote force=False → applied
  c1–c3: publish defer to peer ops_loop (alive, no G5-owned issue)
  c4: local=3457 remote=3457 → applied → projgen-2f71cb68…
  c13/c14 + final: local=3457 remote=3457 → applied
  final → projgen-9ee87879ce1843a0aa89f4801b8b7549
  no --force

ops_reeval_freshness ×N
  coverage_segments_untouched=1 mass=NO-GO
```

Fail-closed: publish only when **local COMPLETE ≥ remote**. `--force-apply-remote` **not** used.  
Projection age kept low via continuous reclock after peer window (PRE `659d1b2b…` → final `9ee87879…`).

## T13 — last_run monitor (no peer kill)

| Track | state | Action |
|-------|-------|--------|
| w0815q_g6_ops ops_loop | **ALIVE** c1–c3 → **DONE** c4+ | leave alone; dual publish defer while alive |
| w0815r_g4_edinet_otc tip curl | **ALIVE** mid | leave alone; densify **not** re-run by G5 |
| w0815r_g3_bars D1 probes | peer scope | leave alone |
| issue_receipts / issue_restore | **none** | dual-issue N/A (ready gap 0) |

Artifacts: `.glm-logs/w0815r_g5_ops/last_run_monitor.json` (mid) + `last_run_monitor_post.json`.  
Resume rule held: **only if dead AND residual non-DEFER**. No dual-resume. No peer kill.

Local `ingestion_run_log` status_counts (stale mirror; CF remote is SoT): pass/ok/partial mixed; no live worker fail storm. Remote empty COMPLETE **0**; Dataset STALE **0**.

## T14 — residual SoT live-sync + DEFER inventory maintain

- Updated `docs/phase62_residual_status.md` LIVE header/snapshot to remote POST **3457** / raw **15125** / Dataset COMPLETE **11** / FRESH `projgen-9ee87879…` / OTC **72**
- **DEFER inventory D1–D10 held** (**no** densify re-run); re-verify remote POST **2026-08-15T02:18Z**:
  - D1 topix/idx residual PARTIAL `2008-01…04` (4+4) held (COMPLETE segs **220/220**)
  - D3 breakdown PARTIAL `2013-01…2015-03` (27) held (COMPLETE **137**)
  - D5 OTC archive beyond tip **72** still site-timeout DEFER (tip held **72**)
  - D9 short_sale PARTIAL `2013-01…10` (10) empty pre-history re-verified
  - D10 fins_summary PARTIAL `2008-01…06` (6) empty pre-history re-verified
  - D2/D4/D6 empty families kept (no invent COMPLETE)
- `tokyo_repo_rows` easy check: local `jsda_repo_rates` **30303**; D1 hot tip **252** (`as_of_date>=2026-07-01`) held
- This proof: `docs/proof/w0815r_g5_ops_20260815.md`

## T15 — throughput + git push

| Track | host POST/min | n | note |
|-------|--------------:|--:|------|
| w0815 G1 general W1 (rpm~495 catalog) | **76.15** | 386 | 18p/368f 429-storm (historical) |
| w0815 G1 general W3 deriv+edinet | **5.03** | 341 | 201p/140f |
| w0815 G2 fins W1 (separate pool) | **49.58** | 79 | 0p/79f abort |
| w0815d G1 options week densify | **2.53** | 120 | 120p/0f (session carry) |
| w0815e G1 options residual week | **2.53** | 178 | 178p/0f (session carry) |
| w0815f G1 options residual week | **2.59** | 126 | 126p/0f (session carry) |
| G5 issue rate | — | **0** | ready gap empty (no acq) |
| Session raw Δ | — | **+23** | 15102→15125 (peer tip densify / concurrent acq) |
| Session COMPLETE seg Δ | — | **0** | 3457→3457 |
| Dataset COMPLETE count | — | **11** | held |
| `tokyo_repo_rows` | — | local **30303** / D1 hot **252** | plane split held |

**Note:** general catalog target **~495** rpm (W1 burst); fins use **separate** rate pool / lower sustained rpm.

Proof + residual LIVE sync + DEFER maintain committed and pushed to `origin/main` (see tip SHA below).

## T16 — Confirm CF-SoT language landed (G1 finished)

| Check | Result |
|-------|--------|
| W24-G1 audit §9 re-verify | **landed** — `docs/proof/column_null_audit_20260815.md` §9 (2026-08-15 10:34JST) |
| Residual W24-G1 note | **landed** — CF SoT re-sample **HOLDS** (master short typed 0% null n=200; fins+margin keyset equal 1.0; **no new mapping bugs**) |
| W25-G1 CF-SoT language lock | **landed** — residual § W20: *D1 = hot tip · R2 = history · COMPLETE = receipt-owned*; local SQLite = **mirror only** |
| Authority line | **held** — `CF SoT: D1 = hot tip · R2 = history · coverage COMPLETE = receipt-owned` |
| Logs | `.glm-logs/w0815q_g1_audit_reverify/` (`REVERIFY_REPORT.json`, `REPORT.md`) |
| New mapping bugs | **none** (G5 does not reopen column audit) |

G1 finished prior to this ops window; G5 **confirms** language is present on residual + audit and does not invent alternate SoT wording.

## Leak-check (漏れなし)

| Check | Result |
|-------|--------|
| empty COMPLETE | **0** |
| `--force-apply-remote` | **not** used |
| peers killed | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| dual-issue while peer issue/ops alive | **N/A** for issue (ready gap empty); publish deferred c1–c3 while peer ops_loop alive |
| DEFER densify re-run | **not** done |
| week-partial options COMPLETE mint | **not** done (full-month filter; options already COMPLETE) |
| G5 sole claim of COMPLETE Δ | **no** — session COMPLETE Δ **0**; G5 owned issue **0** |
| G5 sole claim of Dataset COMPLETE 11 | **no** — held from prior waves (options via W15-G1) |
| G5 sole claim of raw +23 | **no** — peer densify/acq |

## Explicit non-claims

- Dataset COMPLETE for fins/JSDA OTC **not** claimed (still PARTIAL except the eleven listed).
- G5 sole owner of raw densify **not** claimed.
- New historical acq by G5 **not** launched (no DEFER densify).
- DEFER residuals (D1–D10) **not** re-densified.
- fins_summary residual `2008-01…06` **not** sealed (empty pre-history DEFER formal).
- short_sale residual `2013-01…10` **not** sealed (empty pre-history DEFER formal).

## Operator repro

```bash
# continuous ops loop (ready-seal issue gap-fill + fail-closed publish)
.venv/bin/python -u .glm-logs/w0815r_g5_ops/ops_loop.py

# manual single segment (ready only; only if no peer issue / peer ops_loop alive)
.venv/bin/python scripts/issue_receipts_parallel.py \
  --dataset fins_summary --segment-id 2026-04 \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset fins_summary --segment-id 2026-04

# fail-closed publish + FRESH reclock
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```

## origin/main tip

*(filled after push)* — proof `w0815r_g5_ops_20260815.md` + residual LIVE sync (POST COMPLETE **3457** / raw **15125** / G5 issue **+0** / session COMPLETE **+0** / Dataset COMPLETE **11** / OTC **72** / empty **0** / Mass **NO-GO** / tokyo_repo local **30303** / D1 hot **252** / CF-SoT confirmed).

## Orchestrator FINAL metrics

```json
{
  "wave": "W25-G5",
  "tag": "w0815r_g5_ops",
  "pre_complete": 3457,
  "post_complete": 3457,
  "d_complete": 0,
  "pre_raw_n": 15102,
  "post_raw_n": 15125,
  "d_raw": 23,
  "dataset_complete": 11,
  "empty_complete": 0,
  "g5_issued": 0,
  "force_used": false,
  "peers_killed": 0,
  "mass": "NO-GO",
  "phase7": "OFF",
  "active_gen": "projgen-9ee87879ce1843a0aa89f4801b8b7549",
  "otc_complete": 72,
  "fins_summary": {"complete": 218, "partial": ["2008-01","2008-02","2008-03","2008-04","2008-05","2008-06"]},
  "short_sale_partial": ["2013-01","2013-02","2013-03","2013-04","2013-05","2013-06","2013-07","2013-08","2013-09","2013-10"],
  "peer_ops_cycles": 3,
  "tokyo_repo_rows": {"local": 30303, "d1_hot": 252},
  "cf_sot_language": "CONFIRMED"
}
```
