# G1: equities_bars_daily acq + COMPLETE growth (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty-raw ban:** held (`_is_usable_raw` + digest raw ≤20MB; no empty COMPLETE)  
**Worker pass ≠ Coverage COMPLETE** (until signed receipt + promote/publish)  
**Did not kill** peer jobs (topix / indices / master / breakdown / fins / deriv / edinet)

**Prefix:** `w0814_g1_bars_*`  
**Tip base:** `cac338b` (origin/main at start)

---

## Objective

Grow `equities_bars_daily` **COMPLETE segs from 42** via:

1. week-chunk CF premium backfill (`max-jobs 200`, workers 3, general-rpm 495)  
2. R2→local raw+structured seal for months with non-empty full-month raw (floor **2008-05**)  
3. `issue_receipts_parallel` (signed) + promote COMPLETE + publish  
4. reeval `observed_window`  
5. proof + commit/push  

**Forbidden held:** no Mass; no empty COMPLETE; no kill of other jobs; no COMPLETE of bars **pre-2008-05** (empty API band DEFER).

---

## PRE

| metric | value |
|--------|------:|
| bars COMPLETE segs (remote + local) | **42** |
| bars PARTIAL segs | 230 |
| COMPLETE band | 2008-05…2010-10 + 2024-01..03, 2025-04, 2026-01..08 |
| `observed_start` | **2008-05-01** |
| `observed_end` | 2026-08-12 (local meta; remote window 2008-05-01..2026-08-13) |
| dataset status | PARTIAL |
| remote raw_retention_manifests n / c / nz / sum_rows | **2273** / **1875** / **1666** / **30_053_224** |

Artifacts: `.glm-logs/w0814-g1-bars/PRE_*.json`

### Residual (planner)

| field | value |
|------:|
| plan_jobs week-chunks (no floor) | **864** (matches residual brief) |
| plan_jobs with `--from-date 2008-05-01` | **774** (skip pre-2008 empty band + COMPLETE months) |
| first residual week | **2010-11-04** (after COMPLETE through 2010-10) |

---

## 1) Execute backfill (week-chunks)

Command (as specified; floor added after aborted pre-2008 empty-band burn — see note):

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_bars_daily \
  --from-date 2008-05-01 --to-date 2026-08-13 \
  --execute --week-chunks --chunk-days 7 \
  --workers 3 --general-rpm 495 --max-jobs 200 \
  --sleep-on-retry 3 \
  --plan-out .glm-logs/cf-backfill/w0814_g1_bars_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814_g1_bars_queue.json \
  --state-out .glm-logs/cf-backfill/w0814_g1_bars_state.jsonl
```

| field | value |
|-------|------:|
| plan / queued / executed | **774** / **200** / **200** |
| **pass / fail** | **124 / 76** |
| fail mode | worker **HTTP 429** (retries exhausted) under concurrent general-pool peers |
| rowsInserted sum (pass detail) | **1_768_533** |
| host_dispatch_rpm | **~13.67** (window ~874s; first→last host POST) |
| http_429_count (host envelope) | **0** on outer POST (429 inside worker pagination) |
| pass date range | **2010-11-25 … 2014-09-03** week chunks |
| pass unique months | **32** |

### Pre-2008 false start (archived; not COMPLETE)

Initial exact command without `--from-date` queued residual from **2006-08** empty API band (`rowsInserted=0`).  
Own G1 PID only was stopped (~69 state lines archived → `w0814_g1_bars_state_pre2008_partial.jsonl`); **peers not killed**.  
Restarted with floor **2008-05-01** so max-jobs budget grows sealable residual after 2010-10.

### Peer concurrent (not killed)

topix `w0814_g3`, indices, master, markets_breakdown, fins family, derivatives, edinet — shared general pool → bars 429 rate elevated.

---

## 2) R2 seal (raw-required only)

Script: `.glm-logs/w0814-g1-bars/seal_bars_from_r2.py`  
(from w0713 T1 seal path; paths → `w0814-g1-bars`)

Select full-month COMPLETE manifests with `row_count≥20000`, floor `2008-05`, skip already COMPLETE.

| field | value |
|------:|
| unsealed full-month candidates | **76** |
| **selected / ready** | **30 / 30** |
| segment range | **2010-11 … 2013-04** continuous |
| empty / error | **0** |
| digest raw | non-empty JSON ≤20MB (issue_receipts 25MB usable cap) |
| structured upsert | full month pages (empty-raw ban held) |

Result: `.glm-logs/w0814-g1-bars/seal_result.jsonl`  
Note: week-chunk acq this wave fills residual R2 inventory; seal used **pre-existing full-month** remote manifests (run_ids ~801–830) already COMPLETE in raw_retention — not week windows as sole evidence.

---

## 3) Signed receipts + COMPLETE

```text
for seg in 2010-11 … 2013-04:
  .venv/bin/python scripts/issue_receipts_parallel.py \
    --datasets equities_bars_daily --segment-id $seg \
    --workers 1 --no-refresh --json-summary
  .venv/bin/python scripts/restore_local_complete_from_receipt.py \
    --dataset equities_bars_daily --segment-id $seg
```

| field | value |
|------:|
| issue loop | **30 OK / 0 FAIL** (run_id **901622…901651**) |
| promote COMPLETE | **30 OK / 0 FAIL** |
| eligibility | `TRUSTED_COLLECTION` / `SignedReceiptAuthority` / `dev-receipt-v1` |
| raw_row_count == structured_row_count | yes |

### Publish (fail-closed)

```text
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
```

| field | value |
|------:|
| complete_count_guard | **ok local=1290 remote=1260 force=False** |
| remote projection | **applied** |
| empty COMPLETE (`receipt_run_id` null/0) | **0** |

---

## 4) Reeval observed_window

```text
.venv/bin/python scripts/ops_reeval_observed_window.py --dataset equities_bars_daily
```

| field | PRE | POST |
|-------|-----|------|
| **observed_start** | **2008-05-01** | **2008-05-01** (unchanged; no pre-2008 fabricate) |
| observed_end | 2026-08-12 | **2026-08-13** |
| status | PARTIAL | PARTIAL |
| C8 | pass lag 2 | **pass** lag **1** |
| COMPLETE segs | 42 | **72** (via seal path, not reeval inflation) |
| receipt n / sum_raw (reeval window) | — | **1663** / **25_443_166** |

`coverage_segments` not rewritten by reeval (by design).

---

## POST Δ

| metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **bars COMPLETE segs** | **42** | **72** | **+30** |
| bars PARTIAL | 230 | 200 | −30 |
| remote raw n | 2273 | **2542** | **+269** |
| remote raw COMPLETE | 1875 | **2042** | **+167** |
| remote raw nz | 1666 | **1800** | **+134** |
| remote raw sum_rows | 30_053_224 | **31_879_795** | **+1_826_571** |
| observed_start | 2008-05-01 | 2008-05-01 | 0 |
| observed_end | 2026-08-12 | 2026-08-13 | +1d |
| empty COMPLETE | 0 | **0** | 0 |

### COMPLETE months added (30)

**2010-11 … 2013-04** continuous.

Prior COMPLETE retained: 2008-05…2010-10, 2024-01..03, 2025-04, 2026-01..08.

---

## Acceptance

| gate | result |
|------|--------|
| week-chunks max-jobs 200 | **PASS** (executed 200) |
| pass/fail reported | **PASS** (124/76) |
| host rpm reported | **PASS** (~13.67) |
| COMPLETE segs increased from 42 | **PASS** → **72** (+30) |
| only raw months sealed | **PASS** (R2 nz full-month only) |
| empty COMPLETE ban | **PASS** |
| no pre-2008-05 COMPLETE | **PASS** |
| observed_start held at 2008-05-01 | **PASS** |
| reeval bars | **PASS** |
| no Mass / no kill peers | **PASS** |
| commit + push | see tip SHA below |

---

## Explicit non-claims

- **No** dataset-level COMPLETE / Mass / Research READY / Phase7 ON  
- **No** COMPLETE for 2006-08…2008-04 empty API shells  
- Residual week-jobs after max-jobs cap still large (~574+ of 774) until more COMPLETE skip  
- Remaining PARTIAL months after 2013-04 (except recent COMPLETE islands) **DEFER** next seal wave (~46+ full-month candidates still unsealed)  
- Worker / R2 pass alone ≠ Coverage COMPLETE  

---

## Artifacts

| path | role |
|------|------|
| `.glm-logs/cf-backfill/w0814_g1_bars_{plan,queue,state,run}.*` | backfill execute |
| `.glm-logs/cf-backfill/w0814_g1_bars_state_pre2008_partial.jsonl` | aborted pre-2008 burn |
| `.glm-logs/w0814-g1-bars/seal_bars_from_r2.py` | R2→local seal driver |
| `.glm-logs/w0814-g1-bars/seal_{map,result,run}.*` | seal evidence |
| `.glm-logs/w0814-g1-bars/issue_{targeted,run}.*` | signed issue loop |
| `.glm-logs/w0814-g1-bars/promote_complete.log` | local COMPLETE promote |
| `.glm-logs/w0814-g1-bars/{publish,reeval}.txt` | publish + reeval |
| `.glm-logs/w0814-g1-bars/PRE_*.json` / `POST_*.json` | PRE/POST snapshots |

## Commands (reproducible)

```bash
.venv/bin/python scripts/ops/cf_premium_backfill.py \
  --datasets equities_bars_daily --from-date 2008-05-01 --to-date 2026-08-13 \
  --week-chunks --chunk-days 7 --workers 3 --general-rpm 495 --max-jobs 200 \
  --sleep-on-retry 3 \
  --plan-out .glm-logs/cf-backfill/w0814_g1_bars_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814_g1_bars_queue.json \
  --state-out .glm-logs/cf-backfill/w0814_g1_bars_state.jsonl

.venv/bin/python scripts/ops_reeval_observed_window.py --dataset equities_bars_daily
```
