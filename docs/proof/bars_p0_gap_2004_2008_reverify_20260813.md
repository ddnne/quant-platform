# P0 bars: 2004-01 → 2008-04 gap reverify (DEFER / impossible advance)

**Date:** 2026-08-13  
**Base tip at start:** `9a6e930`  
**Mass / READY / raw無し COMPLETE:** **NO-GO**  
**Worker pass ≠ Coverage COMPLETE** (bars COMPLETE segs remain **12**)  
**cf_premium_backfill dual-run:** **avoided** (no driver execute; smoke via direct `/v1/run` only; margin agent may use wrangler RO)

## Objective

Re-verify tip gap-fill / mid-hole proofs for **calendar 2004-01 → 2008-04** against live remote D1 + worker smoke:

1. Confirm **contract / subscription floor** (catalog vs API).
2. Confirm **empty `data[]`** band with raw_n / HTTP / smoke.
3. Advance `observed_start` **only if** SUCCESS + `raw_row_count>0` exists earlier.
4. Else leave **honest DEFER** + residual (do not fabricate `observed_start`).

Tip priors:  
`docs/proof/bars_gap_20060812_20080430_20260812.md`,  
`docs/proof/bars_p0_gap_midhole_20260813.md`,  
`docs/proof/bars_history_observed_start_20260812.md`.

---

## Dual-run gate

| check | result |
|-------|--------|
| `cf_premium_backfill.py` processes | **none** at smoke time |
| This pass mode | **smoke only** — direct POST `/v1/run` (not the multi-job backfill driver) |
| Full re-execute 2006-08→2008-04 week-chunks | **not repeated** (already 90/90 empty shells on tip; reverify smokes sufficient) |

---

## Floors (three distinct bounds)

| bound | value | source |
|-------|------:|--------|
| Catalog / Coverage Contract `history_target_start` | **2004-01-05** | `packages/data_plane/data_contracts/collection_coverage.json` → `equities_bars_daily` |
| Track A focus filter start | **2004-01-05** | `packages/data_plane/ops/range_batch_scheduler.py` `TRACK_A_FOCUS_RANGES` |
| **Subscription / API entitlement floor** | **2006-08-13 ~** | live worker fail detail (J-Quants HTTP **400**) |
| First **non-empty** API probe (this reverify) | **2008-05-06** | smoke `rowsInserted=4988` |
| Receipt-plane `observed_start` (nz SUCCESS min) | **2008-05-01** | remote D1 `collection_receipts` |

**Implication:** catalog 2004 is **not** a fillable Premium entitlement for this tenant. Planner may *request* 2004; API rejects pre-**2006-08-13**.

---

## PRE / live remote D1 (`quant-ingest` via wrangler)

```text
platform/workers/ingestion-premium/node_modules/.bin/wrangler d1 execute quant-ingest \
  --remote --config=platform/workers/ingestion-premium/wrangler.toml --json --command="..."
```

| metric | value |
|--------|------:|
| `dataset_coverage.observed_start` | **2008-05-01** |
| `dataset_coverage.observed_end` | 2026-08-12 |
| `dataset_coverage.status` | PARTIAL |
| `dataset_coverage.row_count` | 803862 (hot residual) |
| `dataset_coverage.evaluated_at` | 2026-08-13T00:56:50Z |
| bars COMPLETE segs | **12** |
| SUCCESS receipts all | n=**1202** (post-smoke), n_nz=**966**, min_nz=**2008-05-01**, sum_raw≈17.1M |
| SUCCESS receipts **2006-08 → 2008-04** | n=**199**, **n_nz=0**, sum_raw=**0** |
| SUCCESS receipts **2004-01 → 2006-08-12** | n=**1**, n_nz=**0**, sum_raw=**0** |
| bars `raw_retention_manifests` | n=**1401**, COMPLETE=**1300**, n_nz=**1064**, sum_rows=**23_380_917** |

### Gap months (SUCCESS receipts by `segment_start` month)

Every month **2006-08 … 2008-04**: **n_nz=0 / sum_raw=0**.  
**2008-05**: n_nz=**19**, sum_raw=**221_893** (first nz month).

Empty SUCCESS shells **must not** extend `observed_*` (coverage_ledger rule).

---

## Smoke (direct `/v1/run`, not dual backfill)

Artifact: `.glm-logs/cf-backfill/bars_p0_gap_reverify_20260813b.json`  
Worker: `https://quant-platform-ingestion-premium.taku-haga.workers.dev`

| tag | range | http | status | rowsInserted | rawBytes | verdict |
|-----|-------|-----:|--------|-------------:|---------:|---------|
| pre_contract_2004 | 2004-01-05 → 2004-01-09 | 200 | **fail** | 0 | 0 | **OOS** — upstream HTTP **400** subscription |
| pre_floor_20060801 | 2006-08-01 → 2006-08-12 | 200 | **fail** | 0 | 0 | **before floor** 2006-08-13 |
| floor_day_20060813 | 2006-08-13 → 2006-08-18 | 200 | pass | **0** | 72 | empty `data[]` shell |
| gap_empty_200703 | 2007-03-01 → 2007-03-07 | 200 | pass | **0** | 84 | empty |
| gap_empty_200706 | 2007-06-01 → 2007-06-05 | 200 | pass | **0** | 60 | empty |
| gap_empty_200804 | 2008-04-01 → 2008-04-05 | 200 | pass | **0** | 60 | empty |
| gap_empty_200804_end | 2008-04-28 → 2008-04-30 | 200 | pass | **0** | 36 | empty |
| first_may_20080501 | 2008-05-01 → 2008-05-02 | 200 | pass | **0** | 24 | still empty (GW calendar) |
| first_nz_20080506 | 2008-05-06 → 2008-05-08 | 200 | pass | **4988** | ~3.3MB | **first non-empty** |
| control_201006 | 2010-06-01 → 2010-06-05 | 200 | pass | **9757** | ~6.5MB | control non-empty |

### Entitlement fail detail (verbatim shape)

```text
HTTP 400: {"message": "Your subscription covers the following dates: 2006-08-13 ~ .
If you want more data, please check other plans:https://jpx-jquants.com/#dataset"}
```

Captured for both `2004-01-05..09` and `2006-08-01..12` (and single-day `2006-08-12`).

---

## Why `observed_start` cannot advance (execute impossible for P0 goal)

| sub-range | raw evidence | HTTP / smoke | action |
|-----------|--------------|--------------|--------|
| **2004-01 → 2006-08-12** | no nz SUCCESS; catalog wants 2004 but **API 400** | fail + subscription message | **DEFER / OOS** — not fillable on this plan |
| **2006-08-13 → 2008-04-30** | SUCCESS shells n≈199, **raw_n=0** entire band; tip already dispatched **90/90** week-chunks with `rowsInserted=0` | smoke pass + empty `data[]` (rawBytes 36–84) | **DEFER** — empty SUCCESS only; **must not** move `observed_start` |
| **2008-05-01+** | nz SUCCESS min **2008-05-01**; first large probe **2008-05-06** | non-empty | floor holds |

**No full `cf_premium_backfill --execute` re-run** of the empty band: would only mint more `raw_row_count=0` shells and risk dual-run with margin. Prior tip execute already closed the dispatch surface.

---

## POST

| metric | value | Δ vs goal |
|--------|------:|-----------|
| **observed_start** | **2008-05-01** | **unchanged** (no advance into 2004–2008-04) |
| observed_end | 2026-08-12 | — |
| status | PARTIAL | — |
| COMPLETE segs bars | 12 | 0 inflation |
| min_nz SUCCESS | 2008-05-01 | holds |

Reeval not required for advance (no new nz pre-2008-05 receipts). Smoke may have added empty/May shells; union rule still floors at **2008-05-01**.

---

## Acceptance

| gate | result |
|------|--------|
| Reverify contract floor (catalog 2004 vs API 2006-08-13) | **PASS** (wrangler + smoke HTTP 400 text) |
| Reverify empty `data[]` 2006-08-13→2008-04 | **PASS** (smoke rawBytes tiny + raw_n=0 on gap receipts) |
| Advance `observed_start` before 2008-05 | **FAIL / impossible** — honest |
| No dual `cf_premium_backfill` | **PASS** (smoke-only) |
| No fabricated COMPLETE / Mass / READY | **PASS** |

---

## Explicit non-claims

- **No** `observed_start` < **2008-05-01**
- **No** claim that catalog `history_target_start=2004-01-05` is currently fetchable under this Premium subscription
- **No** claim that empty worker pass for 2006–2008-04 == historical bars present
- **No** equities_bars_daily COMPLETE inflation
- Worker pass ≠ Coverage COMPLETE seal

---

## Residual (carry forward)

| item | note |
|------|------|
| P0 bars gap **2004-01 → 2008-04** | **DEFER** — OOS + empty API; not a code/backfill bug |
| To move `observed_start` earlier | need SUCCESS + **`raw_row_count>0`** before 2008-05 (requires non-empty J-Quants responses or higher plan) |
| Entitlement message | plan text **`2006-08-13 ~`**; `2006-08-12` and earlier → 400 |
| Empty COMPLETE raw manifests | worker may mark completeness COMPLETE with row_count=0; coverage seal must still require structured+policy gates |
| Hot `row_count=803862` | D1 residual only; history SoT is R2 raw + receipts |

## Commands (reproducible)

```bash
# dual-run gate
ps -ax -o pid= -o command= | awk '/cf_premium_backfill\.py/ {print}'

# coverage + nz floor
platform/workers/ingestion-premium/node_modules/.bin/wrangler d1 execute quant-ingest \
  --remote --config=platform/workers/ingestion-premium/wrangler.toml --json \
  --command="SELECT observed_start, observed_end, status FROM dataset_coverage WHERE dataset='equities_bars_daily'"

platform/workers/ingestion-premium/node_modules/.bin/wrangler d1 execute quant-ingest \
  --remote --config=platform/workers/ingestion-premium/wrangler.toml --json \
  --command="SELECT COUNT(*) n, SUM(CASE WHEN COALESCE(raw_row_count,0)>0 THEN 1 ELSE 0 END) n_nz, MIN(CASE WHEN COALESCE(raw_row_count,0)>0 THEN segment_start END) min_nz FROM collection_receipts WHERE dataset='equities_bars_daily' AND status='SUCCESS'"

# smoke artifact from this pass
cat .glm-logs/cf-backfill/bars_p0_gap_reverify_20260813b.json
```
