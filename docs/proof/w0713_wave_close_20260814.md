# W0713 wave close — G10 T15+T16+T17 (2026-08-14)

**Mass / READY / Phase7:** **NO-GO / OFF**  
**empty COMPLETE:** **0** (`receipt_run_id` null/0 = 0)  
**kill acq jobs:** **none** — peers left running (`cf_premium_backfill` t6/t7, `seal_from_r2` t4/t6, `issue_receipts` bars)

**Session PRE baseline (task brief):** tip `83fe7c0`; raw **n=7917** / **c=7015**; COMPLETE segs **585**;  
bars **12** / master **94** / topix **32** / breakdown **32**

**Base tip at G10 close start:** `100be1c` (origin/main after peer residual tip-sets)

## Scope

| Step | Track | Result |
|------|-------|--------|
| T15 | last_run + peer backfill monitor | cron/manual last_run healthy; peer acq alive; **no kill**; **no double resume** (fails still owned by running peers) |
| T16 | state jsonl host RPM + remote raw Δ + COMPLETE Δ | raw **7917→9387** (+1470); COMPLETE segs **585→729** (+144); RPM samples below |
| T17 | residual live-sync + this proof | stagnant-4 COMPLETE **all changed**; FRESH age=0; Mass NO-GO |

**Forbidden held:** Mass OFF; no empty COMPLETE; no kill acq; Phase7 OFF; no Mass launch.

## T15 — last_run / peer backfill monitor

`/health` last_run (cron earlier in session): `status=pass` datasets=23 failed=0.  
Live peer processes at close (not killed):

| PID role | note |
|----------|------|
| `cf_premium_backfill` t7 margin/investor/… | state n≈756; pass 711 / fail 45; still running |
| `cf_premium_backfill` t6 edinet/deriv retry | retry state **35/35 pass** |
| `seal_from_r2` t4 mb / t6 deriv | peer seals |
| `issue_receipts_parallel` bars | peer T1 chain |

**Resume policy:** fail residual exists in peer state jsonl, but owning PIDs still alive → **no G10 one-shot resume** (二重禁止).

## T16 — host RPM (state jsonl) + remote Δ

### Host POST/min samples (session / wave state)

| Track | host POST/min | n | by state |
|-------|--------------:|--:|----------|
| t7 margin_inv (live) | **12.21** | 750+ | pass dominant / ~45 fail |
| t6 deriv_edinet_retry | **2.44** | 35 | 35 pass |
| t6 deriv_edinet primary | **1.52** | 60 | 41p/19f |
| t1 bars exec | **7.16** | 120 | 86p/34f |
| t1 bars retry | **7.50** | 22 | 21p/1f |
| t2 master | **4.97** | 147 | 63p/84f |
| t3 topix exec | **54.28** | 192 | 192 pass |
| t4 mb residual | **10.11** | 44 | 35p/9f |
| t5_div_pre (prior close) | **4.68** | 120 | 120 pass |

### Remote D1 raw + COMPLETE segs

DB: `quant-ingest` (`platform/workers/ingestion-premium/wrangler.toml`).

| Metric | PRE (brief) | POST (~23:20Z) | Δ |
|--------|------------:|---------------:|--:|
| `raw_retention_manifests` total (`raw_n`) | **7917** | **9387** | **+1470** |
| raw completeness=COMPLETE (`raw_c`) | **7015** | **8267** | **+1252** |
| `coverage_segments` COMPLETE | **585** | **729** | **+144** |
| empty COMPLETE (`receipt_run_id` null/0) | 0 | **0** | 0 |

## 停滞4 dataset — COMPLETE 変化（必須）

| dataset | PRE COMPLETE | POST COMPLETE | Δ | G10 seal evidence (this close) |
|---------|-------------:|--------------:|--:|--------------------------------|
| `equities_bars_daily` | **12** | **13** | **+1** | `2010-06` receipt **900772** (R2 week-merge + sealpage; struct 53674) |
| `equities_master` | **94** | **132** | **+38** | `2018-11` receipt **900771** (+ peer master history seals in same wave) |
| `indices_bars_daily_topix` | **32** | **82** | **+50** | `2023-11` **900765** + `2023-12` **900758** (+ peer topix inventory) |
| `markets_breakdown` | **32** | **33** | **+1** | `2023-12` receipt **900768** (week-chunk merge + sealpage) |

Remote segment spot-check POST:

| dataset / segment | status | receipt_run_id |
|-------------------|--------|---------------:|
| `equities_bars_daily/2010-06` | **COMPLETE** | **900772** |
| `equities_master/2018-11` | **COMPLETE** | **900771** |
| `indices_bars_daily_topix/2023-11` | **COMPLETE** | **900765** |
| `indices_bars_daily_topix/2023-12` | **COMPLETE** | **900758** |
| `markets_breakdown/2023-12` | **COMPLETE** | **900768** |

### Seal path (honest raw-required)

1. Remote `raw_retention_manifests` COMPLETE + `row_count>0` → R2 pages  
2. Combine non-empty pages → local `data/raw/jquants/2026/08/14/*_from_r2_seal_g10.json` (sealpage **&lt;25MB** usable-raw gate)  
3. `normalize_generic` + `SqliteStore.upsert` where local struct missing  
4. `scripts/issue_receipts_parallel.py` signed SUCCESS (`dev-receipt-v1`)  
5. Targeted ledger COMPLETE from eligible SUCCESS receipt  
6. `scripts/publish_ops_projection.py --apply-remote` fail-closed (`local=729 ≥ remote=677`)

**Empty-raw ban held.** Worker pass ≠ Coverage COMPLETE.

## T17 — reeval + freshness + residual

### `ops_reeval_observed_window` (stagnant-4)

No segment rewrite / no COMPLETE claim. `--today 2026-08-14 --freshness-days 7`.

| dataset | status | observed_start | observed_end | C8 |
|---------|--------|----------------|--------------|----|
| `equities_bars_daily` | **PARTIAL** | **`2008-05-01`** | **`2026-08-13`** | **pass** lag **1** |
| `equities_master` | **PARTIAL** | **`2006-08-13`** | **`2026-08-13`** | **pass** lag **1** |
| `indices_bars_daily_topix` | **PARTIAL** | **`2008-01-01`** | **`2026-08-14`** | **pass** lag **0** |
| `markets_breakdown` | **PARTIAL** | **`2015-03-26`** | **`2026-08-13`** | **pass** lag **1** |

### `ops_reeval_freshness` → FRESH age=0

| Field | POST |
|-------|------|
| status | **FRESH** |
| `generated_at` | **`2026-08-13T23:19:18.633394+00:00`** |
| `age_seconds` | **0** (at reclock) |
| `active_generation` | **`projgen-7b6c973445c74bd384ac7c960b622259`** |
| publisher | `scripts/ops_reeval_freshness.py` |
| COMPLETE segments | **untouched by freshness reclock** |

## empty COMPLETE / Mass / Phase7 / acq

| Check | Result |
|-------|--------|
| COMPLETE ∧ (`receipt_run_id` IS NULL OR =0) | **0** |
| Mass / READY / B0 | **NO-GO** |
| Phase 7 | **OFF** |
| Live acq killed? | **no** |

## Artifacts (local, gitignored logs)

- `.glm-logs/g10-w0713-seal/seal_result.jsonl` — R2 mirror ready rows  
- `.glm-logs/g10-w0713-seal/issue_*.txt` — signed issue  
- `.glm-logs/g10-w0713-seal/publish.txt` — fail-closed apply  
- `.glm-logs/g10-w0713-seal/freshness.txt` / `reeval_*.log`

## Residual

`docs/phase62_residual_status.md` live-synced to this POST (COMPLETE **729** / raw_n **9387** / stagnant-4 Δ / FRESH `projgen-7b6c…`).
