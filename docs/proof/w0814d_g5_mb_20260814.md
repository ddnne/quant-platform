# W4-G5 markets_breakdown — w0814d_g5_mb residual seal probe **DEFER** (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (`receipt_run_id` null/0 = 0)  
**Worker pass ≠ Coverage COMPLETE**  
**prefix:** `w0814d_g5_mb_*` · **did not kill** peers  
**Session PRE (task brief):** `markets_breakdown` COMPLETE **137**/164 · gap **27** · mostly pre-2015 empty **DEFER**

## Goal

1. Seal any remaining **non-empty** raw months only (empty-raw ban).  
2. If none sealable → document **DEFER** + optional residual acq.  
3. Reeval `markets_breakdown` observed window + freshness.  
4. Proof + residual SoT live-sync + **push**.

## PRE (remote D1 @ wave start)

| metric | value |
|--------|------:|
| Segment COMPLETE total | **2034** |
| `markets_breakdown` COMPLETE | **137** |
| `markets_breakdown` PARTIAL | **27** |
| `raw_retention_manifests` | **11714** |
| empty COMPLETE | **0** |
| residual PARTIAL band | **`2013-01`…`2015-03`** (27 mo) |
| COMPLETE island min/max | **`2015-04`…`2026-08`** |

## 1) Residual seal probe (non-empty raw only)

Driver: `.glm-logs/w0814d_g5_mb/mb_residual_probe.py`  
Floor/ceil: **2013-01…2015-03** · min full-month rows **20000**  
Source floor ≈ **2015-03-26** (observed_start).

| field | value |
|-------|------:|
| residual PARTIAL | **27** (`2013-01…2015-03`) |
| remote COMPLETE manifests scanned | **990** (meta nz=821 / zero=169) |
| **sealable** | **[]** |
| thin only | `2015-03` max_rows **3628** (`DEFER_thin_partial_month`) |
| empty shells | **26** (`row_count=0` densify only) |
| no_manifest in band | **0** (all residual months have shell and/or thin inventory) |
| **verdict** | **DEFER_pre2015_empty** |

Honesty: prior COMPLETE island starts **2015-04**. No empty COMPLETE invented. Thin `2015-03` is source partial-month only (from **2015-03-26**), not full-month densify.

Artifact: `.glm-logs/w0814d_g5_mb/mb_residual_probe.json`

## 2) Optional residual acq (dry-run; execute DEFER)

Pre-source densify already ran in prior waves (W2-G2 100p empty shells; W3-G5 probe). Fresh dry-run confirms planner still has week-chunks but **cannot** produce sealable raw:

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets markets_breakdown \
  --week-chunks --chunk-days 7 \
  --from-date 2013-01-01 --to-date 2015-03-31 \
  --workers 2 --general-rpm 495 --max-jobs 50 \
  --plan-out  .glm-logs/cf-backfill/w0814d_g5_mb_residual_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814d_g5_mb_residual_queue.json
# mode=dry-run plan_jobs=117 queued=50 executed=0
```

| field | value |
|-------|------:|
| plan_jobs | **117** |
| queued (max-jobs 50) | **50** |
| date span | `2013-01-04` → `2015-03-31` |
| **execute** | **DEFER** — shells already densified; expect `rowsInserted=0`; would not flip sealable |

## 3) Seal execute

**Skipped** — sealable **0**. No R2→struct seal, no issue receipts, no COMPLETE promote.

## 4) Reeval + freshness

```text
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset markets_breakdown --today 2026-08-14 --freshness-days 7
.venv/bin/python scripts/ops_reeval_freshness.py
```

| field | POST |
|-------|------|
| status | **PARTIAL** (dataset not fully COMPLETE) |
| **observed_start** | **`2015-03-26`** |
| **observed_end** | **`2026-08-13`** |
| C8 | **pass** lag **1** |
| nz SUCCESS receipts | n=**810**, sum_raw=**13_935_575** |
| coverage_segments | **untouched by reeval** |
| freshness | **FRESH** `projgen-075b61ae47c841e48076ee3974f803b5` |
| mass | **NO-GO** |

No fail-closed publish of coverage deltas (no new COMPLETE). Freshness reclock only.

## POST (remote D1 live verify)

| metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| Segment COMPLETE total | **2034** | **2034** | **0** |
| `markets_breakdown` COMPLETE | **137** | **137** | **0** |
| `markets_breakdown` PARTIAL | **27** | **27** | 0 |
| `raw_retention_manifests` | **11714** | **11731** | +17 (concurrent peers; not this wave seal) |
| empty COMPLETE | 0 | **0** | 0 |
| projection | — | **FRESH** `projgen-075b61ae…` | |

### COMPLETE islands (`markets_breakdown`)

- History continuous: **`2015-04`…`2023-11`** + tip **`2023-12`** + **`2024-01`…`2026-08`**  
- Residual PARTIAL: **`2013-01`…`2015-03`** (pre-source empty shells + thin `2015-03`) — **DEFER**

### mb residual DEFER (27)

| class | months | note |
|-------|--------|------|
| empty shells | `2013-01…2015-02` (**26**) | API returns empty `data[]` pre source floor |
| thin partial | `2015-03` (**1**) | max_rows **3628** from **2015-03-26** only |

## Acceptance

| gate | result |
|------|--------|
| seal non-empty raw only | **PASS** (sealable **0**; no seal) |
| mb COMPLETE PRE **137** → POST **137** | **PASS** (+0 honest) |
| empty COMPLETE / Mass / Phase7 OFF / peers not killed | **PASS** |
| reeval C8 pass lag1 + FRESH | **PASS** |
| optional residual acq documented | **PASS** (dry-run plan **117**/queued **50**; execute **DEFER**) |

## Explicit non-claims

- No dataset-level COMPLETE for mb (**137/164**).  
- No Mass / READY / Phase7.  
- No COMPLETE without raw; no empty `{"data":[]}` seals.  
- Worker pass ≠ COMPLETE (pre-2015 densify shells prove the distinction).  
- This wave attributed seals = **+0**.

## Artifacts

| path | role |
|------|------|
| `.glm-logs/w0814d_g5_mb/PRE_*.json` | PRE D1 snaps |
| `.glm-logs/w0814d_g5_mb/POST_*.json` | POST D1 snaps |
| `.glm-logs/w0814d_g5_mb/mb_residual_probe.py` | sealability probe |
| `.glm-logs/w0814d_g5_mb/mb_residual_probe.json` | **DEFER_pre2015_empty** |
| `.glm-logs/w0814d_g5_mb/mb_residual_probe.log` | probe log |
| `.glm-logs/w0814d_g5_mb/acq_dryrun.log` | residual acq dry-run |
| `.glm-logs/cf-backfill/w0814d_g5_mb_residual_plan.json` | plan **117** |
| `.glm-logs/cf-backfill/w0814d_g5_mb_residual_queue.json` | queue **50** |
| `.glm-logs/w0814d_g5_mb/reeval_mb.log` | observed window reeval |
| `.glm-logs/w0814d_g5_mb/freshness.log` | freshness reclock |

## Operator repro

```bash
# residual seal probe (expect sealable=[])
.venv/bin/python -u .glm-logs/w0814d_g5_mb/mb_residual_probe.py

# optional residual acq dry-run only
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets markets_breakdown \
  --week-chunks --chunk-days 7 \
  --from-date 2013-01-01 --to-date 2015-03-31 \
  --workers 2 --general-rpm 495 --max-jobs 50 \
  --plan-out  .glm-logs/cf-backfill/w0814d_g5_mb_residual_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814d_g5_mb_residual_queue.json

# reeval
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset markets_breakdown --today 2026-08-14 --freshness-days 7
.venv/bin/python scripts/ops_reeval_freshness.py
```

## Verdict

| Check | Result |
|-------|--------|
| Residual seal probe pre-2015 | **PASS** (sealable **0**; **DEFER_pre2015_empty**) |
| COMPLETE PRE **137** → POST **137** | **PASS** (**+0** honest) |
| empty COMPLETE | **0** |
| C8 pass lag1 + FRESH | **PASS** |
| optional residual acq execute | **DEFER** (dry-run only; shells densified) |

**W4-G5 w0814d_g5_mb: COMPLETE 137→137 (+0). Gap 27 pre-2015 DEFER held.**
