# G2 / T2 — `equities_master` close (raw + signed receipt) — 2026-08-14

**Mass / READY / Phase7:** **NO-GO / OFF**  
**empty COMPLETE:** **0** (`receipt_run_id` null/0 = 0)  
**kill peers:** **none** (fins/breakdown issue + peer publish left alone)  
**worker pass ≠ Coverage COMPLETE**

**Prefix:** `w0713_t2_master_*`  
**Scope:** close residual `equities_master` segments with **usable raw + structured + signed receipt** only.

## PRE / POST

| Metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **Local** `equities_master` COMPLETE segs | **94** | **220** | **+126** |
| **Remote** `equities_master` COMPLETE segs | **94** | **220** | **+126** |
| Local total COMPLETE segs (all datasets) | 585† | **846** | +261 (peers concurrent) |
| Remote total COMPLETE segs | — | **846** | — |
| empty COMPLETE | 0 | **0** | 0 |

† Session residual baseline at start of multi-agent day; this track’s honest PRE for **master** is **94**.

Projection after reeval: **FRESH** `projgen-397f5b6902e14b22b4580aeb37052cf6` (age≈0; `coverage_segments` untouched by freshness).

## 1) `cf_premium_backfill` (execute)

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_master \
  --execute --workers 3 --general-rpm 495 --max-jobs 0 \
  --plan-out  .glm-logs/cf-backfill/w0713_t2_master_plan.json \
  --queue-out .glm-logs/cf-backfill/w0713_t2_master_queue.json \
  --state-out .glm-logs/cf-backfill/w0713_t2_master_state.jsonl
```

| field | value |
|-------|------:|
| PID | **84330** (natural exit; not killed) |
| plan jobs | **147** (`2006-08` … `2018-11`) |
| executed | **147** |
| **pass** | **63** |
| **fail** | **84** |
| host POST/min | **4.97** (window ~1761s; host 429 = **0**) |

### Fail taxonomy (worker summary)

| class | n | notes |
|-------|--:|-------|
| HTTP **429** (upstream transient) | **80** | Worker→JQ; host envelope 429 = 0 |
| HTTP **503** | **3** | |
| HTTP **400** subscription edge | **1** | `2006-08` from=`2006-08-13` vs plan start `2006-08-14` |

**Note:** Prior T7/T478 already left substantial R2 raw for these months. This execute re-dispatched residual planner jobs; seal used **R2 raw with Date-in-window** (not empty / not misdated pages).

## 2) Raw mirror + structured (seal prep)

Remote `raw_retention_manifests` (`equities_master`, `row_count>0`): **242** runs.  
Manifest map → **147** PARTIAL months with some remote raw.

For each candidate month: `wrangler r2 object get quant-raw/raw/equities_master/{run_id}/page-000001.json` →  
local `data/raw/jquants/2026/08/14/equities_master_from=…_to=…_from_r2_run….json` + `normalize_generic` → local `jquants_records`.

### Window integrity gate (empty-raw / misdate ban)

| class | n | action |
|-------|--:|--------|
| **window-ok** (page `Date` ∈ segment month) | **126** | eligible for signed seal |
| **window-bad** (page `Date` stuck on `2008-05-07`) | **21** | **DEFER** — not sealed |

**DEFER months (no COMPLETE invented):**  
`2006-08` … `2008-04` (21). R2 manifests claim month windows but page bodies are identical digests of `2008-05-07` snapshots — not honest evidence for those segments.

Artifacts:  
`.glm-logs/cf-backfill/w0713_t2_master_seal_prepared.json`  
`.glm-logs/cf-backfill/w0713_t2_master_seal_window_ok.json`  
`.glm-logs/cf-backfill/w0713_t2_master_seal_window_bad.json`

## 3) Signed receipt / seal (raw-required only)

Path: `SignedReceiptAuthority` / `TRUSTED_COLLECTION` / `dev-receipt-v1`  
Policy: `source_query` `expected_items=1`, `raw_row_count == structured_row_count`, pagination exhausted.

Direct evaluate after issue (avoid hanging peer-contended full `refresh_coverage_ledger` / `run_coverage` during multi-agent DB load). Busy-timeout + retries under concurrent fins/breakdown issue + peer publish.

| wave | result |
|------|--------|
| issue1 | partial (+8) then `database is locked` (peer writers) |
| issue2 resume | **+116** COMPLETE; 2 already_complete skips |
| **net master +N** | **+126** (94 → **220**) |

Sample receipt run_ids: `2008-05`→**900745**, `2010-01`→**900777**, `2015-06`→**900887**, `2018-11`→**900771**.  
Receipt band observed: **900730–900926** (includes prior one-off probes).

POST COMPLETE inventory for master starts at **`2008-05`** (first honest window-ok month) through tip months incl. **`2026-08`**.

## 4) Publish + reeval

```text
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
# complete_count_guard ok local=846 remote=846 force=False
# remote projection applied

.venv/bin/python scripts/ops_reeval_freshness.py
# FRESH projgen-397f5b6902e14b22b4580aeb37052cf6
# coverage_segments_untouched=1 mass=NO-GO
```

Verified remote D1 after apply:

| query | result |
|-------|--------|
| `equities_master` COMPLETE / PARTIAL | **220** / **94** |
| all-datasets COMPLETE | **846** |
| empty COMPLETE | **0** |

## Explicit non-claims / DEFER

| Item | Status |
|------|--------|
| COMPLETE without usable raw | **Forbidden** — held |
| `2006-08`…`2008-04` (21) | **DEFER** — R2 pages misdated (`2008-05-07` bodies) |
| Pre-plan PARTIAL `2000-07`…`2006-07` | **DEFER** — outside planner residual / no honest raw |
| Backfill fail residual (84, mostly 429) | **OPEN** for optional retry; seal did not depend on this wave’s worker-pass |
| Dataset-level COMPLETE for `equities_master` | still **PARTIAL** (history hole pre-2008-05) |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| Peer acq/issue killed? | **no** |

## Verdict

| Check | Result |
|-------|--------|
| master COMPLETE PRE→POST | **94 → 220 (+126)** local = remote |
| raw-required seal only | **PASS** |
| empty COMPLETE | **0 PASS** |
| no peer kill | **PASS** |
| Mass / Phase7 | **NO-GO / OFF PASS** |
| Overall G2/T2 master close | **PASS** (honest +126; 21 misdated months DEFER) |

## Operator repro (seal only, after R2 raw exists)

```bash
# dry: window-ok months already mirrored under data/raw/jquants/2026/08/14/
.venv/bin/python scripts/issue_receipts_parallel.py \
  --datasets equities_master --struct-hint --limit 200 --workers 4 --order asc --dry-run

# publish fail-closed
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
```
