# W17-G5 — w0815i ops (continuous collection: receipts + projection + DEFER formalize) (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**force-apply:** **not** used (fail-closed guard held)  
**peers killed:** **0** (G1 options `issue_restore_fast` + `issue_receipts` drain, G2 fins densify/issue, H2 options waiters left alive)  
**Worker pass ≠ COMPLETE:** held  
**DEFER densify re-run:** **not** done (policy)  
**G5 role:** ready-seal issue gap-fill (no dual with live issue PIDs) + fail-closed publish/`ops_reeval_freshness` + residual SoT live-sync + **T15 DEFER inventory formalize** + throughput snap

**Live verified:** 2026-08-15 (JST) / ~2026-08-14T23:58Z UTC  
**Projection:** **FRESH** `projgen-a9473edc12374cad95f37ab3457b0342`  
**Artifacts:** `.glm-logs/w0815i_g5_ops/` (`ops_loop.py`, `ops_loop.stdout`, `PRE_remote.json`, `POST_remote.json`, `FINAL_metrics.json`, `publish_final.log`, `reeval_final.log`)

## PRE / POST (remote D1 `quant-ingest`)

| Metric | PRE (G5 start ~23:32Z) | POST (~23:58Z) | Δ |
|--------|-----------------------:|---------------:|--:|
| `raw_retention_manifests` | **14953** | **14991** | **+38** |
| Segment COMPLETE total | **3409** | **3426** | **+17** |
| Dataset COMPLETE | **10** (PRE snap) / **11** mid | **11** | held (options via **W15-G1**) |
| Dataset STALE | **0** | **0** | 0 |
| empty COMPLETE | **0** | **0** | held |
| FRESH generation | `projgen-6899460e…` (PRE) | `projgen-a9473edc…` | reclocked ×N |

### Dataset COMPLETE set (remote POST)

| dataset | status |
|---------|--------|
| `markets_calendar` | **COMPLETE** (held) |
| `jsda_tokyo_repo_rates` | **COMPLETE** (held) |
| `jsda_corporate_bond_transactions` | **COMPLETE** (held) |
| `edinet_major_shareholders` | **COMPLETE** (held) |
| `equities_investor_types` | **COMPLETE** (held) |
| `markets_margin_alert` | **COMPLETE** (held) |
| `markets_margin_interest` | **COMPLETE** (held) |
| `markets_short_ratio` | **COMPLETE** (held) |
| `derivatives_bars_daily_futures` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options_225` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options` | **COMPLETE** (held; **W15-G1** segs **164/164**) |

### Key segment COMPLETE (remote PRE→POST)

| dataset | PRE | POST | Δ | note |
|---------|----:|-----:|--:|------|
| `derivatives_bars_daily_options` | **157** | **164** | **+7** | peer options full-month drain (`2023-03…06` + residual); G5 dual-gate → **0** options issue |
| `fins_summary` | **200** | **210** | **+10** | G5 ready-seal **+9** + peer tip |
| platform total | **3409** | **3426** | **+17** | concurrent peers + G5 publish; G5 **not** sole owner |

## T10 — issue ready seals (no dual with live options issue)

Continuous `ops_loop` (cycles **14**/14; sleep 90s; ~**23:32–23:57Z** window) scanned seal/reagg results for `status=ready` without local COMPLETE.  
**DEFER skip:** `equities_earnings_calendar`, `equities_bars_daily_am` (tip-Date / today-mode).  
**empty-raw ban:** `raw_rows>0` gate held.  
**full-month filter:** options issue only from G1 `reagg_seal_result.jsonl` with `full_month=true` (week-partial sealpages **not** issued as COMPLETE).

| Action | Detail |
|--------|--------|
| Dual-issue gate | skip when any `issue_receipts_parallel` / `issue_driver` / `issue_promote` / `issue_restore_fast` / `issue_as_ready` alive |
| G5 direct issue+restore | **9** success (`fins_summary` ready seals — see list); options **0** (dual-gate while peer options issue alive cycles 1–9) |
| Fail / aborted | **0** |
| Peer concurrent | G1 options `issue_restore_fast` + serial `issue_receipts` (`2023-03…06`); peer fins densify/`issue_receipts` (`2024-10`); H2 post_when_ready waiters |
| Cycles w/ peer issue | dual gate held (skip while options/fins issue workers alive); gate free cycle **10+** after peer options drain |

### G5-owned issue list

| dataset | segments | n |
|---------|----------|--:|
| `fins_summary` | `2024-11`, `2025-04`, `2025-05`, `2025-06`, `2025-08`, `2026-01`, `2026-02`, `2026-03`, `2026-04` | **9** |
| **TOTAL** | | **9** |

Empty-raw ban held. No `cf_premium_backfill` launched by G5. No DEFER densify.  
PRE full-month options ready missing local COMPLETE: **4** (`2023-03…06`); peer drained all; residual ready_without_complete options **0** at loop end.

## T11 — publish fail-closed + ops_reeval_freshness (keep age low)

```text
publish_ops_projection --apply-remote
  complete_count_guard ok local>=remote force=False → applied
  (×N incl. final local=3426 remote=3423 → applied → remote 3426)
  no --force

ops_reeval_freshness ×N
  … → projgen-a9473edc12374cad95f37ab3457b0342
  coverage_segments_untouched=1 mass=NO-GO
```

Fail-closed: publish only when **local COMPLETE ≥ remote**. `--force-apply-remote` **not** used.  
Projection age kept low via continuous reclock during peer COMPLETE growth (loop gens: `9fa5a02d…` → `f8be6b37…` → `85bb7628…` → `ea44ab0b…` → `b96d7d92…` → final `a9473edc…`).

## T12 — residual SoT live-sync

- Updated `docs/phase62_residual_status.md` LIVE header/snapshot to remote POST **3426** / raw **14991** / Dataset COMPLETE **11** / FRESH `projgen-a9473edc…`
- Host POST row + proof index for **W17-G5**
- This proof: `docs/proof/w0815i_g5_ops_20260815.md`

## T15 — DEFER inventory formalize (short_sale empty pre-history + keep empty families)

**Formally add / reaffirm D9:** `markets_short_sale_report` PARTIAL **`2013-01…10`** (**10**) = **empty pre-history** before first nz **`2013-11`**.

| segment_id | remote status POST | densify |
|------------|--------------------|---------|
| `2013-01` … `2013-10` | **PARTIAL** (all 10) | **DEFER** (no nz raw; empty-shell ban) |

Evidence held: W16-G1 `w0815h_g1_short_sale` window_ok **0/10**; first nz run **8196** (`2013-11`, **2637** rows).  
**Do not densify** unless vendor returns nz for those months **or** contract floor moves to **2013-11**.

**Keep empty (no densify / no invent COMPLETE):**

| family | DEFER ID | residual held |
|--------|----------|---------------|
| topix / idx | D1 | PARTIAL `2008-01…04` (4+4) |
| master | D2 | misdate + pre-plan empty |
| breakdown | D3 | PARTIAL `2013-01…2015-03` (27) |
| earn / am | D4 | tip-only history |
| EDINET cross/large | D6 | pre-island empty |
| short_sale | D9 | **`2013-01…10`** formal |

DEFER densify **not** re-run this session.

## T13 — throughput (raw Δ, COMPLETE Δ, dataset COMPLETE, general ~495 / fins separate)

| Track | host POST/min | n | note |
|-------|--------------:|--:|------|
| w0815 G1 general W1 (rpm~495 catalog) | **76.15** | 386 | 18p/368f 429-storm (historical) |
| w0815 G1 general W3 deriv+edinet | **5.03** | 341 | 201p/140f |
| w0815 G2 fins W1 (separate pool) | **49.58** | 79 | 0p/79f abort |
| w0815d G1 options week densify | **2.53** | 120 | 120p/0f (session carry) |
| w0815e G1 options residual week | **2.53** | 178 | 178p/0f (session carry) |
| w0815f G1 options residual week | **2.59** | 126 | 126p/0f (session carry) |
| G5 issue rate | — | **9** | fins_summary ready-seal only (no acq); dual-gate skip options entire peer drain |
| Session raw Δ | — | **+38** | 14953→14991 |
| Session COMPLETE seg Δ | — | **+17** | 3409→3426 |
| Dataset COMPLETE count | — | **11** | held (options via W15-G1) |

**Note:** general catalog target **~495** rpm (W1 burst); fins use **separate** rate pool / lower sustained rpm.

## T14 — last_run monitor (no peer kill)

| Track | state | Action |
|-------|-------|--------|
| w0815g_g1_options issue_restore_fast + issue_receipts | **ALIVE** early → **DONE** mid-window | leave alone; dual-issue skip while their issue alive |
| w0815h_g2_options post_when_ready waiters | peer scope | leave alone |
| w0815i_g2_fins_summary densify/issue | **ALIVE** | leave alone; dual-issue skip during their issue |
| w0815h_g1_short_sale residual | DEFER | leave alone |

Resume rule held: **only if dead AND residual non-DEFER**. No dual-resume. No peer kill.

## T16 — git push each batch

Proof + residual LIVE sync + T15 DEFER formalize committed and pushed to `origin/main` (see tip SHA below).

## Leak-check (漏れなし)

| Check | Result |
|-------|--------|
| empty COMPLETE | **0** |
| `--force-apply-remote` | **not** used |
| peers killed | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| dual-issue while peer issue alive | **skipped** (gate; options cycles 1–9) |
| DEFER densify re-run | **not** done |
| week-partial options COMPLETE mint | **not** done (full-month filter) |
| G5 sole claim of COMPLETE +17 | **no** — concurrent options/fins peers; G5 owned issue **9** fins_summary only |
| G5 sole claim of Dataset COMPLETE 11 | **no** — options via **W15-G1** |

## Explicit non-claims

- Dataset COMPLETE for fins/JSDA OTC **not** claimed (still PARTIAL except the eleven listed).
- G5 sole owner of raw densify **not** claimed.
- New historical acq by G5 **not** launched (no DEFER densify).
- DEFER residuals (D1–D9) **not** re-densified.
- Peer options full-month drain / Dataset COMPLETE options **not** claimed as G5-owned (dual-gate skip; W15-G1 owned).
- short_sale `2013-01…10` **not** sealed (empty pre-history DEFER formal).

## Operator repro

```bash
# continuous ops loop (ready-seal issue gap-fill + fail-closed publish)
.venv/bin/python -u .glm-logs/w0815i_g5_ops/ops_loop.py

# manual single segment (ready only; only if no peer issue alive)
.venv/bin/python scripts/issue_receipts_parallel.py \
  --dataset fins_summary --segment-id 2026-04 \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset fins_summary --segment-id 2026-04

# fail-closed publish + FRESH reclock
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```

## origin/main tip

`(pending push)` — proof `w0815i_g5_ops_20260815.md` + residual LIVE sync (POST COMPLETE **3426** / raw **14991** / G5 issue **+9** fins_summary / session COMPLETE **+17** / Dataset COMPLETE **11** / T15 DEFER formal).

## Orchestrator FINAL metrics

```json
{
  "wave": "W17-G5",
  "tag": "w0815i_g5_ops",
  "pre_complete": 3409,
  "post_complete": 3426,
  "d_complete": 17,
  "pre_raw_n": 14953,
  "post_raw_n": 14991,
  "d_raw": 38,
  "dataset_complete": 11,
  "empty_complete": 0,
  "g5_issued": 9,
  "force_used": false,
  "peers_killed": 0,
  "mass": "NO-GO",
  "phase7": "OFF",
  "active_gen": "projgen-a9473edc12374cad95f37ab3457b0342",
  "options_complete": {"pre": 157, "post": 164, "d": 7},
  "fins_summary": {"pre": 200, "post": 210, "d": 10},
  "g5_fins_issued": 9,
  "short_sale_partial": ["2013-01","2013-02","2013-03","2013-04","2013-05","2013-06","2013-07","2013-08","2013-09","2013-10"]
}
```
