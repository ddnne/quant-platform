# W21-G4 — w0815n ops (continuous collection: receipts + projection + DEFER maintain) (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**force-apply:** **not** used (fail-closed guard held)  
**peers killed:** **0** (G1 JSDA OTC tip, G2 seal harvest, G3 tip densify, peer **w0815o_g4_ops** left alive)  
**Worker pass ≠ COMPLETE:** held  
**DEFER densify re-run:** **not** done (policy)  
**G4 role:** ready-seal issue gap-fill (no dual with live issue PIDs / peer ops_loop) + fail-closed publish/`ops_reeval_freshness` + residual SoT live-sync + **LIVE+DEFER inventory maintain** + throughput snap

**Live verified:** 2026-08-15 (JST) / ~2026-08-15T01:04Z UTC  
**Projection:** **FRESH** `projgen-7b3b8bf644e64fe8ab9024e800ed3e62`  
**Artifacts:** `.glm-logs/w0815n_g4_ops/` (`ops_loop.py`, `ops_loop.stdout`, `PRE_remote.json`, `POST_remote.json`, `FINAL_metrics.json`, `publish_final.log`, `reeval_final.log`)

## PRE / POST (remote D1 `quant-ingest`)

| Metric | PRE (G4 start ~00:41Z) | POST (~01:04Z) | Δ |
|--------|-----------------------:|---------------:|--:|
| `raw_retention_manifests` | **15020** | **15057** | **+37** |
| Segment COMPLETE total | **3442** | **3457** | **+15** |
| Dataset COMPLETE | **11** | **11** | 0 (held) |
| Dataset STALE | **0** | **0** | 0 |
| empty COMPLETE | **0** | **0** | held |
| FRESH generation | `projgen-a0aa0e3a…` (PRE) | `projgen-7b3b8bf6…` | reclocked ×N |

### Dataset COMPLETE set (remote POST)

| dataset | status |
|---------|--------|
| `markets_calendar` | **COMPLETE** (held) |
| `jsda_tokyo_repo_rates` | **COMPLETE** (held) |
| `jsda_corporate_bond_transactions` | **COMPLETE** (held) |
| `edinet_major_shareholders` | **COMPLETE** (held) |
| `equities_investor_types` | **COMPLETE** (held) |
| `markets_margin_alert` | **COMPLETE** (held) |
| `markets_margin_interest` | **COMPLETE** (held) |
| `markets_short_ratio` | **COMPLETE** (held) |
| `derivatives_bars_daily_futures` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options_225` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options` | **COMPLETE** (held; **W15-G1**) |

### Key segment COMPLETE (remote PRE→POST)

| dataset | PRE | POST | Δ | note |
|---------|----:|-----:|--:|------|
| `jsda_otc_bond_reference_prices` | **57** | **72** | **+15** | peer **W21-G1** OTC tip residual seal/issue ([`w0815n_g1_jsda_otc_20260815.md`](w0815n_g1_jsda_otc_20260815.md)); G4 dual-gate N/A — ready-gap empty; G4 **not** sole claim |
| `fins_summary` | **218** | **218** | **0** | held (D10 empty pre-history `2008-01…06`) |
| `derivatives_bars_daily_options` | **164** | **164** | **0** | held Dataset COMPLETE **11** |
| platform total | **3442** | **3457** | **+15** | concurrent peers + G4 publish absorb; G4 **not** sole owner |

## T1 — issue ready seals (no dual with live issue PIDs)

Continuous `ops_loop` (cycles **14**/14; sleep 90s; ~**00:41–01:04Z** window ≈ **23 min**) scanned seal/reagg results for `status=ready` without local COMPLETE.  
**DEFER skip:** `equities_earnings_calendar`, `equities_bars_daily_am` (tip-Date / today-mode).  
**empty-raw ban:** `raw_rows>0` gate held.  
**full-month filter:** options issue only from G1 reagg seals with `full_month=true` (week-partial sealpages **not** issued as COMPLETE).

| Action | Detail |
|--------|--------|
| Dual-issue gate | skip when any `issue_receipts_parallel` / `issue_driver` / `issue_promote` / `issue_restore_fast` / `issue_as_ready` **or peer ops_loop** alive |
| G4 direct issue+restore | **0** (ready_without_complete **0** all 14 cycles — prior seals already COMPLETE; peer OTC path owned by **W21-G1**) |
| Fail / aborted | **0** |
| Peer concurrent | G1 JSDA OTC tip residual (**+15** COMPLETE); G2 seal harvest (true harvest **0** / DEFER reject); G3 tip densify raw **+**; peer **w0815o_g4_ops** ops_loop (cycles 5–14) |
| Cycles w/ peer issue | dual gate free for ready inventory (empty); peer issue_receipts seen cycle **3** (g3 tip dry scan) — issue still **0** gap |
| peer_ops_cycles | **10** (publish defer while `w0815o_g4_ops` alive) |

### G4-owned issue list

| dataset | segments | n |
|---------|----------|--:|
| *(none — ready gap empty)* | | **0** |
| **TOTAL** | | **0** |

Empty-raw ban held. No `cf_premium_backfill` launched by G4. No DEFER densify.  
PRE ready_without_complete: **0**; residual ready_without_complete **0** at loop end.

## T2 — publish fail-closed + ops_reeval_freshness (keep age low)

```text
publish_ops_projection --apply-remote
  complete_count_guard ok local>=remote force=False → applied
  (c1 local=3442 remote=3442 → applied; mid-window defer to peer ops_loop ×10;
   final local=3457 remote=3457 → applied)
  no --force

ops_reeval_freshness ×N
  c1 → projgen-b27eb45abbf14cf0bc5db14111273a04
  final → projgen-7b3b8bf644e64fe8ab9024e800ed3e62
  coverage_segments_untouched=1 mass=NO-GO
```

Fail-closed: publish only when **local COMPLETE ≥ remote**. `--force-apply-remote` **not** used.  
Projection age kept low via continuous reclock (G4 + peer gens: `a0aa0e3a…` → `b27eb45a…` → peer mid `e398ed2a…`/`50230079…`/`c0ac0865…` → final `7b3b8bf6…`).  
While peer ops_loop alive and G4 owned issue **0**, mid-cycle publish deferred (no dual thrash); **final** publish always ran fail-closed.

## T3 — residual SoT live-sync + DEFER inventory maintain

- Updated `docs/phase62_residual_status.md` LIVE header/snapshot to remote POST **3457** / raw **15057** / Dataset COMPLETE **11** / FRESH `projgen-7b3b8bf6…` / OTC **72**
- **DEFER inventory D1–D10 held** (**no** densify re-run); re-verify remote POST **2026-08-15T01:04Z**:
  - D1 topix/idx PARTIAL `2008-01…04` (4+4) re-verified
  - D3 breakdown PARTIAL `2013-01…2015-03` (27) re-verified
  - D5 OTC archive beyond tip **72** still site-timeout DEFER (tip advanced **57→72** via **W21-G1**)
  - D9 short_sale PARTIAL `2013-01…10` (10) empty pre-history re-verified
  - D10 fins_summary PARTIAL `2008-01…06` (6) empty pre-history re-verified
  - D2/D4/D6 empty families kept (no invent COMPLETE)
- This proof: `docs/proof/w0815n_g4_ops_20260815.md`

## T4 — throughput (raw Δ, COMPLETE Δ, dataset COMPLETE, general ~495 / fins separate)

| Track | host POST/min | n | note |
|-------|--------------:|--:|------|
| w0815 G1 general W1 (rpm~495 catalog) | **76.15** | 386 | 18p/368f 429-storm (historical) |
| w0815 G1 general W3 deriv+edinet | **5.03** | 341 | 201p/140f |
| w0815 G2 fins W1 (separate pool) | **49.58** | 79 | 0p/79f abort |
| w0815d G1 options week densify | **2.53** | 120 | 120p/0f (session carry) |
| w0815e G1 options residual week | **2.53** | 178 | 178p/0f (session carry) |
| w0815f G1 options residual week | **2.59** | 126 | 126p/0f (session carry) |
| G4 issue rate | — | **0** | ready gap empty (no acq) |
| Session raw Δ | — | **+37** | 15020→15057 (peer tip densify W21-G3 / W22-G2) |
| Session COMPLETE seg Δ | — | **+15** | 3442→3457 (peer OTC **+15** W21-G1) |
| Dataset COMPLETE count | — | **11** | held |

**Note:** general catalog target **~495** rpm (W1 burst); fins use **separate** rate pool / lower sustained rpm.

## T5 — last_run monitor (no peer kill)

| Track | state | Action |
|-------|-------|--------|
| w0815n_g1_jsda OTC tip residual | **DONE** mid (OTC **+15** → **72**) | leave alone; G4 publish absorbed peer COMPLETE |
| w0815n_g2_seal_harvest | **DONE** (true harvest **0**) | leave alone |
| w0815n_g3_tip densify / issue dry | **ALIVE** early → **DONE** | leave alone; dual-issue skip if issue alive |
| w0815o_g4_ops ops_loop | **ALIVE** cycles 5–14 | leave alone; publish defer while peer ops alive |
| short_sale `2013-01…10` / fins_summary `2008-01…06` | DEFER empty pre-history | leave alone; densify **not** re-run |

Resume rule held: **only if dead AND residual non-DEFER**. No dual-resume. No peer kill.

## T6 — git push each batch

Proof + residual LIVE sync + DEFER maintain committed and pushed to `origin/main` (see tip SHA below).

## Leak-check (漏れなし)

| Check | Result |
|-------|--------|
| empty COMPLETE | **0** |
| `--force-apply-remote` | **not** used |
| peers killed | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| dual-issue while peer issue / peer ops alive | **N/A** for issue (ready gap empty); publish defer while peer ops alive |
| DEFER densify re-run | **not** done |
| week-partial options COMPLETE mint | **not** done (full-month filter; options already COMPLETE) |
| G4 sole claim of COMPLETE +15 | **no** — peer **W21-G1** OTC tip; G4 owned issue **0** |
| G4 sole claim of Dataset COMPLETE 11 | **no** — held from prior waves (options via **W15-G1**) |

## Explicit non-claims

- Dataset COMPLETE for fins/JSDA OTC **not** claimed (OTC still PARTIAL except the eleven listed COMPLETE datasets).
- G4 sole owner of raw densify **not** claimed.
- New historical acq by G4 **not** launched (no DEFER densify).
- DEFER residuals (D1–D10) **not** re-densified.
- Peer OTC tip **+15** **not** claimed as G4-owned issue (peer **W21-G1** path; G4 publish-only absorb).
- short_sale `2013-01…10` / fins_summary `2008-01…06` **not** sealed (empty pre-history DEFER held).

## Operator repro

```bash
# continuous ops loop (ready-seal issue gap-fill + fail-closed publish)
.venv/bin/python -u .glm-logs/w0815n_g4_ops/ops_loop.py

# manual single segment (ready only; only if no peer issue alive)
.venv/bin/python scripts/issue_receipts_parallel.py \
  --dataset fins_summary --segment-id 2026-04 \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset fins_summary --segment-id 2026-04

# fail-closed publish + FRESH reclock
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```

## origin/main tip

`(pending push)` — proof `w0815n_g4_ops_20260815.md` + residual LIVE sync (POST COMPLETE **3457** / raw **15057** / G4 issue **+0** / session COMPLETE **+15** peer OTC / Dataset COMPLETE **11** / DEFER D1–D10 held).

## Orchestrator FINAL metrics

```json
{
  "wave": "W21-G4",
  "tag": "w0815n_g4_ops",
  "pre_complete": 3442,
  "post_complete": 3457,
  "d_complete": 15,
  "pre_raw_n": 15020,
  "post_raw_n": 15057,
  "d_raw": 37,
  "dataset_complete": 11,
  "empty_complete": 0,
  "g4_issued": 0,
  "force_used": false,
  "peers_killed": 0,
  "mass": "NO-GO",
  "phase7": "OFF",
  "active_gen": "projgen-7b3b8bf644e64fe8ab9024e800ed3e62",
  "jsda_otc": {"pre": 57, "post": 72, "d": 15},
  "fins_summary": {"pre": 218, "post": 218, "d": 0},
  "options_complete": {"pre": 164, "post": 164, "d": 0},
  "short_sale_partial": ["2013-01","2013-02","2013-03","2013-04","2013-05","2013-06","2013-07","2013-08","2013-09","2013-10"],
  "fins_summary_partial": ["2008-01","2008-02","2008-03","2008-04","2008-05","2008-06"]
}
```
