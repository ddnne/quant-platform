# W2-G3 indices — `indices_bars_daily_topix` gap + `indices_bars_daily` residual (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (SUCCESS `raw_row_count>0` required)  
**Worker pass ≠ Coverage COMPLETE**  
**prefix:** `w0814b_g3_*` · general pool · **did not kill** peers (g2 mb / g5–g8 / edinet seals left alive)

**Base tip at start:** `be7ad33` → live tip at close `c512385` (peers landed during wave)  
**Live verified:** 2026-08-14 (JST) / ~2026-08-14T04:15Z UTC  
**Projection:** **FRESH** `projgen-dcfb986b206e497fab7ff19c2f4a0d06` age=0

## Objective

| track | dataset | action | params |
|-------|---------|--------|--------|
| **G3-topix** | `indices_bars_daily_topix` | gap=4 (`2008-01…04`) raw/seal if non-empty | workers **2**, general-rpm **495**, max-jobs **10** |
| **G3-idx** | `indices_bars_daily` | residual acq + real-raw seal | workers **2**, general-rpm **495**, max-jobs **100** |

**Forbidden held:** no Mass; no empty COMPLETE; no invent receipt without raw; no peer kill.

---

## PRE (remote D1 `quant-ingest` + local)

Artifacts: `.glm-logs/w0814b-g3-indices/PRE_*.json`

| metric | value |
|--------|------:|
| **topix COMPLETE** | **220** / 224 |
| topix PARTIAL | **4** (`2008-01`, `2008-02`, `2008-03`, `2008-04`) |
| **idx COMPLETE** | **33** / 224 |
| idx PARTIAL | **191** |
| platform segment COMPLETE total | **1376** |
| empty COMPLETE (topix/idx) | **0** |

Dry-run residual (planner, local COMPLETE skip):

| dataset | plan_jobs | queued (max-jobs) |
|---------|----------:|------------------:|
| `indices_bars_daily_topix` gap `2008-01-01…2008-04-30` | **4** | **4** |
| `indices_bars_daily` | **191** | **100** (cap 100) |

---

## 1. Residual / gap backfill (`cf_premium_backfill`)

### topix gap — `w0814b_g3_topix_gap_*`

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily_topix \
  --from-date 2008-01-01 --to-date 2008-04-30 \
  --execute --workers 2 --general-rpm 495 --max-jobs 10 \
  --sleep-on-retry 3.0 \
  --plan-out .glm-logs/cf-backfill/w0814b_g3_topix_gap_exec_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814b_g3_topix_gap_exec_queue.json \
  --state-out .glm-logs/cf-backfill/w0814b_g3_topix_gap_exec_state.jsonl
```

| field | value |
|-------|------:|
| plan / queued / executed | **4 / 4 / 4** |
| **pass / fail** | **4 / 0** |
| host POST rpm | **65.03** (window ~2.8s) |
| http_429_count | **0** |
| remote run_ids | **9445–9448** |
| **row_count** | **0** every month |

Worker pass only — **empty API band**. Empty-raw ban → **no seal / no COMPLETE**.

Manifests (local): `.glm-logs/w0814b-g3-indices/topix/manifests/indices_bars_daily_topix/{9445..9448}.json`  
params `2008-01…04` full months, `row_count=0`, completeness=COMPLETE (raw empty shell).

### idx residual — `w0814b_g3_idx_*`

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily \
  --from-date 2008-01-01 --to-date 2026-08-13 \
  --execute --workers 2 --general-rpm 495 --max-jobs 100 \
  --sleep-on-retry 3.0 \
  --plan-out .glm-logs/cf-backfill/w0814b_g3_idx_exec_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814b_g3_idx_exec_queue.json \
  --state-out .glm-logs/cf-backfill/w0814b_g3_idx_exec_state.jsonl
```

| field | value |
|-------|------:|
| plan_jobs (full residual) | **191** |
| queued / executed (max-jobs) | **100 / 100** |
| **pass / fail** | **100 / 0** |
| host POST rpm | **4.61** (window ~1287s) |
| http_429_count | **0** |
| segment span (state) | **2008-01…2017-02** residual months (planner order) |

Worker pass densifies R2 only — COMPLETE requires raw + structured + signed SUCCESS.

---

## 2. Seal months with **real raw only**

Path (empty-raw ban held):

1. Remote `raw_retention_manifests` COMPLETE + `row_count>0` (prefer newest run / month).  
2. `wrangler r2 object get quant-raw/raw/{dataset}/{run_id}/…` → local raw JSON.  
3. `normalize_generic` + `SqliteStore.upsert` (reject `{"data":[]}`).  
4. `issue_receipts_parallel.py` → signed SUCCESS (`TRUSTED_COLLECTION` / Ed25519).  
5. `restore_local_complete_from_receipt.py` → local COMPLETE.  
6. Fail-closed `publish_ops_projection.py --apply-remote` (no `--force-apply-remote`).

Helpers (local, not committed):  
`.glm-logs/w0814b-g3-indices/seal_from_r2_{topix,idx}.py`

### topix seal probe

| field | value |
|------:|
| unsealed segments with non-empty raw | **0** |
| selected / ready | **0 / 0** |
| gap `2008-01…04` | **DEFER** (empty raw) |

### idx seals (**+96**)

| wave | role | n ready | note |
|------|------|--------:|------|
| wave1 | early ready from acq mid-flight | **4** | `2009-03…06` |
| wave2 | main seal after densify | **49** ready (7× DB-lock errors mid-peer) | continuous issue + promote |
| wave3 | post-acq residual + lock-retry | **43 / 43** | includes prior lock-fails + `2014-06…2017-02` |
| **total promoted** | | **96** | promote logs ×96 |

Sealed segment_ids (all **96**):

`2008-11…12`, `2009-01…12`, `2010-01…12`, `2011-01…12`, `2012-01…03`, `2012-07…12`,  
`2013-01…12`, `2014-01…07`, `2014-09…12`, `2015-01…12`, `2016-01…12`, `2017-01…02`

- structured rows / month ≈ 792–3197 (multi-index daily bars)  
- `raw_row_count == structured_row_count` on issue  
- empty-raw skips: **0** (only non-empty pages sealed)  
- receipt run_ids span **901739…902102** (issue waves; peer races may interleave IDs)

### Local ledger after issue+promote

| dataset | PRE COMPLETE | POST COMPLETE | Δ |
|---------|-------------:|--------------:|--:|
| `indices_bars_daily_topix` | **220** | **220** | **0** |
| `indices_bars_daily` | **33** | **129** | **+96** |
| empty COMPLETE (topix/idx) | 0 | **0** | 0 |

---

## 3. Publish (fail-closed) + reeval

```text
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
# complete_count_guard ok local=1727 remote=1654 force=False
# remote projection applied

.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily_topix --today 2026-08-14
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily --today 2026-08-14
.venv/bin/python scripts/ops_reeval_freshness.py
```

| dataset | observed_start | observed_end | status | C8 |
|---------|----------------|--------------|--------|-----|
| `indices_bars_daily_topix` | **2008-01-01** | **2026-08-14** | PARTIAL | **pass lag 0** |
| `indices_bars_daily` | **2008-05-01** | **2026-08-13** | PARTIAL | **pass lag 1** |

Neither dataset is dataset-level COMPLETE.  
Freshness: **FRESH** `projgen-dcfb986b206e497fab7ff19c2f4a0d06` age=0 · `coverage_segments_untouched=1` · mass=NO-GO.

---

## POST (remote D1 live verify)

| metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **topix COMPLETE segs** | **220** | **220** | **0** |
| topix PARTIAL | 4 | **4** | 0 |
| **idx COMPLETE segs** | **33** | **129** | **+96** |
| idx PARTIAL | 191 | **95** | −96 |
| platform COMPLETE segs | **1376** | **1727** | +351 (G3 **+96** + concurrent peers) |
| empty COMPLETE (topix/idx) | 0 | **0** | 0 |
| projection | — | **FRESH** `projgen-dcfb986b…` age=0 | |

### topix residual DEFER (4)

`2008-01`, `2008-02`, `2008-03`, `2008-04` — worker re-dispatch **4/4 pass**, all **`row_count=0`** full-month shells. Honest empty-raw ban: **no COMPLETE**.

### idx residual

Remaining **95** PARTIAL months without full-month non-empty R2 evidence (or outside this max-jobs seal band). Acq pass ≠ COMPLETE; next wave when raw present.

---

## Acceptance

| gate | result |
|------|--------|
| topix gap raw/seal try (`2008-01…04`) | **PASS** (acq 4/4; empty → DEFER, no empty COMPLETE) |
| idx residual acq max-jobs **100**, workers=2, rpm=495 | **PASS** (100/100 pass; 0×429) |
| seal with real raw only | **PASS** (idx **+96**) |
| topix COMPLETE PRE **220** → POST **220** | **PASS** (gap empty held) |
| idx COMPLETE PRE **33** → POST **129** | **PASS** |
| no empty COMPLETE / no Mass / Phase7 OFF / peers not killed | **PASS** |
| reeval C8 pass + FRESH age=0 | **PASS** |
| fail-closed publish (local ≥ remote, no force) | **PASS** |

---

## Explicit non-claims

- No dataset-level COMPLETE for topix (220/224) or idx (129/224).  
- No Mass / READY / Phase7.  
- No COMPLETE without raw; no empty `{"data":[]}` seals.  
- Worker pass ≠ COMPLETE (topix gap empty shells prove the distinction).  
- Concurrent peers also advanced platform COMPLETE; **G3-attributed seals = topix +0 / idx +96**.

---

## Artifacts

| path | role |
|------|------|
| `.glm-logs/cf-backfill/w0814b_g3_topix_gap_exec_*` | topix gap execute |
| `.glm-logs/cf-backfill/w0814b_g3_idx_exec_*` | idx residual execute |
| `.glm-logs/w0814b-g3-indices/PRE_*.json` | PRE remote/local snap |
| `.glm-logs/w0814b-g3-indices/POST_*.json` | POST remote/local snap |
| `.glm-logs/w0814b-g3-indices/topix/*` | gap manifests + seal probe |
| `.glm-logs/w0814b-g3-indices/idx/*` | seal maps/results + issue logs |
| `.glm-logs/w0814b-g3-indices/publish.log` | fail-closed apply-remote |
| `.glm-logs/w0814b-g3-indices/reeval_*.log` | observed_* reeval |
| `.glm-logs/w0814b-g3-indices/freshness*.log` | FRESH reclock |
| `.glm-logs/w0814b-g3-indices/sealed_segments.json` | 96 sealed segment_ids |

## Operator repro

```bash
# dry residual
.venv/bin/python scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily_topix \
  --from-date 2008-01-01 --to-date 2008-04-30 \
  --workers 2 --general-rpm 495 --max-jobs 10 \
  --plan-out .glm-logs/cf-backfill/w0814b_g3_topix_gap_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814b_g3_topix_gap_queue_dry.json

.venv/bin/python scripts/ops/cf_premium_backfill.py \
  --datasets indices_bars_daily \
  --from-date 2008-01-01 --to-date 2026-08-13 \
  --workers 2 --general-rpm 495 --max-jobs 100 \
  --plan-out .glm-logs/cf-backfill/w0814b_g3_idx_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814b_g3_idx_queue_dry.json

# after real-raw mirror + struct upsert
.venv/bin/python scripts/issue_receipts_parallel.py \
  --datasets indices_bars_daily --segment-id YYYY-MM \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset indices_bars_daily --segment-id YYYY-MM

.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily_topix --today 2026-08-14
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset indices_bars_daily --today 2026-08-14
.venv/bin/python scripts/ops_reeval_freshness.py
```
