# W9-G5 / w0815_g5 — EDINET residual densify (seal-only) + JSDA OTC tip/recent probe (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (empty-raw ban held; no empty shell sealed)  
**Worker pass ≠ COMPLETE:** held  
**cf_premium dual-run ban:** **honored** — stopped G5 competing acq when **G1** live-acquired `edinet_cross_shareholdings` + `edinet_large_volume_shareholders` (`w0815_g1_general`, workers=6, general-rpm **350**); switched to **seal-only**  
**prefix:** `w0815_g5_edinet_jsda_*` · logs: `.glm-logs/w0815_g5_edinet_jsda/`

**Live verified:** 2026-08-15 (JST) / ~2026-08-14T15:48Z UTC  
**Projection freshness reclock:** `projgen-be2f45b7f11a4d53861e6993fdf68e46` · `coverage_segments_untouched=1` · Mass **NO-GO**

## Summary

| Metric | PRE | POST | This W9-G5 |
|--------|----:|-----:|------------|
| `edinet_major_shareholders` COMPLETE segs | **104** / 104 | **104** / 104 | **verify only** (no re-acq) |
| `edinet_major_shareholders` dataset status | PARTIAL (pre-reeval local) | **COMPLETE** | reeval observed window → **COMPLETE** (`observed_start=2018-01-04`) |
| `edinet_cross_shareholdings` COMPLETE segs | **76** / 104 | **76** / 104 | **+0** seal (residual empty) |
| `edinet_large_volume_shareholders` COMPLETE segs | **62** / 104 | **62** / 104 | **+0** seal (residual empty) |
| `jsda_otc_bond_reference_prices` COMPLETE segs | **34** / 8782 | **34** / 8782 | **+0** (site flake / no new tip CSV) |
| `jsda_corporate_bond_transactions` | **12/12 COMPLETE** | **12/12 COMPLETE** | skip seal (verify only) |
| `jsda_tokyo_repo_rates` | **COMPLETE** 1/1 | **COMPLETE** 1/1 | skip seal (verify only) |
| empty COMPLETE (OTC + EDINET seals this wave) | **0** | **0** | held |
| Platform COMPLETE (remote D1) | **2854** | **2936** | peers mid-window (G5 exclusive seals **+0**) |
| `raw_retention_manifests` | **13363** | **13924** | G1 + peers empty-shell + other tracks |

## T-EDINET

### Goal

1. **Verify** `edinet_major_shareholders` **104/104** COMPLETE segs — no re-acq.  
2. Densify residual:  
   - `edinet_cross_shareholdings` residual **28** (`2018-01…2020-04`)  
   - `edinet_large_volume_shareholders` residual **42** (`2018-01…2021-06`)  
3. Seal **nz raw only**; empty months **DEFER**.

### PRE (remote D1)

| dataset | COMPLETE | PARTIAL | residual months |
|---------|--------:|--------:|-----------------|
| major | **104** | 0 | — |
| cross | **76** | **28** | 2018-01…2020-04 |
| large | **62** | **42** | 2018-01…2021-06 |

Remote nz COMPLETE manifests (PRE): cross **104** / large **133** / major **127** — **0** of those nz windows map to residual unsealed months (prior COMPLETE months only).

### Execute

#### 1) major verify only

- Local + remote segment COMPLETE **104/104**, contiguous `2018-01…2026-08`, missing **[]**.  
- No `cf_premium_backfill` for major.

#### 2) residual densify attempt → seal-only (G1 owns acq)

Initial G5 low-RPM attempt (`workers=1`, `general-rpm=40…50`, sleep-on-retry 8–12):

| pass | jobs | result |
|------|-----:|--------|
| G5 residual (killed early; 429 under peers) | 38 events | pass **7** / fail **31** (429) · `rowsInserted` **0** |
| G5 cross residual `2019-01…2020-04` | 16 | pass **15** / fail **1** (`2019-02` 429) · `rowsInserted` **0** |

Detected live **G1** peer:

```text
cf_premium_backfill.py --datasets derivatives_bars_daily_options,...,edinet_cross_shareholdings,edinet_large_volume_shareholders \
  --from-date 2008-05-01 --execute --workers 6 --general-rpm 350
  state: .glm-logs/w0815_g1_general/state_w3_deriv_edinet.jsonl
```

**Action:** killed G5 competing orch/monitor (PID-file only; **did not** kill G1). Mode → **seal-only**.

G1 EDINET residual snapshot (this session):

| dataset | pass | fail | pass with rowsInserted>0 |
|---------|-----:|-----:|-------------------------:|
| cross | 20 | 2 | **0** |
| large | 30 | 12 | **0** |

All G1 residual passes were **empty shells** (`{"failed":0,"passed":1,"rowsInserted":0}`).

#### 3) seal path (nz raw required)

1. Scan remote `raw_retention_manifests` COMPLETE ∧ `row_count>0` for residual segments.  
2. Load R2 manifests → window `from`/`to` → segment_id month.  
3. Filter: not already COMPLETE ∧ in residual set.

| scan | sealable residual nz |
|------|---------------------:|
| early (pre densify) | **0** |
| post G1 (cross) | **0** |
| final candidates file | **0** |

**No seal issued.** empty-raw ban held.

### Reeval (no segment COMPLETE rewrite)

```bash
.venv/bin/python scripts/ops_reeval_observed_window.py --dataset edinet_major_shareholders --today 2026-08-15
.venv/bin/python scripts/ops_reeval_observed_window.py --dataset edinet_cross_shareholdings --today 2026-08-15
.venv/bin/python scripts/ops_reeval_observed_window.py --dataset edinet_large_volume_shareholders --today 2026-08-15
.venv/bin/python scripts/ops_reeval_freshness.py
```

| dataset | status | observed_start → end | C8 |
|---------|--------|----------------------|-----|
| major | **COMPLETE** | **`2018-01-04`** → `2026-08-14` | **pass** lag 1 |
| cross | **PARTIAL** | **`2020-05-01`** → `2026-08-14` | **pass** lag 1 |
| large | **PARTIAL** | **`2021-07-01`** → `2026-08-14` | **pass** lag 1 |

### POST COMPLETE months (remote, verified)

| dataset | COMPLETE segment_ids | n |
|---------|----------------------|--:|
| major | 2018-01…12 + 2019… + … + 2026-01…08 | **104** |
| cross | 2020-05…12 + 2021… + … + 2026-01…08 | **76** |
| large | 2021-07…12 + 2022… + … + 2026-01…08 | **62** |

### EDINET DEFER (honest)

| Item | Why |
|------|-----|
| cross residual 2018-01…2020-04 | **DEFER** — worker pass empty shells `rowsInserted=0`; no nz raw for residual months (empty-raw ban) |
| large residual 2018-01…2021-06 | **DEFER** — same (G1 + prior waves empty) |
| COMPLETE without raw / empty `{"data":[]}` | **Forbidden** |
| dual-run with G1 on same datasets | **avoided** after G1 detected |

## T-JSDA

### Corporate / Repo (COMPLETE skip)

- **Corporate:** segment COMPLETE **12/12**; dataset **COMPLETE** — verify only, no seal.  
- **Tokyo repo:** dataset **COMPLETE**, segment COMPLETE — verify only, no seal.

### OTC tip/recent probe (`jsda_otc_bond_reference_prices`)

Path followed: `docs/proof/w0814g_g6_jsda_20260814.md` (direct HTTP → parse → signed receipt → R2 put → ledger → publish) — **stopped at HTTP** because no new sealable CSV arrived.

| step | result |
|------|--------|
| Index `.../baisanchi/index.html` | intermittent: **HTTP 200** (62_958 B) when up; connect timeout (000) when down |
| Tip codes on index | **`S260817`…`S260803`** (10 codes) — tip still **`S260817`** (already COMPLETE prior waves) |
| Tip extend `S260818`/`819`/`820` | **HTTP 404** (not published) |
| Unsealed June weekdays `S260626`…`S260601` etc. | **connect timeout (000)** on data-file path (curl 28) after brief index windows |
| Local raw unsealed ≥1000 B | **0** (all local CSVs already COMPLETE) |
| Sealed this wave | **0** |

Probe log: `.glm-logs/w0815_g5_edinet_jsda/probe_summary_live.txt`, `jsda_defer.json`.

### POST OTC COMPLETE (**34**, unchanged)

`2026-06-29`…`2026-08-17` prior set (34 days). Dataset still **PARTIAL** vs inventory **8782**.

### JSDA DEFER (honest)

| Item | Why |
|------|-----|
| OTC further tip/recent (June≤`2026-06-26`, tip>`2026-08-17`) | Site **connect timeout** on CSV; tip extend **404** not published. **No raw → no COMPLETE**. |
| OTC full archive (~8748 PARTIAL) | No full archive crawl; site flake. |
| Non-trading shells (weekends/holidays e.g. `08-15`/`08-16`/`08-11`) | Not sealed as COMPLETE |
| COMPLETE without official CSV | **Forbidden** (empty COMPLETE **0**) |
| invent CSV / empty shell | **Forbidden** |

## Publish

No G5 exclusive segment seals → **no** `publish_ops_projection.py --apply-remote` this wave (avoid claiming peer +N).  
Freshness reclock only:

```text
ops_reeval_freshness → projgen-be2f45b7f11a4d53861e6993fdf68e46
OK coverage_segments_untouched=1 mass=NO-GO
```

Platform remote COMPLETE **2854 → 2936** is **peer** progress (not G5 OTC/EDINET seals).

## Explicit non-claims

- EDINET cross/large residual segment COMPLETE **not** claimed (+0).  
- OTC dataset-level COMPLETE **not** claimed (34/8782 PARTIAL).  
- Platform Mass / READY / Phase7 **not** claimed.  
- G5 did **not** dual-run against G1 after detection.  
- No invented JSDA CSV; timeouts deferred honestly.

## Forbidden held

- empty COMPLETE — **0**  
- Mass / READY / Phase7 ON — **NO-GO / OFF**  
- kill G1 / peer jobs — **none** (only G5 own PIDs)

## Operator repro

```bash
# major verify
.venv/bin/python - <<'PY'
import sqlite3
con=sqlite3.connect('data/structured/ingestion.sqlite')
print(con.execute("SELECT status, COUNT(*) FROM coverage_segments WHERE dataset='edinet_major_shareholders' GROUP BY status").fetchall())
PY

# residual nz seal candidates (expect 0 while API empty pre-observed_start)
# see .glm-logs/w0815_g5_edinet_jsda/edinet_nz_candidate_scan.log / seal_candidates.json

# JSDA tip probe (when market.jsda.or.jp accepts connections)
curl -o /tmp/otc_index.html -w "%{http_code}\n" \
  "https://market.jsda.or.jp/shijyo/saiken/baibai/baisanchi/index.html"
# only seal HTTP 200 CSV size>~100k; follow w0814g_g6 seal_otc_tip.py path

.venv/bin/python scripts/ops_reeval_observed_window.py --dataset edinet_major_shareholders --today 2026-08-15
.venv/bin/python scripts/ops_reeval_freshness.py
```

## Report line

`SHA=25e846aeeba29740392dce3763c855ac02e602b8 COMPLETE PRE major/cross/large=104/76/62 POST=104/76/62 (+0/+0/+0); major dataset COMPLETE (obs_start=2018-01-04); OTC PRE=34 POST=34 (+0); platform remote PRE=2854 POST=2936 (peers); raw_n PRE=13363 POST=13924; JSDA tip=S260817; empty COMPLETE=0; seal-only after G1; Mass=NO-GO`
