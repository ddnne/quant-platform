# W10-G10 — `equities_master` residual 94 PARTIAL (w0815b_g10_master) — 2026-08-15

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**empty-raw ban:** held (no COMPLETE without window-ok `Date` ∈ segment month)  
**Worker pass ≠ Coverage COMPLETE**  
**kill peers:** **none**  
**prefix:** `w0815b_g10_master_*` · CF D1 SoT

## Goal

1. PRE remote/local master COMPLETE/PARTIAL; list residual segs.  
2. Root-cause: subscription floor, Date-field misdate, expected_scope mismatch.  
3. Residual months: sample newest R2 raw; if any true `window_ok` → seal+issue.  
4. Optional gentle densify (workers 1, rpm 40) only for months without recent probe.  
5. Document unfixable residual as **DEFER** with evidence; dataset-level COMPLETE only if rule-legal without inventing segs.  
6. publish + proof + **git push**.

## PRE / POST

| Metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **Local** `equities_master` COMPLETE | **220** | **220** | **0** |
| **Remote** `equities_master` COMPLETE | **220** | **220** | **0** |
| Local / remote master PARTIAL | **94** / **94** | **94** / **94** | 0 |
| master TOTAL segs | **314** | **314** | 0 |
| empty COMPLETE (no SUCCESS receipt) | **0** | **0** | 0 |
| COMPLETE span | `2008-05`…`2026-08` continuous | same | 0 holes |
| post-2008 PARTIAL holes | **[]** | **[]** | — |
| Remote master `raw_retention_manifests` n / row_count>0 | **473 / 396** | **473 / 396** | 0 |

Projection after reeval freshness: **FRESH** `projgen-306c51c2f8e94d199fb7f39a5bc350be`  
(`coverage_segments_untouched=1`; mass=**NO-GO**).

Observed window reeval (`ops_reeval_observed_window.py --dataset equities_master --today 2026-08-15`):

| field | value |
|-------|------|
| status | **PARTIAL** |
| observed_start | **`2006-08-13`** (receipt-derived; not segment COMPLETE) |
| observed_end | **`2026-08-14`** |
| C8 | **pass** lag **1** |

### Residual taxonomy (94 PARTIAL — all pre-2008-05)

| band | n | segments | action |
|------|--:|----------|--------|
| Planner residual (misdate band) | **21** | `2006-08`…`2008-04` | re-probe → **DEFER** (window_ok=0) |
| Pre-plan / outside contract queue | **73** | `2000-07`…`2006-07` | **DEFER** (below subscription floor; no planner jobs) |
| Post-2008 holes | **0** | — | n/a |

Artifacts: `.glm-logs/w0815b_g10_master/pre_local.json`, `pre_remote_status.json`, `defer_list.json`, `post_status.json`, `root_cause.json`.

## 1) Root cause (three factors)

### A. Subscription floor

- Code SoT: `packages/data_plane/ops/backfill_planner.py`  
  `JQUANTS_SUBSCRIPTION_FLOOR = date(2006, 8, 13)`  
  Evidence note: receipt run_id=2522 FAILED with  
  `"Your subscription covers the following dates: 2006-08-13 ~ ."`.
- Planner clamps `requested_from` and never emits OOS jobs before that day.  
- **Effect:** 73 PARTIAL months `2000-07`…`2006-07` stay outside residual queue forever under current entitlement.  
- Edge: `2006-08` plan uses `from=2006-08-13`; prior densify saw HTTP **400** when upstream treated start as `2006-08-14`.

### B. Date field misdate

- Residual densify R2 bodies (prior wave runs **11587–11715**, re-probed this wave) request windows in `2006-08`…`2008-04` but every sampled page has **`Date` = `2008-05-07` only**.  
- `raw_row_count` is thousands per month (manifest COMPLETE) yet **zero** rows normalize into the requested segment month.  
- Seal gate: `Date ∈ segment month` → **window_ok=0** for all 21 planner residual months.  
- Newer tip runs **11714** / **11744** (empty `from`/`to`, `Date`=`2026-08-13`/`2026-08-14`) are not residual densify.

### C. expected_scope / history mismatch

| layer | start | notes |
|-------|-------|-------|
| Coverage Contract `history_target_start` | **2000-07-13** | `collection_coverage.json` → plans **314** calendar months |
| J-Quants subscription floor | **2006-08-13** | planner clamp |
| Honest API Date floor (observed in raw) | **2008-05-07** | first month with true in-window Date sealable at scale |
| Continuous COMPLETE | **2008-05**…**2026-08** | **220** segs |

So residual **94** is structural under current API fidelity + contract history start: not a missing-seal bug in the 2008-05+ span.

### Dataset-level COMPLETE?

**Not applied.** Rule-legal path:

- `ops_reeval_observed_window` already floors **`observed_start=2006-08-13`** from SUCCESS receipts **without inventing segment COMPLETE**.  
- Dataset `status` remains **PARTIAL** while any required segment is non-COMPLETE (`coverage_ledger` segment aggregate).  
- Promoting dataset COMPLETE while 94 segs stay PARTIAL would invent coverage. **Forbidden.**  
- Empty-raw / misdate ban held; no segment COMPLETE written this wave.

## 2) R2 residual probe (newest raw)

Probe: `.glm-logs/w0815b_g10_master/probe_residual.py`  
Sources: prior densify `window_bad_newest` / `residual_best` + any `run_id` > prior max (found **11744** tip only).

| class | n | action |
|-------|--:|--------|
| **window-ok** (`Date` ∈ segment month) | **0** | none to seal |
| **window-bad** (`Date` stuck on **`2008-05-07`**) | **21/21** planner months | **DEFER** |
| tip / non-residual newer runs | 2 | ignored for residual seal |
| sealed ready / issue / restore | **0** | — |

`planner_bad_dates_unique = ["2008-05-07"]`.

Artifacts:

- `.glm-logs/w0815b_g10_master/probe_summary.json`
- `.glm-logs/w0815b_g10_master/window_ok.json` (empty)
- `.glm-logs/w0815b_g10_master/window_bad.json`
- `.glm-logs/w0815b_g10_master/residual_best.json`
- `.glm-logs/w0815b_g10_master/probe_results.json`

## 3) Optional densify

Dry plan residual still **21** jobs (`2006-08`…`2008-04`):

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_master --workers 1 --general-rpm 40 --max-jobs 0 \
  --plan-out  .glm-logs/cf-backfill/w0815b_g10_master_plan.json \
  --queue-out .glm-logs/cf-backfill/w0815b_g10_master_queue.json
```

| field | value |
|-------|------:|
| plan jobs / queued | **21 / 21** |
| **execute** | **skipped** |

**Why skip:** Prior wave (w0814h_g10) densified all 21 months on 2026-08-14 (20 pass / 1 sub-edge fail) and left fresh R2 runs. This wave re-probed every planner month → still window_bad. Instruction: densify only for months **without recent probe**. No unprobed residual month remains; re-acq would burn general pool without seal ROI.

## 4) Receipts / publish

| step | result |
|------|--------|
| issue_receipts / restore | **skipped / N/A** — window_ok **0**; empty-raw / misdate ban held |
| full `publish_ops_projection --apply-remote` | **not required** — COMPLETE Δ **0** (fail-closed path unused) |
| `ops_reeval_observed_window.py` | **OK** status PARTIAL; C8 pass lag1; observed_start `2006-08-13` |
| `ops_reeval_freshness.py` | **OK** FRESH `projgen-306c51c2f8e94d199fb7f39a5bc350be`; segs untouched; Mass **NO-GO** |

## Explicit non-claims / DEFER

| Item | Status |
|------|--------|
| COMPLETE without usable in-window raw | **Forbidden** — held |
| `2006-08`…`2008-04` (21) | **DEFER** — misdated `2008-05-07` bodies (post densify + this re-probe) |
| `2000-07`…`2006-07` (73) | **DEFER** — below subscription floor / no planner jobs |
| Dataset-level COMPLETE for `equities_master` | still **PARTIAL** (honest; no invented segs) |
| Post-2008 residual holes | **none** — continuous COMPLETE `2008-05`…`2026-08` |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| Peer acq/issue killed? | **no** |
| Densify re-fire this wave | **skipped** (recent probe evidence) |

## Verdict

| Check | Result |
|-------|--------|
| master COMPLETE PRE→POST | **220 → 220 (+0)** local = remote |
| residual re-probe all planner months | **PASS** (21 window_bad; 0 window_ok) |
| raw-required seal only / misdated skip | **PASS** (window_ok **0**, sealed **0**) |
| empty COMPLETE | **0 PASS** |
| no peer kill | **PASS** |
| C8 master | **pass** lag **1** |
| Mass / Phase7 | **NO-GO / OFF PASS** |
| Overall W10-G10 master residual | **PASS** (honest +0; 94 PARTIAL DEFER with evidence) |

## Operator repro

```bash
# PRE remote status
platform/workers/ingestion-premium/node_modules/.bin/wrangler d1 execute quant-ingest \
  --remote --config=platform/workers/ingestion-premium/wrangler.toml --json \
  --command "SELECT status, COUNT(*) AS c FROM coverage_segments WHERE dataset='equities_master' GROUP BY status"

# residual plan (expect 21 = 2006-08…2008-04 while COMPLETE=220)
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_master --workers 1 --general-rpm 40 --max-jobs 0 \
  --plan-out  .glm-logs/cf-backfill/w0815b_g10_master_plan.json \
  --queue-out .glm-logs/cf-backfill/w0815b_g10_master_queue.json

# residual window probe (no seal if window_ok=0)
.venv/bin/python -u .glm-logs/w0815b_g10_master/probe_residual.py

# reeval (no COMPLETE claim)
.venv/bin/python scripts/ops_reeval_observed_window.py \
  --dataset equities_master --today 2026-08-15 --freshness-days 7
.venv/bin/python scripts/ops_reeval_freshness.py
```
