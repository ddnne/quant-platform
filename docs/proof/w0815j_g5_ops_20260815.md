# W18-G5 — w0815j ops (continuous collection: receipts + projection + DEFER maintain) (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**force-apply:** **not** used (fail-closed guard held)  
**peers killed:** **0** (G2 seal_harvest R2 scan, G4 JSDA OTC tip seal, G1/G3 fins residual probes left alive)  
**Worker pass ≠ COMPLETE:** held  
**DEFER densify re-run:** **not** done (policy)  
**G5 role:** ready-seal issue gap-fill (no dual with live issue PIDs) + fail-closed publish/`ops_reeval_freshness` + residual SoT live-sync + **LIVE+DEFER inventory maintain** + throughput snap

**Live verified:** 2026-08-15 (JST) / ~2026-08-15T00:31Z UTC  
**Projection:** **FRESH** `projgen-3c068e1ce0a14928a0525f6e546b6b56`  
**Artifacts:** `.glm-logs/w0815j_g5_ops/` (`ops_loop.py`, `ops_loop.stdout`, `PRE_remote.json`, `POST_remote.json`, `FINAL_metrics.json`, `publish_final.log`, `reeval_final.log`)

## PRE / POST (remote D1 `quant-ingest`)

| Metric | PRE (G5 start ~00:07Z) | POST (~00:31Z) | Δ |
|--------|-----------------------:|---------------:|--:|
| `raw_retention_manifests` | **14997** | **15020** | **+23** |
| Segment COMPLETE total | **3434** | **3442** | **+8** |
| Dataset COMPLETE | **11** | **11** | 0 (held) |
| Dataset STALE | **0** | **0** | 0 |
| empty COMPLETE | **0** | **0** | held |
| FRESH generation | `projgen-0472c602…` (PRE) | `projgen-3c068e1c…` | reclocked ×N |

### Dataset COMPLETE set (remote POST)

| dataset | status |
|---------|--------|
| `markets_calendar` | **COMPLETE** (held) |
| `jsda_tokyo_repo_rates` | **COMPLETE** (held) |
| `jsda_corporate_bond_transactions` | **COMPLETE** (held) |
| `edinet_major_shareholders` | **COMPLETE** (held) |
| `equities_investor_types` | **COMPLETE** (held) |
| `markets_margin_alert` | **COMPLETE** (held) |
| `markets_margin_interest` | **COMPLETE** (held) |
| `markets_short_ratio` | **COMPLETE** (held) |
| `derivatives_bars_daily_futures` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options_225` | **COMPLETE** (held; **W13-G3**) |
| `derivatives_bars_daily_options` | **COMPLETE** (held; **W15-G1**) |

### Key segment COMPLETE (remote PRE→POST)

| dataset | PRE | POST | Δ | note |
|---------|----:|-----:|--:|------|
| `jsda_otc_bond_reference_prices` | **49** | **57** | **+8** | peer **W18-G4** OTC tip seal/issue (G5 dual-gate N/A — no ready-seal issue inventory; G5 **not** sole claim) |
| `fins_summary` | **218** | **218** | **0** | held (prior **W17-G2** / **W17-G5** issue; no new ready residual) |
| `derivatives_bars_daily_options` | **164** | **164** | **0** | held Dataset COMPLETE **11** |
| platform total | **3434** | **3442** | **+8** | concurrent peers + G5 publish; G5 **not** sole owner |

## T10 — issue ready seals (no dual with live issue PIDs)

Continuous `ops_loop` (cycles **14**/14; sleep 90s; ~**00:07–00:29Z** window ≈ **22 min**) scanned seal/reagg results for `status=ready` without local COMPLETE.  
**DEFER skip:** `equities_earnings_calendar`, `equities_bars_daily_am` (tip-Date / today-mode).  
**empty-raw ban:** `raw_rows>0` gate held.  
**full-month filter:** options issue only from G1 `reagg_seal_result.jsonl` with `full_month=true` (week-partial sealpages **not** issued as COMPLETE).

| Action | Detail |
|--------|--------|
| Dual-issue gate | skip when any `issue_receipts_parallel` / `issue_driver` / `issue_promote` / `issue_restore_fast` / `issue_as_ready` alive |
| G5 direct issue+restore | **0** (ready_without_complete **0** all 14 cycles — prior seals already COMPLETE; peer OTC path used `SEALED` + peer-owned issue, not ready-gap inventory) |
| Fail / aborted | **0** |
| Peer concurrent | G2 `scan_window_ok` R2 harvest; G4 JSDA OTC tip `seal_otc_tip`; G1/G3 fins residual / empty-shell probes |
| Cycles w/ peer issue | dual gate free (no peer issue worker PID); ready inventory empty → issue **0** |

### G5-owned issue list

| dataset | segments | n |
|---------|----------|--:|
| *(none — ready gap empty)* | | **0** |
| **TOTAL** | | **0** |

Empty-raw ban held. No `cf_premium_backfill` launched by G5. No DEFER densify.  
PRE ready_without_complete: **0**; residual ready_without_complete **0** at loop end.

## T11 — publish fail-closed + ops_reeval_freshness (keep age low)

```text
publish_ops_projection --apply-remote
  complete_count_guard ok local>=remote force=False → applied
  (×N incl. c1 local=3434 remote=3434; c11 local=3442 remote=3440 → applied;
   final local=3442 remote=3442 → applied)
  no --force

ops_reeval_freshness ×N
  … → projgen-3c068e1ce0a14928a0525f6e546b6b56
  coverage_segments_untouched=1 mass=NO-GO
```

Fail-closed: publish only when **local COMPLETE ≥ remote**. `--force-apply-remote` **not** used.  
Projection age kept low via continuous reclock during peer COMPLETE growth (loop gens: `e24e3a21…` → `0b82e426…` → `79055ebf…` → final `3c068e1c…`).

## T12 — residual SoT live-sync + DEFER inventory maintain

- Updated `docs/phase62_residual_status.md` LIVE header/snapshot to remote POST **3442** / raw **15020** / Dataset COMPLETE **11** / FRESH `projgen-3c068e1c…` / OTC **57**
- **DEFER inventory D1–D9 held** (**no** densify re-run):
  - D1 topix/idx PARTIAL `2008-01…04` (4+4) re-verified
  - D3 breakdown PARTIAL `2013-01…2015-03` (27) re-verified
  - D5 OTC archive beyond tip **57** still site-timeout DEFER
  - D9 short_sale PARTIAL `2013-01…10` (10) empty pre-history re-verified
  - D2/D4/D6 empty families kept (no invent COMPLETE)
- This proof: `docs/proof/w0815j_g5_ops_20260815.md`

## T13 — throughput (raw Δ, COMPLETE Δ, dataset COMPLETE, general ~495 / fins separate)

| Track | host POST/min | n | note |
|-------|--------------:|--:|------|
| w0815 G1 general W1 (rpm~495 catalog) | **76.15** | 386 | 18p/368f 429-storm (historical) |
| w0815 G1 general W3 deriv+edinet | **5.03** | 341 | 201p/140f |
| w0815 G2 fins W1 (separate pool) | **49.58** | 79 | 0p/79f abort |
| w0815d G1 options week densify | **2.53** | 120 | 120p/0f (session carry) |
| w0815e G1 options residual week | **2.53** | 178 | 178p/0f (session carry) |
| w0815f G1 options residual week | **2.59** | 126 | 126p/0f (session carry) |
| G5 issue rate | — | **0** | ready gap empty (no acq) |
| Session raw Δ | — | **+23** | 14997→15020 |
| Session COMPLETE seg Δ | — | **+8** | 3434→3442 (peer OTC **+8**) |
| Dataset COMPLETE count | — | **11** | held |

**Note:** general catalog target **~495** rpm (W1 burst); fins use **separate** rate pool / lower sustained rpm.

## T14 — last_run monitor (no peer kill)

| Track | state | Action |
|-------|-------|--------|
| w0815j_g2_seal_harvest `scan_window_ok` | **ALIVE** early → **DONE** mid | leave alone |
| w0815j_g4_jsda `seal_otc_tip` | **ALIVE** early → **DONE** (OTC **+8**) | leave alone; G5 publish absorbed peer COMPLETE |
| w0815j_g1_fins_summary / g3_fins_rest residual | peer scope / DEFER empty | leave alone; densify **not** re-run |
| short_sale `2013-01…10` | DEFER empty pre-history | leave alone |

Resume rule held: **only if dead AND residual non-DEFER**. No dual-resume. No peer kill.

## T15 — git push each batch

Proof + residual LIVE sync + DEFER maintain committed and pushed to `origin/main` (see tip SHA below).

## Leak-check (漏れなし)

| Check | Result |
|-------|--------|
| empty COMPLETE | **0** |
| `--force-apply-remote` | **not** used |
| peers killed | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| dual-issue while peer issue alive | **N/A** (no peer issue worker; ready gap empty) |
| DEFER densify re-run | **not** done |
| week-partial options COMPLETE mint | **not** done (full-month filter; options already COMPLETE) |
| G5 sole claim of COMPLETE +8 | **no** — peer G4 OTC tip; G5 owned issue **0** |
| G5 sole claim of Dataset COMPLETE 11 | **no** — held from prior waves (options via **W15-G1**) |

## Explicit non-claims

- Dataset COMPLETE for fins/JSDA OTC **not** claimed (OTC still PARTIAL except the eleven listed COMPLETE datasets).
- G5 sole owner of raw densify **not** claimed.
- New historical acq by G5 **not** launched (no DEFER densify).
- DEFER residuals (D1–D9) **not** re-densified.
- Peer OTC tip **+8** **not** claimed as G5-owned issue (peer seal path; G5 publish-only absorb).
- short_sale `2013-01…10` **not** sealed (empty pre-history DEFER held).

## Operator repro

```bash
# continuous ops loop (ready-seal issue gap-fill + fail-closed publish)
.venv/bin/python -u .glm-logs/w0815j_g5_ops/ops_loop.py

# manual single segment (ready only; only if no peer issue alive)
.venv/bin/python scripts/issue_receipts_parallel.py \
  --dataset fins_summary --segment-id 2026-04 \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset fins_summary --segment-id 2026-04

# fail-closed publish + FRESH reclock
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```

## origin/main tip

*(filled after push)* — proof `w0815j_g5_ops_20260815.md` + residual LIVE sync (POST COMPLETE **3442** / raw **15020** / G5 issue **+0** / session COMPLETE **+8** peer OTC / Dataset COMPLETE **11** / DEFER D1–D9 held).

## Orchestrator FINAL metrics

```json
{
  "wave": "W18-G5",
  "tag": "w0815j_g5_ops",
  "pre_complete": 3434,
  "post_complete": 3442,
  "d_complete": 8,
  "pre_raw_n": 14997,
  "post_raw_n": 15020,
  "d_raw": 23,
  "dataset_complete": 11,
  "empty_complete": 0,
  "g5_issued": 0,
  "force_used": false,
  "peers_killed": 0,
  "mass": "NO-GO",
  "phase7": "OFF",
  "active_gen": "projgen-3c068e1ce0a14928a0525f6e546b6b56",
  "jsda_otc": {"pre": 49, "post": 57, "d": 8},
  "fins_summary": {"pre": 218, "post": 218, "d": 0},
  "options_complete": {"pre": 164, "post": 164, "d": 0},
  "short_sale_partial": ["2013-01","2013-02","2013-03","2013-04","2013-05","2013-06","2013-07","2013-08","2013-09","2013-10"]
}
```
