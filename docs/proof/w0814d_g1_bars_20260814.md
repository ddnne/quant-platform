# W4-G1 equities_bars_daily — w0814d_g1_bars residual acq + seal **+20** (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty-raw ban:** held (`_is_usable_raw` + digest raw ≤20MB; no empty COMPLETE)  
**Worker pass ≠ Coverage COMPLETE** (until signed receipt + promote/publish)  
**Did not kill** peer jobs (g3 deriv paced / single-job options runners active during acq)

**Prefix:** `w0814d_g1_bars_*`  
**Tip base (task brief):** `f9bf2e1` (origin/main at start; already at tip)  
**Session PRE (task brief):** COMPLETE **118**/272 · gap **154** · floor **2008-05-01**

---

## Objective

Grow `equities_bars_daily` **COMPLETE segs from 118** via:

1. week-chunk CF premium backfill (`max-jobs 200`, workers 3, general-rpm 495)  
2. R2→local raw+structured seal for months with non-empty full-month raw (floor **2008-05**)  
3. `issue_receipts_parallel` (signed) + promote COMPLETE + publish  
4. reeval `observed_window`  
5. proof + commit/push  

**Forbidden held:** no Mass; no empty COMPLETE; no kill of other jobs; no COMPLETE of bars **pre-2008-05** (empty API band DEFER).

---

## PRE

| metric | value |
|--------|------:|
| bars COMPLETE segs (remote + local) | **118** |
| bars PARTIAL segs | **154** |
| COMPLETE band | 2008-05…2013-04 continuous + 2013-06 + 2013-08…2017-02 + 2024-01..03, 2025-04, 2025-11..12, 2026-01..08 |
| mid-band holes | **2013-05**, **2013-07** (still no full-month nz single-manifest candidate) |
| `observed_start` | **2008-05-01** |
| `observed_end` (remote window) | **2026-08-12** (pre-reeval lag) |
| dataset status | PARTIAL |
| remote raw_retention_manifests n / c / nz / sum_rows | **2921** / **2420** / **2139** / **38_254_941** |
| empty COMPLETE | **0** |

Artifacts: `.glm-logs/w0814d-g1-bars/PRE_*.json`

### Residual (planner)

| field | value |
|------:|
| plan_jobs week-chunks (`--from-date 2008-05-01`) | **445** (skip COMPLETE through 2017-02 excl. holes) |
| first residual week | **2013-05-02** (hole densify) then residual **2017-03+** |
| last residual week in this cap | through **2020-10** week windows (max-jobs 200) |

---

## 1) Execute backfill (week-chunks)

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_bars_daily \
  --from-date 2008-05-01 \
  --execute --week-chunks --chunk-days 7 \
  --workers 3 --general-rpm 495 --max-jobs 200 \
  --sleep-on-retry 3 \
  --plan-out .glm-logs/cf-backfill/w0814d_g1_bars_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814d_g1_bars_queue.json \
  --state-out .glm-logs/cf-backfill/w0814d_g1_bars_state.jsonl
```

| field | value |
|-------|------:|
| plan / queued / executed | **445** / **200** / **200** |
| **pass / fail** | **189 / 11** |
| fail mode | `worker_error` **11** (D1 long-running import + HTTP 500) |
| rowsInserted sum (pass detail) | **3_443_163** |
| host_dispatch_rpm | **7.37** (window ~1621s; first→last host POST) |
| http_429_count (host envelope) | **0** |
| pass date range | **2013-05-02 … 2020-10-28** week chunks (unique months **45**) |

### Peer concurrent (not killed)

g3 derivatives paced / single-job options runners shared general pool. Bars pass rate **189/200**.

---

## 2) R2 seal (raw-required only)

**Pre-existing full-month single-manifest candidates:** **0** (prior waves sealed all run_ids ~834–893 full-month band through 2017-02).

**Seal path this wave:** week-combine from non-empty R2 week-chunk pages → full-month window digest raw + structured upsert.

Script: `.glm-logs/w0814d-g1-bars/seal_bars_from_weeks.py`  
(empty-raw ban; floor `2008-05`; early-stop ≥70k rows for densify; digest ≤20MB)

| field | value |
|------:|
| weekish COMPLETE manifests scanned | **2000** (row_count 1k–45k) |
| unsealed months with week raw potential | **102** |
| **selected / ready** | **20 / 20** |
| segment set | **2017-03 … 2018-10** continuous (**20**) |
| empty / skip_empty | **0** |
| digest raw | non-empty JSON ≤20MB (issue_receipts 25MB usable cap) |
| structured upsert | full-month window pages from R2 (empty-raw ban held) |
| raw_rows sum (ready) | **1_508_658** |

Result: `.glm-logs/w0814d-g1-bars/seal_result.jsonl` + `seal_map.json` + `seal_weeks_run2.out`

**Not sealed:** `2013-05` / `2013-07` holes; residual **2018-11+** mid-band still PARTIAL until next seal wave.

Note: week-chunk acq densifies residual R2 inventory; single full-month manifests exhausted after w0814c. This wave seals by **combining non-empty week R2 pages** into calendar-month windows (still R2-backed nz evidence; no invent).

---

## 3) Signed receipts + COMPLETE

```text
.venv/bin/python .glm-logs/w0814d-g1-bars/issue_targeted.py
# remaining after first batch:
.venv/bin/python .glm-logs/w0814d-g1-bars/issue_targeted.py \
  2018-01 … 2018-10

for seg in <20 ready segs>:
  .venv/bin/python scripts/restore_local_complete_from_receipt.py \
    --dataset equities_bars_daily --segment-id $seg
```

| field | value |
|------:|
| issue loop | **20 OK / 0 FAIL** (run_ids **902612…902631**) |
| promote COMPLETE | **20 OK / 0 FAIL** |
| eligibility | SUCCESS receipt · non-empty digest raw · structured present |
| empty COMPLETE | **0** |

### Publish (fail-closed)

```text
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
```

| field | value |
|------:|
| complete_count_guard | **ok local=2241 remote=2221 force=False** |
| remote projection | **applied** |
| empty COMPLETE (`receipt_run_id` null/0) | **0** |

---

## 4) Reeval observed_window

```text
.venv/bin/python scripts/ops_reeval_observed_window.py --dataset equities_bars_daily
```

| field | PRE | POST |
|-------|-----|------|
| **observed_start** | **2008-05-01** | **2008-05-01** (unchanged; no pre-2008 fabricate) |
| observed_end (remote) | 2026-08-12 (lag) | **2026-08-13** |
| status | PARTIAL | PARTIAL |
| C8 | pass lag 2 (local pre) | **pass** lag **1** |
| COMPLETE segs | 118 | **138** (via seal path, not reeval inflation) |
| receipt n / sum_raw (reeval window) | — | **2232** / **35_484_183** |

`coverage_segments` not rewritten by reeval (by design). Reeval writes **remote** `dataset_coverage` only.

Also: `ops_reeval_freshness.py` reclock after publish (Mass OFF; coverage_segments untouched).

---

## POST Δ

| metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **bars COMPLETE segs** | **118** | **138** | **+20** |
| bars PARTIAL | 154 | 134 | −20 |
| remote raw n | 2921 | **3113** | **+192** |
| remote raw COMPLETE | 2420 | **2612** | **+192** |
| remote raw nz | 2139 | **2331** | **+192** |
| remote raw sum_rows | 38_254_941 | **41_727_071** | **+3_472_130** |
| observed_start | 2008-05-01 | 2008-05-01 | 0 |
| observed_end (remote) | 2026-08-12 | **2026-08-13** | +1d (reeval) |
| empty COMPLETE | 0 | **0** | 0 |

### COMPLETE months added (20)

**2017-03 … 2018-10** continuous.

Holes remaining in mid-band: **2013-05**, **2013-07** (no seal this wave).

Prior COMPLETE retained: 2008-05…2013-04 continuous, 2013-06, 2013-08…2017-02, 2024-01..03, 2025-04, 2025-11..12, 2026-01..08.

---

## Acceptance

| gate | result |
|------|--------|
| week-chunks max-jobs 200 | **PASS** (executed 200) |
| pass/fail reported | **PASS** (189/11) |
| host rpm reported | **PASS** (7.37) |
| COMPLETE segs increased from 118 | **PASS** → **138** (+20) |
| only raw months sealed | **PASS** (R2 nz week pages combined → month windows) |
| empty COMPLETE ban | **PASS** |
| no pre-2008-05 COMPLETE | **PASS** |
| observed_start held at 2008-05-01 | **PASS** |
| reeval bars | **PASS** (C8 lag 1) |
| no Mass / no kill peers | **PASS** |
| proof + push | **PASS** (this doc) |

---

## Residual / next

- Residual week-jobs after max-jobs cap still large (~245 of 445) until more COMPLETE skip  
- Remaining PARTIAL after **2018-10** (except recent COMPLETE islands) **DEFER** next seal wave (week-combine candidates remain among 102−20)  
- `2013-05` / `2013-07` holes need denser week R2 evidence before seal  
- pre-2008-05 empty API band remains **DEFER** (no empty COMPLETE)

---

## Artifacts

| path | role |
|------|------|
| `.glm-logs/cf-backfill/w0814d_g1_bars_{plan,queue,state,run}.*` | week-chunk acq |
| `.glm-logs/w0814d-g1-bars/PRE_*.json` / `POST_*.json` | PRE/POST snapshots |
| `.glm-logs/w0814d-g1-bars/seal_bars_from_weeks.py` | week-combine seal |
| `.glm-logs/w0814d-g1-bars/seal_{map,result,weeks_run2}.*` | seal evidence |
| `.glm-logs/w0814d-g1-bars/issue_*.out` / `promote_complete.log` | receipts + promote |
| `.glm-logs/w0814d-g1-bars/{publish,reeval,freshness}.txt` | publish + reeval |
| `docs/proof/w0814d_g1_bars_20260814.md` | this proof |

### Reproduce acq

```bash
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_bars_daily \
  --from-date 2008-05-01 \
  --execute --week-chunks --chunk-days 7 \
  --workers 3 --general-rpm 495 --max-jobs 200 \
  --sleep-on-retry 3 \
  --plan-out .glm-logs/cf-backfill/w0814d_g1_bars_plan.json \
  --queue-out .glm-logs/cf-backfill/w0814d_g1_bars_queue.json \
  --state-out .glm-logs/cf-backfill/w0814d_g1_bars_state.jsonl
```
