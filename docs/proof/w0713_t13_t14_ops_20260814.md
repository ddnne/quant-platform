# G9 = T13 + T14 ops — receipts + projection freshness (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**cf_premium_backfill:** **not** launched by G9 (avoid general-pool contention with peer T1/T2/T4/T6/T7)  
**empty-raw ban:** held (`issue_receipts_parallel` / `_is_usable_raw`; skip `no_raw` only)

## Report

| Metric | PRE (session start) | POST |
|--------|--------------------:|-----:|
| origin/main SHA (at close) | `83fe7c0`… | **`1c613d2`** (pre-push tip; residual push SHA below) |
| projection ACTIVE gen | `projgen-fe1eae005c73494ba543dd0d95a915f0` | **`projgen-d4677ef69f7d49f88145b31ad04c170a`** |
| generated_at (UTC) | `2026-08-13T17:33:28.609408+00:00` | **`2026-08-13T23:16:52.555524+00:00`** |
| projection age (live) | **~16983 s** (~4.7 h) | **~0–5 s** |
| remote COMPLETE segs | **585** | **677** |
| remote `raw_retention_manifests` | **7825** | **9324** |
| G9 receipts **+N** | — | **+27** |
| empty COMPLETE | **0** | **0** |

### Projection age PRE/POST (T14)

| Step | gen | age live |
|------|-----|--------:|
| Session PRE (stale ACTIVE) | `projgen-fe1eae005c73494ba543dd0d95a915f0` | **~16983** |
| Mid reclock (G9 first `ops_reeval_freshness`) | `projgen-d36f10d1c78a4e9cb2ed188e6295e937` | **~0** |
| Peer intermediate | `projgen-c1aacf4414f34e6d846971328a576953` | ~490 |
| Mid final (pre-push) | `projgen-9b9eca10e640421e815c81970ec7bbd1` | **~0–5** |
| **Final POST** | **`projgen-d4677ef69f7d49f88145b31ad04c170a`** | **~0–5** |

`coverage_segments` untouched by reeval (Mass NO-GO). Full `publish_ops_projection --apply-remote` attempted under peer DB lock; remote COMPLETE already reflected G9 seals via peer fail-closed publish path.

## T13 — issue_receipts_parallel (+27)

Peer **t5-fins** R2-mirrored residual months (usable raw + struct; no G9 `cf_premium`). G9 issued signed SUCCESS receipts only:

| dataset | segments | n | receipt run_id range (ex.) |
|---------|----------|--:|----------------------------|
| `fins_details` | **2019-09…12**, **2020-02…08** (no 2020-01 this wave) | **11** | 900710–900718, 900727–900729 |
| `fins_dividend` | **2018-01…08, 10…12** (no 2018-09 this wave) | **11** | 900721–900726, 900732–900735, 900743 |
| `fins_earnings_date` | **2018-01…05** | **5** | 900738–900742 |
| **Total** | | **27** | |

### Dataset COMPLETE (remote POST)

| dataset | PRE (residual prior) | POST |
|---------|---------------------:|-----:|
| `fins_details` | 23 | **34** (+11) |
| `fins_dividend` | 2 | **13** (+11) |
| `fins_earnings_date` | 2 | **10** (+8) |
| `fins_summary` | 30 | **30** (Δ0 this G9 issue; next residual months DEFER) |

Remote segment COMPLETE total **585 → 677** includes G9 **+27** plus concurrent peer seals (G8 OTC/indices, topix, bars, …).

## Actions

1. `git pull --ff-only origin main` (frequent) — tip advanced via peers to `1c613d2` pre-push.
2. **T14:** `scripts/ops_reeval_freshness.py` ×2 (session mid + final) → ACTIVE `projgen-9b9eca10…` age≈0; segs untouched.
3. **T13:** dry-scan then `issue_receipts_parallel --struct-hint` waves 1–5 on fins family as peer R2 seal became ready → **issued=27**; empty-raw ban held (`skipped no_raw` on incomplete prepare).
4. No G9 `cf_premium_backfill` launch.

## Explicit non-claims

| Item | Why |
|------|-----|
| Dataset-level COMPLETE for any `fins_*` | still **PARTIAL** |
| G9 sole owner of COMPLETE **585→677** | concurrent peers (G8 OTC/indices, topix, bars, …) also sealed |
| New `cf_premium` by G9 | **Forbidden** this pass (general-pool contention) |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| COMPLETE without raw / empty `{"data":[]}` | **Forbidden** |

## Operator repro

```bash
# after peer R2 mirror + local struct upsert (empty-raw ban)
.venv/bin/python scripts/issue_receipts_parallel.py \
  --datasets fins_details,fins_dividend,fins_earnings_date \
  --struct-hint --limit 40 --workers 6 --order asc --json-summary

# targeted freshness (no segment rewrite)
.venv/bin/python scripts/ops_reeval_freshness.py

# full apply when local COMPLETE >= remote (fail-closed guard)
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --refresh-coverage --apply-remote
```
