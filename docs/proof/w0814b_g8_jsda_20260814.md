# W2-G8 — JSDA OTC tip/recent **+2** (raw only) (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**empty-raw ban:** held (only official `market.jsda.or.jp` CSV bytes; no invent)  
**Worker pass ≠ COMPLETE:** held (direct HTTP → local raw → parse/upsert → identity-matched `SignedReceiptAuthority` → ledger)

**Live verified:** 2026-08-14 (JST) / ~2026-08-14T03:16Z UTC  
**Projection:** **FRESH** `projgen-1e79a513c427490a8604dbc7bad88bde` (`coverage_segments_untouched=1`)

## Summary

| Metric | PRE | POST | This W2-G8 |
|--------|----:|-----:|------------|
| JSDA OTC COMPLETE segs | **9** / 8779 | **11** / 8779 | **+2** |
| JSDA corporate | **12/12 COMPLETE** | **12/12 COMPLETE** | verify only |
| JSDA tokyo repo | **COMPLETE** 1/1 | **COMPLETE** 1/1 | verify only |
| empty COMPLETE | **0** | **0** | held |
| Platform COMPLETE (local publish) | **1411** remote pre-guard | **1442** remote | + peers + G8 OTC **+2** |

## Path

1. `git pull --ff-only origin main` (already up to date).
2. Local direct probe of `market.jsda.or.jp`:
   - Index intermittent **200** then **connect timeout (000)**.
   - Data-file window opened briefly: **`S260731.csv` HTTP 200** (2_193_408 B) and **`S260730.csv` HTTP 200** (2_150_316 B).
   - Further sequential tip pull stopped on **`S260729` connect timeout (000)** → **DEFER** remaining recent/history days.
   - `2026-08-11` (Mountain Day) official file **404** — not sealed (non-publication day).
3. CF `ingestion-jsda` assist:
   - Temporary `MAX_DATA_FILES` raise + year-page crawl; when origin healthy, discovery stores tip S files already sealed.
   - Later root index **HTTP 522** from worker during outage window (same site flake).
   - **Bugfix shipped:** year-archive selection now sorts by **numeric year desc** (was list-reverse, which preferred oldest `archive2002` when `MAX_YEAR_PAGES=1`).
   - Vars restored to production `MAX_DATA_FILES=3` / `MAX_YEAR_PAGES=1` and redeployed.
4. Operator seal (`.glm-logs/w0814b_g8_jsda/`):
   - `parse_otc_reference_csv` (headerless positional) + `normalize_otc_reference_prices` + upsert
   - Inventory `expected_items` set to structured count (Coverage V2 identity match for `official_archive_day`)
   - Identity-matched `SignedReceiptAuthority` SUCCESS (`expected_scope` / `expected_items` == required)
   - `refresh_coverage_ledger(..., today='2026-08-14')` → OTC COMPLETE **9→11**
5. R2 retention put (content-addressed):
   - `raw/jsda/jsda_otc_bond_reference_prices/file_S260730.csv/14cff1d6…cfe8.csv`
   - `raw/jsda/jsda_otc_bond_reference_prices/file_S260731.csv/ca4da93e…403b0.csv`
6. `publish_ops_projection.py --apply-remote` (fail-closed guard: local ≥ remote)  
   `ops_reeval_freshness.py` → FRESH age=0; `coverage_segments_untouched=1`

## OTC sealed this wave (**+2**)

| segment_id | raw | rows | receipt run_id | digest (sha256) |
|------------|-----|-----:|---------------:|-----------------|
| **2026-07-30** | `S260730.csv` | **12376 / 12376** | **901821** | `14cff1d6…bcd5cfe8` |
| **2026-07-31** | `S260731.csv` | **12390 / 12390** | **901820** | `ca4da93e…e1403b0` |

Eligibility: `TRUSTED_COLLECTION` / `SignedReceiptAuthority` / `dev-receipt-v1`.  
Fetched via: `direct_jsda_http+local_raw` (site window) + R2 put for retention.

### POST OTC COMPLETE (**11**)

`2026-07-30`, `2026-07-31`, `2026-08-03`, `2026-08-04`, `2026-08-05`, `2026-08-06`, `2026-08-07`, `2026-08-10`, `2026-08-12`, `2026-08-13`, `2026-08-14`.  
Dataset still **PARTIAL** (history target 2002-08-02; **8779** required days).

## Corporate / Repo (verify only)

- **Corporate:** segment COMPLETE **12/12** (`2015`…`2026`); dataset **COMPLETE** — unchanged.
- **Tokyo repo:** dataset **COMPLETE**, segment `jsda-era-timeseries` **COMPLETE** — unchanged.

## DEFER (honest)

| Item | Why |
|------|-----|
| OTC further tip/recent (e.g. `2026-07-29` and earlier July) | Local + CF data-file **connect timeout / 522** after two successful days. **No raw → no COMPLETE**. |
| OTC full archive (~8768 PARTIAL remain) | Site flake + no full archive crawl this wave. |
| Non-trading / missing official days (e.g. `2026-08-11` 404) | Not sealed as COMPLETE |
| COMPLETE without raw / empty shell | **Forbidden** (empty COMPLETE **0**) |
| Mass / READY / Phase7 | **NO-GO / OFF** |

## Publish (fail-closed)

```text
complete_count_guard ok local=1442 remote=1411 force=False
remote projection applied
ops_reeval_freshness → projgen-1e79a513c427490a8604dbc7bad88bde FRESH
OK coverage_segments_untouched=1 mass=NO-GO
```

Remote D1 post-check: OTC COMPLETE **11** / PARTIAL **8768**; total COMPLETE **1442**; corp+repo datasets COMPLETE; OTC dataset PARTIAL.

No `--force-apply-remote`.

## Explicit non-claims

- OTC dataset-level COMPLETE **not** claimed (still PARTIAL vs 8779-day inventory).
- Platform Mass / READY / Phase7 **not** claimed.
- Concurrent peers may advance total COMPLETE beyond G8-attributed **+2**.

## Code delta

- `platform/workers/ingestion-jsda/src/index.ts`: prefer **highest year archives first** when `MAX_YEAR_PAGES` is limited (fixes reverse-list selecting `archive2002` over `archive2026`).

## Operator repro

```bash
# tip raw (when market.jsda.or.jp accepts connections)
curl -o data/raw/jsda/jsda_otc_bond_reference_prices/2026-07-31/S260731.csv \
  https://market.jsda.or.jp/shijyo/saiken/baibai/baisanchi/files/2026/S260731.csv

# seal: parse/normalize/upsert + inventory expected_items=N + SignedReceiptAuthority
# then:
.venv/bin/python -c "..."  # see .glm-logs/w0814b_g8_jsda/
.venv/bin/python scripts/publish_ops_projection.py --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```
