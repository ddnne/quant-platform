# W12-G5 — w0815d ops (continuous collection: receipts + projection + DEFER maintain) (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**force-apply:** **not** used (fail-closed guard held; wrangler race retry only)  
**peers killed:** **0** (G1 options acq+seal, G3 short_jsda, G4 fut/o225 seal+issue, residual densify, fins peer left alive)  
**Worker pass ≠ COMPLETE:** held  
**DEFER densify re-run:** **not** done (policy)  
**G5 role:** ready-seal issue gap-fill (no dual with live issue PIDs) + fail-closed publish/`ops_reeval_freshness` + residual SoT live-sync + DEFER table maintain + throughput snap

**Live verified:** 2026-08-15 (JST) / ~2026-08-14T17:45Z UTC  
**Projection:** **FRESH** `projgen-305d4bb4a1e24709bc4a7230278d7e36`  
**Artifacts:** `.glm-logs/w0815d_g5_ops/` (`ops_loop.py`, `ops_loop.stdout`, `PRE_*`, `POST_*`, `FINAL_metrics.json`, `g5_issued.json`)

## PRE / POST (remote D1 `quant-ingest`)

| Metric | PRE (G5 start ~17:15Z) | POST (~17:45Z) | Δ |
|--------|-----------------------:|---------------:|--:|
| `raw_retention_manifests` | **14188** | **14262** | **+74** |
| Segment COMPLETE total | **3252** | **3288** | **+36** |
| Dataset COMPLETE | **7** (PRE snap) → **8** mid (W12-G3 short_ratio) | **8** | **+1** (short_ratio; **not** G5 sole claim) |
| Dataset STALE | **0** | **0** | 0 |
| empty COMPLETE | **0** | **0** | held |
| FRESH generation | prior / peer | `projgen-305d4bb4…` | reclocked ×N |

### Dataset COMPLETE set (remote POST)

| dataset | status |
|---------|--------|
| `markets_calendar` | **COMPLETE** (held) |
| `jsda_tokyo_repo_rates` | **COMPLETE** (held) |
| `jsda_corporate_bond_transactions` | **COMPLETE** (held) |
| `edinet_major_shareholders` | **COMPLETE** (held) |
| `equities_investor_types` | **COMPLETE** (held) |
| `markets_margin_alert` | **COMPLETE** (held) |
| `markets_margin_interest` | **COMPLETE** (held) |
| `markets_short_ratio` | **COMPLETE** (**W12-G3**; not G5 sole claim) |

### Key segment COMPLETE (remote PRE→POST)

| dataset | PRE | POST | Δ | note |
|---------|----:|-----:|--:|------|
| `derivatives_bars_daily_futures` | **142** | **164** | **+22** | G5 issue **+6** + G4 seal/issue peers |
| `derivatives_bars_daily_options` | **50** | **56** | **+6** | G5 issue **+6** (2013-10/11, 2014-04, 2019-01…03) + peer seal |
| `derivatives_bars_daily_options_225` | **157** | **164** | **+7** | peer G4 issue (G5 dual-issue gate skipped when peer issue alive) |
| `fins_summary` | **194** | **195** | **+1** | peer fins issue |
| platform total | **3252** | **3288** | **+36** | concurrent peers + G5; G5 **not** sole owner |

## T1 — issue ready seals (no dual with live issue PIDs)

Continuous `ops_loop` (cycles ≤15, sleep 90s) scanned `.glm-logs/**/seal_result*` for `status=ready` without local COMPLETE.  
**DEFER skip:** `equities_earnings_calendar`, `equities_bars_daily_am` (tip-Date / today-mode).  
**empty-raw ban:** `raw_rows>0` gate held.

| Action | Detail |
|--------|--------|
| Dual-issue gate | skip when any `issue_receipts_parallel` / `issue_driver` / `issue_promote` alive |
| G5 direct issue+restore | **12** success (futures **6** + options **6**) |
| Fail | options `2013-12` once (`sqlite3.OperationalError: database is locked`); not forced |
| Peer concurrent | G1 options seal+issue incremental; G4 fut/o225 seal+`issue_restore_fast`; residual densify; fins issue |

### G5-owned issue list

| dataset | segments | n |
|---------|----------|--:|
| `derivatives_bars_daily_futures` | `2013-03`, `2015-02`, `2015-03`, `2015-10`, `2015-11`, `2016-02` | **6** |
| `derivatives_bars_daily_options` | `2013-10`, `2013-11`, `2014-04`, `2019-01`, `2019-02`, `2019-03` | **6** |
| **TOTAL** | | **12** |

Empty-raw ban held. No `cf_premium_backfill` launched by G5. No DEFER densify.

## T2 — publish fail-closed + ops_reeval_freshness

```text
publish_ops_projection --apply-remote
  complete_count_guard ok local>=remote force=False → applied (×N)
  one wrangler race: "Not currently importing anything" → retry (attempts≤3; no --force)
  final: local=3288 remote=3284 force=False → applied

ops_reeval_freshness ×N
  … → projgen-305d4bb4a1e24709bc4a7230278d7e36
  coverage_segments_untouched=1 mass=NO-GO
```

Fail-closed: publish only when **local COMPLETE ≥ remote**. `--force-apply-remote` **not** used.

## T3 — residual SoT live-sync + DEFER table maintain

- Updated `docs/phase62_residual_status.md` LIVE header/snapshot to remote POST **3288** / raw **14262** / Dataset COMPLETE **8** / FRESH `projgen-305d4bb4…`
- DEFER inventory **D1–D9** held (**no** densify re-run)
- This proof: `docs/proof/w0815d_g5_ops_20260815.md`

## T4 — throughput (raw Δ, COMPLETE Δ, dataset COMPLETE, general ~495 / fins separate)

| Track | host POST/min | n | note |
|-------|--------------:|--:|------|
| w0815 G1 general W1 (rpm~495 catalog) | **76.15** | 386 | 18p/368f 429-storm (historical) |
| w0815 G1 general W3 deriv+edinet | **5.03** | 341 | 201p/140f |
| w0815 G2 fins W1 (separate pool) | **49.58** | 79 | 0p/79f abort |
| w0815c G5 residual densify | **3.61** | 80 | 38p/42f (session) |
| w0815d G1 options acq | **4.18** | 63 | 8p/55f |
| G5 issue rate | — | **12** | seal→receipt only (no acq) |
| Session raw Δ | — | **+74** | 14188→14262 |
| Session COMPLETE seg Δ | — | **+36** | 3252→3288 |
| Dataset COMPLETE count | — | **8** | +short_ratio via W12-G3 |

**Note:** general catalog target **~495** rpm (W1 burst); fins use **separate** rate pool / lower sustained rpm.

## T5 — last_run monitor (no peer kill)

| Track | state | Action |
|-------|-------|--------|
| w0815c_g5_deriv residual densify | **ALIVE** early → cont. | no kill |
| w0815d_g1_options acq+seal | **ALIVE** | leave alone; dual-issue skip when their issue alive |
| w0815d_g4_fut_o225 seal/issue | **ALIVE** | leave alone |
| w0815d_g3_short_jsda | finished dataset COMPLETE | no dual |
| fins peer issue | intermittent | dual-issue gate |

Resume rule held: **only if dead AND residual non-DEFER**. No dual-resume. No peer kill.

## Leak-check (漏れなし)

| Check | Result |
|-------|--------|
| empty COMPLETE | **0** |
| `--force-apply-remote` | **not** used |
| peers killed | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| dual-issue while peer issue alive | **skipped** (gate; cycles 2–3) |
| DEFER densify re-run | **not** done |
| G5 sole claim of COMPLETE +36 | **no** — concurrent G1/G4/peers |
| G5 sole claim of Dataset COMPLETE +1 | **no** — W12-G3 short_ratio |

## Explicit non-claims

- Dataset COMPLETE for futures/options/o225/fins **not** claimed (still PARTIAL except the eight listed).
- G5 sole owner of raw densify **not** claimed.
- New historical acq by G5 **not** launched.
- DEFER residuals (D1–D9) **not** re-densified.

## Operator repro

```bash
# continuous ops loop (seal→issue gap-fill + fail-closed publish)
.venv/bin/python -u .glm-logs/w0815d_g5_ops/ops_loop.py

# manual single segment
.venv/bin/python scripts/issue_receipts_parallel.py \
  --dataset derivatives_bars_daily_futures --segment-id 2016-02 \
  --workers 1 --no-refresh --json-summary
.venv/bin/python scripts/restore_local_complete_from_receipt.py \
  --dataset derivatives_bars_daily_futures --segment-id 2016-02

# fail-closed publish + FRESH reclock
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --apply-remote
.venv/bin/python scripts/ops_reeval_freshness.py
```

## origin/main tip

`879855d70e66743014560eaed1d412ef63be2c81` — proof `w0815d_g5_ops_20260815.md` + residual LIVE sync (POST COMPLETE **3288** / raw **14262** / G5 issue **+12** / session COMPLETE **+36**).
