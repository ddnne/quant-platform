# W15-G1 options residual seal → 164/164 — w0815g_g1_options (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (emptyish COMPLETE manifests options **25** unchanged; not promoted)  
**empty-raw ban:** held (`row_count>0` + usable pages / sealpage only; full-month only for COMPLETE)  
**peer kill ban:** **honored** — did **not** kill live w0815f residual week densify while alive  
**prefix:** `w0815g_g1_options_*` · densify ALIVE at start → seal-only inventory; densify drained mid-wave → **surgical full-month re-agg** from week R2 pages → issue → restore → publish → dataset COMPLETE verify  
**PRE residual tip (task):** options **~29 PARTIAL**  
**host:** **jpm** (`MacBook-Air.local`)  
**PRE SHA:** `abddf5c6a635bfdaea2c7d75b099e66341424157`  
**PROOF SHA:** `c394e500a7143feada3345b8798685d77872c13b`

## Goal

1. PRE remote COMPLETE for options (residual ~29 PARTIAL).  
2. If densify peers ALIVE → **seal-only** full-month window_ok → issue → restore → publish.  
3. If densify DEAD → residual densify week-chunks w=5 general-rpm **450** **or** higher-ROI path to 164/164.  
4. When segs **164/164**: promote dataset COMPLETE (surgical re-agg) if C* pass.  
5. Proof + residual + **git push**. Mass **NO-GO**. empty-raw ban.  
6. Skip DEFER datasets (topix/master/breakdown/earn/am/OTC archive/edinet empty/bars pre-2008/short_sale 2013-01..10).

## PRE (remote D1 @ wave start)

| Metric | Value |
|--------|------:|
| Segment COMPLETE (global remote) | **3380** |
| `derivatives_bars_daily_futures` COMPLETE | **164** |
| `derivatives_bars_daily_options` COMPLETE | **135** (PARTIAL **29**) |
| `derivatives_bars_daily_options_225` COMPLETE | **164** |
| nz COMPLETE manifests options | **742** |
| emptyish COMPLETE manifests options | **25** (not promoted) |
| Peer densify (w0815f) | **ALIVE** week w=5 rpm=400 max-jobs=0 from 2018-08 (`state_residual_week.jsonl` ~116→126 pass) |

Artifacts: `.glm-logs/w0815g_g1_options/PRE_*.json`, `PRE_sha.txt`, `PEER_ACQ.txt`

## Peer policy

```text
At start: w0815f residual week densify ALIVE (w=5 rpm 400 max-jobs=0)
  → seal-only inventory first (no dual densify while peer alive)
Mid-wave: peer densify EXITED natural drain 126/126 pass
  → dry residual still planned 126 identical week jobs
  → chose surgical full-month re-agg from existing R2 week pages
     (higher ROI to COMPLETE than re-fetching same weeks at rpm 450)
Did NOT kill peers
```

## Inventory (window_ok unsealed)

Fresh manifests index: options **743** nz COMPLETE (max run_id **13267**).

| dataset | full-month unsealed | week-partial unsealed |
|---------|--------------------:|----------------------:|
| `derivatives_bars_daily_options` | **0** | **29** (all residual PARTIAL) |
| `derivatives_bars_daily_futures` | **0** | **0** |
| `derivatives_bars_daily_options_225` | **0** | **0** |

Honesty: week densify emits N-day ranges; prior full-month window_ok stock was already sealed by W14-G1. Residual **29** months had **only** week-partial R2 manifests.

### PARTIAL residual at PRE

`2018-09`, `2021-03…2023-06` → **29** months.

## Surgical full-month re-agg seal

`surgical_reagg_seal.py` — for each residual month, load all overlapping week R2 manifests/pages, filter rows to calendar month, write full-month sealpage (`from=01`…`to=last day`) + structured upsert. Empty-raw ban held. No invented empty bodies.

| map | selected | ready | skip/error |
|-----|---------:|------:|-----------:|
| options residual full-month reagg | **29** | **29** | **0** |

- month_rows sum ≈ **11.46M**  
- sealpage ~5.3MB each (full raw often **>140–190MB** → sealpage only)  
- distinct trading days per month ≈ **18–22** (calendar trading days)

Sealed COMPLETE-eligible segs (29):  
`2018-09`, `2021-03…2023-06`

## Issue + restore (full-month reagg ready)

```bash
.venv/bin/python scripts/issue_receipts_parallel.py --dataset … --segment-id … --no-refresh
.venv/bin/python scripts/restore_local_complete_from_receipt.py --dataset … --segment-id …
```

| field | value |
|------:|
| ready full-month | **29** |
| already COMPLETE at issue start | **7** (concurrent peer/ops) |
| todo non-COMPLETE | **22** |
| issued ok | **22** / **22** |
| local options COMPLETE after | **164** |
| PARTIAL residual after | **0** |

Eligibility: signed SUCCESS receipt only. Empty-raw ban held.  
Note: restore script reported `ok_noop` (stdout pattern) while ledger already COMPLETE after issue path — remote/local segs verified **164/164 COMPLETE**.

## Publish (fail-closed)

```bash
.venv/bin/python scripts/publish_ops_projection.py --apply-remote
```

```text
complete_count_guard ok local=3417 remote=3416 force=False
remote projection applied
```

Second publish after dataset verify:

```text
complete_count_guard ok local=3421 remote=3420 force=False
remote projection applied
```

## Reeval + freshness

```bash
for ds in derivatives_bars_daily_options derivatives_bars_daily_futures derivatives_bars_daily_options_225; do
  .venv/bin/python scripts/ops_reeval_observed_window.py --dataset "$ds" --today 2026-08-15
done
.venv/bin/python scripts/ops_reeval_freshness.py
```

| dataset | status (observed reeval @ publish) | observed_start → end | C8 |
|---------|-------------------------------------|----------------------|-----|
| options | segment plane **164/164** → dataset **COMPLETE** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |
| futures | **COMPLETE** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |
| options_225 | **COMPLETE** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |

C1–C5 + C8 **pass** (0 failing) for all three derivatives datasets.

- Freshness **FRESH** `projgen-a1b1ff51d9b841f6a8141b8d60a6952c`.  
- `coverage_segments_untouched=1`; Mass **NO-GO**.  
- No segment rewrite. No COMPLETE claim from reeval alone.

## Dataset COMPLETE promote (surgical re-agg)

With segs **164/164**, ran `surgical_reagg_dataset.py` (same gate as W13-G3):

| dataset | action | status_counts |
|---------|--------|---------------|
| `derivatives_bars_daily_options` | **verify_only** (already COMPLETE) | `{"COMPLETE": 164}` |
| `derivatives_bars_daily_futures` | verify_only | `{"COMPLETE": 164}` |
| `derivatives_bars_daily_options_225` | verify_only | `{"COMPLETE": 164}` |

Platform Dataset COMPLETE **11** held/verified (options included).  
Remote `dataset_coverage.status=COMPLETE` for options confirmed.

## POST (remote D1 live verify)

| item | PRE | POST | Δ |
|------|----:|-----:|--:|
| Segment COMPLETE total | **3380** | **3421** | **+41** |
| `derivatives_bars_daily_options` COMPLETE | **135** | **164** | **+29** |
| `derivatives_bars_daily_futures` COMPLETE | **164** | **164** | **0** |
| `derivatives_bars_daily_options_225` COMPLETE | **164** | **164** | **0** |
| options PARTIAL residual | **29** | **0** | **−29** |
| options dataset_coverage | PARTIAL (pre-wave) | **COMPLETE** | promoted |
| nz COMPLETE manifests options | **742** | **754** | densify residual |
| emptyish COMPLETE manifests | **25** | **25** | no empty promote |
| empty COMPLETE | — | **0** | |

### POST COMPLETE ranges (remote)

| dataset | COMPLETE lo…hi | n | residual PARTIAL |
|---------|----------------|--:|-----------------:|
| options | **2013-01…2026-08** | **164** | **0** |
| futures | **2013-01…2026-08** | **164** | **0** |
| options_225 | **2013-01…2026-08** | **164** | **0** |

Honesty: this G1 uniquely sealed **29** residual months via surgical full-month re-agg of real week densify R2 pages → issue COMPLETE. Global COMPLETE **+41** includes concurrent ledger movement beyond +29 options (remote advanced during multi-hour issue window).

## Host RPM / densify

| wave | workers | general-rpm cap | observed |
|------|--------:|----------------:|----------|
| peer residual week densify (w0815f) | 5 | **400** | **126/126 pass** then DEAD (not killed) |
| dual densify at rpm 450 | — | — | **not launched** — peer already filled residual week plan; reagg used existing R2 |
| dry residual plan post-drain | — | 450 | **126** jobs (identical week residual; re-exec low ROI) |

## Forbidden / honesty

- Did **not** launch Mass / READY / Phase7 ON.  
- Did **not** invent empty COMPLETE (`{"data":[]}` ban).  
- Did **not** promote week-partial ranges without full-month reagg sealpage.  
- Did **not** kill long-running peer densify while alive.  
- Worker pass ≠ Coverage COMPLETE (week densify pass only provided R2 raw; COMPLETE via reagg+receipt path).  
- Full-month large options densify remains HTTP **500** path; week-chunks + surgical reagg is the viable residual COMPLETE path.  
- Skipped DEFER datasets (topix/master/breakdown/earn/am/OTC archive/edinet empty/bars pre-2008/short_sale 2013-01..10).

## Explicit non-claims / DEFER

| Item | Why |
|------|-----|
| full-month densify for 300k+ row options months | **DEFER** — HTTP 500; use `--week-chunks` + surgical reagg |
| re-exec identical week densify plan after peer 126/126 | **skipped** — low ROI vs reagg from R2 |
| topix/idx empty, master, breakdown pre-2015, earn_calendar, bars_am, OTC archive, edinet empty, bars pre-2008-05, short_sale 2013-01..10 | **DEFER** (task skip list) |
| Mass / READY / Phase7 | **NO-GO / OFF** |

## Artifacts

| path | role |
|------|------|
| `.glm-logs/w0815g_g1_options/PRE_*.json` | PRE remote |
| `.glm-logs/w0815g_g1_options/PEER_ACQ.txt` | densify ALIVE at start |
| `.glm-logs/w0815g_g1_options/manifests_index_*.json` | nz COMPLETE index |
| `.glm-logs/w0815g_g1_options/seal_map_full_month.json` | 29 reagg months |
| `.glm-logs/w0815g_g1_options/surgical_reagg_seal.py` | full-month reagg seal |
| `.glm-logs/w0815g_g1_options/reagg_seal_result.jsonl` | 29 ready |
| `.glm-logs/w0815g_g1_options/issue_restore_fast.py` / `*_results.json` | issue 22/22 ok |
| `.glm-logs/w0815g_g1_options/publish.log` | fail-closed publish |
| `.glm-logs/w0815g_g1_options/surgical_reagg_dataset.py` / `reagg_result.json` | dataset COMPLETE verify |
| `.glm-logs/w0815g_g1_options/FINAL_*.json` | POST remote |
| `docs/proof/w0815g_g1_options_residual_20260815.json` | residual snapshot |
| `docs/proof/w0815g_g1_options_20260815.md` | this proof |
