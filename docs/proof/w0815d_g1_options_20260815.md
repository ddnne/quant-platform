# W12-G1 options residual densify+seal — w0815d_g1_options (2026-08-15)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0**  
**empty-raw ban:** held (`row_count>0` + usable pages / sealpage only)  
**peer kill ban:** **honored** — did **not** kill live W11-G5 residual densify (`w0815c_g5_deriv` pid 44593, w=3 rpm=150 max-jobs=80) or concurrent W12-G4 fut/o225  
**prefix:** `w0815d_g1_options_*` · **options-only densify** (partitioned from peer early months) + seal window_ok  
**PRE residual tip (task):** options **~114 PARTIAL** highest-ROI general-pool  
**PRE SHA:** `c8c6ff029553fb75fc080f95ebdb6f970a6e48d8`  
**PROOF SHA:** `36db760ef986ccadca694e5cc6135d12e750f7b4`

## Goal

1. PRE remote COMPLETE for options / futures / o225.  
2. Densify `derivatives_bars_daily_options` residual via `cf_premium_backfill --execute` (options only; no dual-run peer futures/o225).  
3. Seal all window_ok → issue SUCCESS → restore COMPLETE → publish fail-closed → freshness.  
4. If peer acq dies for futures/o225 residual, seal those too (peer W12-G4 owned; inventory showed **0** remaining unsealed after G4).  
5. Proof + residual + **git push**.

## PRE (remote D1 @ wave start)

| Metric | Value |
|--------|------:|
| Segment COMPLETE (global remote) | **3252** |
| `derivatives_bars_daily_futures` COMPLETE | **142** (PARTIAL **9**, UNKNOWN **13**) |
| `derivatives_bars_daily_options` COMPLETE | **50** (PARTIAL **110**, UNKNOWN **4**) |
| `derivatives_bars_daily_options_225` COMPLETE | **157** (UNKNOWN **7**) |
| nz COMPLETE manifests (trio) | futures **232** / options **254** / o225 **250** |
| Peer acq | **ALIVE** `w0815c_g5_deriv` densify all-three residual w=3 rpm=150 max-jobs=80 |

Artifacts: `.glm-logs/w0815d_g1_options/PRE_*.json`, `PRE_sha.txt`, `PEER_ACQ.txt`

## Peer policy (no dual-run)

```text
Peer densify ALIVE @ general-rpm 150 covering futures+o225+early options (max-jobs 80
  → futures 22 + o225 7 + options ~51 through ~2018-03)
Path: partition options densify from 2018-04; seal-only from existing window_ok raw
After peer exited: early options month densify + week-chunk densify for large months
```

Did **not** kill peer. Concurrent W12-G4 sealed futures/o225 residual (proof `w0815d_g4_fut_o225_20260815.md`).

## Inventory (window_ok unsealed with nz raw)

| dataset | window_ok unsealed | notes |
|---------|-------------------:|-------|
| `derivatives_bars_daily_options` | **26** | early peer-pass months + 2019–2021 island |
| `derivatives_bars_daily_futures` | **0** (post-G4) | sealed by W12-G4 |
| `derivatives_bars_daily_options_225` | **0** (post-G4) | sealed by W12-G4 |

Options segs sealed this wave:  
`2013-10/11/12`, `2014-04`, `2019-01…12`, `2020-01/02/05/07/08/10/11/12`, `2021-01/02`

Artifacts: `inventory_window_ok.json`, `all_unsealed_window_ok.json`, `seal_map.json`

## Densify (options only)

### Wave A — full-month residual from 2018-04 (partitioned)

```bash
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets derivatives_bars_daily_options \
  --from-date 2018-04-01 --execute \
  --workers 5 --general-rpm 300 --sleep-on-retry 4.0 --max-jobs 0 \
  --plan-out .glm-logs/w0815d_g1_options/plan_options_exec.json \
  --queue-out .glm-logs/w0815d_g1_options/queue_options_exec.json \
  --state-out .glm-logs/w0815d_g1_options/state_options.jsonl
```

| field | value |
|-------|------:|
| dry residual (2018-04+) | **63** |
| pass / fail | **8 / 55** |
| fail mode | **HTTP 500** (full-month large options resource limit) |
| host_jobs/min | **~4.18** (window ~15.1 min) |
| rowsInserted sum (pass) | **2_754_456** |
| pass segs | `2018-04…07` + late recovery months |

### Wave B — week-chunks (after month densify 500s)

```bash
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets derivatives_bars_daily_options \
  --from-date 2018-08-01 --week-chunks --chunk-days 7 --execute \
  --workers 5 --general-rpm 300 --sleep-on-retry 4.0 --max-jobs 120 \
  --state-out .glm-logs/w0815d_g1_options/state_options_week.jsonl
```

| field | value |
|-------|------:|
| dry plan | **244** week jobs |
| max-jobs | **120** |
| pass (at proof) | **~95+** (still running) |
| host_jobs/min | **~2.5** @ rpm cap **300** |
| rowsInserted sum | **~8.3M+** |

### Wave C — early residual (after peer died)

```bash
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets derivatives_bars_daily_options \
  --from-date 2013-01-01 --to-date 2018-03-31 --execute \
  --workers 3 --general-rpm 150 --sleep-on-retry 4.0 --max-jobs 60 \
  --state-out .glm-logs/w0815d_g1_options/state_options_early.jsonl
```

| field | value |
|-------|------:|
| pass | **18** |
| host_jobs/min | **~0.5** (large pages; rpm cap 150) |
| rowsInserted sum | **~5.0M** |

**NOTE:** Worker pass ≠ Coverage COMPLETE. Week densify left alive for next residual seal wave.

## Seal execute

`seal_from_r2.py` (adapted from G5) — R2 pages → local raw (+ sealpage when ≥~24.5MB / too large for full write) → `normalize_generic` + `SqliteStore.upsert`. Empty-raw ban held.

| map | selected | ready | skip/error |
|-----|---------:|------:|-----------:|
| options window_ok | **26** | **26** | **0** |

Large 2019–2021 months: full raw often **>80–160MB** → **sealpage only** (~5.3MB usable) + full structured upsert.

## Issue + restore

```bash
.venv/bin/python scripts/issue_receipts_parallel.py --dataset … --segment-id … --no-refresh
.venv/bin/python scripts/restore_local_complete_from_receipt.py --dataset … --segment-id …
```

| field | value |
|------:|
| ready unique (seal maps) | **38** |
| todo non-COMPLETE | **20** |
| issued ok | **20** |
| restored COMPLETE | **20** |
| cleanup residual | **0** |

Eligibility: `TRUSTED_COLLECTION` / signed SUCCESS only. Empty-raw ban held.

Restored segs: `2013-12`, `2019-04…12`, `2020-01/02/05/07/08/10/11/12`, `2021-01/02`  
(others in seal map already COMPLETE via peer race / prior).

## Publish (fail-closed)

```bash
.venv/bin/python scripts/publish_ops_projection.py --apply-remote
```

```text
complete_count_guard ok local=3308 remote=3288 force=False
remote projection applied (13015 queries)
```

## Reeval + freshness

```bash
for ds in derivatives_bars_daily_options derivatives_bars_daily_futures derivatives_bars_daily_options_225; do
  .venv/bin/python scripts/ops_reeval_observed_window.py --dataset "$ds" --today 2026-08-15
done
.venv/bin/python scripts/ops_reeval_freshness.py
```

| dataset | status | observed_start → end | C8 |
|---------|--------|----------------------|-----|
| options | **PARTIAL** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |
| futures | **PARTIAL** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |
| options_225 | **PARTIAL** | **`2013-01-04`** → `2026-08-14` | **pass** lag 1 |

- Freshness **FRESH** `projgen-9ae0c4af1d2f417b9fe3ee4cce599a2a`.  
- `coverage_segments_untouched=1`; Mass **NO-GO**.  
- No segment rewrite. No COMPLETE claim from reeval alone.

## POST (remote D1 live verify)

| item | PRE | POST | Δ |
|------|----:|-----:|--:|
| Segment COMPLETE total | **3252** | **3308** | **+56** |
| `derivatives_bars_daily_options` COMPLETE | **50** | **76** | **+26** |
| `derivatives_bars_daily_futures` COMPLETE | **142** | **164** | **+22** (W12-G4 + publish backlog) |
| `derivatives_bars_daily_options_225` COMPLETE | **157** | **164** | **+7** (W12-G4 + publish backlog) |
| options PARTIAL residual | **110** | **88** | **−22** |
| nz COMPLETE manifests options | **254** | **388** | densify ongoing |
| empty COMPLETE | — | **0** | |

### POST COMPLETE ranges (remote)

| dataset | COMPLETE lo…hi | n | residual PARTIAL |
|---------|----------------|--:|-----------------:|
| options | **2013-01…2026-08** | **76** | **88** |
| futures | **2013-01…2026-08** | **164** | **0** planner residual |
| options_225 | **2013-01…2026-08** | **164** | **0** planner residual |

Honesty: global COMPLETE **+56** includes W12-G4 futures/o225 seals published in the same fail-closed apply. This G1 uniquely sealed **26** options window_ok months and restored the non-COMPLETE subset; remote options COMPLETE **+26**.

## Host RPM

| wave | workers | general-rpm cap | observed host_jobs/min |
|------|--------:|----------------:|-----------------------:|
| month densify | 5 | **300** | **~4.2** (worker pagination dominates; many HTTP 500) |
| week densify | 5 | **300** | **~2.5** |
| early densify | 3 | **150** | **~0.5** |
| combined with peer G5 (while alive) | 3+5 | 150+300 | under ~500 upstream ceiling; no 429 storm |

## Forbidden / honesty

- Did **not** launch Mass / READY / Phase7 ON.  
- Did **not** invent empty COMPLETE (`{"data":[]}` ban / emptyish not used for COMPLETE).  
- Did **not** dual full-month options densify for months peer max-jobs 80 already covered (partitioned `from-date 2018-04`).  
- Did **not** kill long-running peer acq/seal.  
- Worker pass ≠ Coverage COMPLETE (week densify left running; COMPLETE only via seal+receipt path).  
- Full-month large options densify → HTTP **500**; week-chunks is the viable residual path.

## Explicit non-claims / DEFER

| Item | Why |
|------|-----|
| options continuous COMPLETE mid-history | **DEFER** — residual PARTIAL **88**; week densify still filling raw; next seal wave |
| full-month densify for 300k+ row options months | **DEFER** — HTTP 500; use `--week-chunks` |
| futures/o225 residual densify this G1 | **N/A** — owned by W12-G4; planner residual **0** post-G4 |
| COMPLETE without raw / empty `{"data":[]}` | **Forbidden** |
| Mass / READY / Phase7 | **NO-GO / OFF** |

## Residual pointers

- POST dry residual (trio): **88** jobs, all `derivatives_bars_daily_options`.  
- Live week densify: w=5 rpm=300 max-jobs=120 state `.glm-logs/w0815d_g1_options/state_options_week.jsonl`.  
- Next: re-inventory window_ok after week densify drains → seal → issue → restore. Prefer week-chunks for remaining large months.

## Operator artifacts

| path | role |
|------|------|
| `.glm-logs/w0815d_g1_options/seal_from_r2.py` | R2→local seal (sealpage for large months) |
| `.glm-logs/w0815d_g1_options/seal_map.json` | 26 options window_ok map |
| `.glm-logs/w0815d_g1_options/seal_result.jsonl` | seal outcomes (26 ready) |
| `.glm-logs/w0815d_g1_options/issue_restore_all_results.json` | 20 issue+restore |
| `.glm-logs/w0815d_g1_options/state_options*.jsonl` | densify state (month/week/early) |
| `.glm-logs/w0815d_g1_options/publish.log` | fail-closed publish |
| `.glm-logs/w0815d_g1_options/POST_*.json` | remote POST probes |
| `.glm-logs/w0815d_g1_options/plan_residual_post.json` | residual dry **88** |

## Close

| field | value |
|-------|------|
| options COMPLETE | **50 → 76 (+26)** |
| platform COMPLETE | **3252 → 3308 (+56)** |
| empty COMPLETE | **0** |
| Mass / READY / Phase7 | **NO-GO / OFF** |
| FRESH | `projgen-9ae0c4af1d2f417b9fe3ee4cce599a2a` |
| residual options PARTIAL | **88** (week densify LIVE) |
