# G1 = T1: equities_bars_daily COMPLETE close (2026-08-14)

**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty-raw ban:** held (`_is_usable_raw` + digest raw ≤20MB; no empty COMPLETE)  
**Worker pass ≠ Coverage COMPLETE** (until signed receipt + ledger refresh)  
**Did not kill** peer jobs: topix `w0713_t3`, master `w0713_t2`, margin/misc `w0713_t7`, deriv/edinet `w0713_t6`, peer seals

**Prefix:** `w0713_t1_bars_*`  
**Tip base:** `83fe7c0`

---

## Objective

Increase `equities_bars_daily` **COMPLETE segs from 12** via:

1. week-chunk CF premium backfill (max-jobs 80–150) for residual / mid-holes  
2. R2→local raw+structured seal for months with non-empty raw (floor **2008-05**)  
3. `issue_receipts_parallel` (signed) + publish → COMPLETE **+N**  
4. reeval `observed_window`  
5. proof + commit/push  

**Forbidden held:** no Mass; no empty COMPLETE; no kill of other jobs; no COMPLETE of bars **pre-2008-05** (empty API band DEFER).

---

## PRE

| metric | value |
|--------|------:|
| bars COMPLETE segs (remote + local) | **12** |
| bars PARTIAL segs | 260 |
| COMPLETE ids | 2024-01..03, 2025-04, 2026-01..08 |
| `observed_start` | **2008-05-01** |
| `observed_end` | 2026-08-12 |
| dataset status | PARTIAL |
| raw_retention_manifests n / nz / sum_rows | **2128** / **1539** / **28_735_498** |
| local structured months / rows | 12 / 803_862 |
| SUCCESS nz receipts (local) | 76 (old scope/unsigned; not COMPLETE-eligible) |

Artifacts: `.glm-logs/w0713-t1-bars/PRE_*.json`

### Dry residual (planner)

| field | value |
|------:|
| plan_jobs week-chunks 2008-05-01→cutoff | **905** (COMPLETE-only skip; PARTIAL re-queues) |

---

## 1) Execute backfill (week-chunks)

```text
.venv/bin/python -u scripts/ops/cf_premium_backfill.py \
  --datasets equities_bars_daily \
  --from-date 2008-05-01 --to-date 2026-08-13 \
  --execute --week-chunks --chunk-days 7 \
  --workers 2 --general-rpm 495 --max-jobs 120 \
  --sleep-on-retry 3.0 \
  --plan-out .glm-logs/cf-backfill/w0713_t1_bars_exec_plan.json \
  --queue-out .glm-logs/cf-backfill/w0713_t1_bars_exec_queue.json \
  --state-out .glm-logs/cf-backfill/w0713_t1_bars_exec_state.jsonl
```

| field | value |
|-------|------:|
| plan / queued / executed | **905** / **120** / **120** |
| **pass / fail** | **86 / 34** |
| fail mode | worker **HTTP 429** (retries exhausted) under concurrent general-pool peers |
| rowsInserted sum (pass detail) | ~**985_115** |
| host_dispatch_rpm | **~7.16** (window ~997s) |
| http_429_count (host envelope) | 0 on outer POST (429 inside worker pagination) |
| months touched (queue order) | ~2008-05 … 2010-08 week chunks |

### 429 retry band

```text
--from-date 2009-02-01 --to-date 2009-06-30 --max-jobs 40 --sleep-on-retry 5.0
```

| field | value |
|------:|
| executed | **22** |
| pass / fail | **21 / 1** |

**Combined execute wave:** pass **107** / fail **35** (120 + 22 jobs; not additive months).

Peer concurrent (not killed): `indices_bars_daily_topix` w0713_t3 finished 192; `equities_master`; margin/investor family; deriv/edinet.

---

## 2) R2 seal (raw-required only)

Script: `.glm-logs/w0713-t1-bars/seal_bars_from_r2.py`  
Select full-month COMPLETE manifests with `row_count≥20000`, floor `2008-05`, skip already COMPLETE.

| field | value |
|------:|
| candidates (unsealed full-month) | 30 selected |
| **ready** | **30 / 30** |
| segment range | **2008-05 … 2010-10** |
| empty / error | **0** |
| digest raw | non-empty JSON ≤20MB (issue_receipts 25MB usable cap) |
| structured upsert | full month pages (empty-raw ban held) |

Result: `.glm-logs/w0713-t1-bars/seal_result.jsonl`  
Note: peer path independently sealed **2010-06** (receipt `900772`) before our issue loop — counted in COMPLETE delta once.

---

## 3) Signed receipts + COMPLETE

Per-segment (avoid slow `struct-hint` full-table EXISTS on multi-M `jquants_records`):

```text
for seg in 2008-05 … 2010-10 (skip COMPLETE):
  .venv/bin/python scripts/issue_receipts_parallel.py \
    --datasets equities_bars_daily --segment-id $seg \
    --workers 1 --no-refresh --json-summary
refresh_coverage_ledger(datasets=['equities_bars_daily'])
```

| field | value |
|------:|
| issue loop | **29 OK / 0 FAIL** (+ 2010-06 already COMPLETE) |
| eligibility | `TRUSTED_COLLECTION` / `SignedReceiptAuthority` / `dev-receipt-v1` |
| raw_row_count == structured_row_count | yes (script policy) |

### Publish (fail-closed)

```text
.venv/bin/python scripts/publish_ops_projection.py \
  --db data/structured/ingestion.sqlite --refresh-coverage --apply-remote
```

| field | value |
|------:|
| complete_count_guard | **ok local=846 remote=742 force=False** |
| remote projection | **applied** |
| freshness | `ops_reeval_freshness` → `projgen-661d3b08b24c4577923f223b72348ddb` |

---

## 4) Reeval observed_window

```text
.venv/bin/python scripts/ops_reeval_observed_window.py --dataset equities_bars_daily
```

| field | PRE | POST |
|-------|-----|------|
| **observed_start** | **2008-05-01** | **2008-05-01** (unchanged; no pre-2008 fabricate) |
| observed_end | 2026-08-12 | **2026-08-13** |
| status | PARTIAL | PARTIAL |
| C8 | pass | **pass** lag **0** |
| COMPLETE segs | 12 | **42** (via seal path, not reeval inflation) |
| receipt n / sum_raw (reeval window) | — | **1537** / **23_665_747** |

`coverage_segments` not rewritten by reeval (by design).

---

## POST Δ

| metric | PRE | POST | Δ |
|--------|----:|-----:|--:|
| **bars COMPLETE segs** | **12** | **42** | **+30** |
| bars PARTIAL | 260 | 230 | −30 |
| raw manifests n | 2128 | **2271** | **+143** |
| raw nz | 1539 | **1665** | **+126** |
| raw sum_rows | 28_735_498 | **30_048_781** | **+1_313_283** |
| observed_start | 2008-05-01 | 2008-05-01 | 0 |
| observed_end | 2026-08-12 | 2026-08-13 | +1d |

### COMPLETE months added (30)

**2008-05 … 2010-10** continuous (includes peer `2010-06`).

Prior COMPLETE retained: 2024-01..03, 2025-04, 2026-01..08.

---

## Acceptance

| gate | result |
|------|--------|
| week-chunks max-jobs 80–150 | **PASS** (120 + retry 22) |
| pass/fail reported | **PASS** (86/34 + 21/1) |
| COMPLETE segs increased from 12 | **PASS** → **42** (+30) |
| only raw months sealed | **PASS** (R2 nz full-month only) |
| empty COMPLETE ban | **PASS** |
| no pre-2008-05 COMPLETE | **PASS** |
| observed_start held at 2008-05-01 | **PASS** |
| reeval bars | **PASS** |
| no Mass / no kill peers | **PASS** |
| commit + push | see tip SHA below |

---

## Explicit non-claims

- **No** dataset-level COMPLETE / Mass / Research READY / Phase7 ON  
- **No** COMPLETE for 2006-08…2008-04 empty API shells  
- Residual week-jobs after max-jobs cap still large (~785+) until more COMPLETE skip  
- Remaining PARTIAL months after 2010-10 (except recent COMPLETE islands) **DEFER** next seal wave  
- Worker / R2 pass alone ≠ Coverage COMPLETE  

---

## Artifacts

| path | role |
|------|------|
| `.glm-logs/cf-backfill/w0713_t1_bars_{plan,queue_dry,exec_*}.{json,jsonl,log}` | backfill dry + exec |
| `.glm-logs/cf-backfill/w0713_t1_bars_retry_*` | 429 retry band |
| `.glm-logs/w0713-t1-bars/seal_bars_from_r2.py` | R2→local seal driver |
| `.glm-logs/w0713-t1-bars/seal_{map,result,log}.*` | seal evidence |
| `.glm-logs/w0713-t1-bars/issue_targeted.log` | signed issue loop |
| `.glm-logs/w0713-t1-bars/{publish,reeval}.txt` | publish + reeval |
| `.glm-logs/w0713-t1-bars/PRE_*.json` | PRE snapshots |

## Commands (reproducible)

```bash
# dry residual
.venv/bin/python scripts/ops/cf_premium_backfill.py \
  --datasets equities_bars_daily --from-date 2008-05-01 --to-date 2026-08-13 \
  --week-chunks --workers 2 --general-rpm 495 \
  --plan-out .glm-logs/cf-backfill/w0713_t1_bars_plan.json \
  --queue-out .glm-logs/cf-backfill/w0713_t1_bars_queue_dry.json

.venv/bin/python scripts/ops_reeval_observed_window.py --dataset equities_bars_daily
```
