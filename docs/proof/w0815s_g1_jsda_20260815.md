# W26-G1 / w0815s_g1 — JSDA OTC tip/recent **+0 DEFER** + COMPLETE skip (corp/repo) (2026-08-15)

**Wave:** `w0815s` / **W26-G1** / **JSDA OTC tip**  
**Mass / READY / Phase7:** still **NO-GO / OFF**  
**empty COMPLETE:** **0** (no empty shell; no invent)  
**empty-raw ban:** held (no official full CSV this wave → **no seal**)  
**Worker pass ≠ COMPLETE:** held  
**corporate+repo:** **COMPLETE skip** (verify only; no re-seal)  
**force-apply:** **not** used  
**this-wave OTC seal delta:** **+0** (official 404 / site timeout DEFER)  
**densify on empty history:** **DEFER / ban held** (not exercised)  
**prefix:** `w0815s_g1_*` · logs: `.glm-logs/w0815s_g1_jsda/`  
**CF SoT:** D1 hot / R2 history / receipt COMPLETE

**Live verified:** 2026-08-15 (JST) / ~2026-08-15T02:28Z UTC  
**Projection:** **FRESH** `projgen-69d0bd867f4a44768bf199bda2fcc349` (`coverage_segments_untouched=1`, mass=**NO-GO**)  
**Base HEAD (wave start / pre-proof):** `1dd7ddd`  
**Proof HEAD (post-push):** `35f1437`

## Summary

| Metric | At wave start (this agent) | Ledger at close | This W26-G1 claim |
|--------|---------------------------:|----------------:|------------------:|
| JSDA OTC COMPLETE segs | **72** / 8781 | **72** / 8781 | **+0** (no seal) |
| JSDA corporate | **12/12 COMPLETE** | **12/12 COMPLETE** | skip (verify only) |
| JSDA tokyo repo | **COMPLETE** 1/1 | **COMPLETE** 1/1 | skip (verify only) |
| empty COMPLETE (OTC) | **0** | **0** | held |
| Platform COMPLETE | **3457** local/D1 | **3457** local/D1 | no delta |
| Dataset COMPLETE | **11** | **11** | held |
| `raw_retention_manifests` | **15145** | **15145** | no this-agent delta |
| D1 `jsda_repo_rates` hot tip | **252** max=`2026-08-10` | **252** max=`2026-08-10` | hot publish **SKIP** |

> **Honesty:** This agent produced **0** FULL_OK downloads and **0** signed receipts. Tip already COMPLETE through **`2026-08-17`**. Site index **403 Forbidden**; tip advance codes official **404**; residual/GW/holiday/control CSV **timeout** (rc=28) → **DEFER**. Local repo tip **not** ahead of D1 → no `publish_jsda_hot_to_d1`. Ops projection **SKIP** (no this-wave seal delta).

## Path

1. **COMPLETE skip (verify only):**
   - `jsda_tokyo_repo_rates` dataset **COMPLETE**, segment `jsda-era-timeseries` **COMPLETE** — no seal.
   - `jsda_corporate_bond_transactions` dataset **COMPLETE**, segments **12/12** (`2015`…`2026`) — no seal.
   - Artifact: `.glm-logs/w0815s_g1_jsda/verify_complete_skip.json`

2. **OTC tip/recent probe** (`market.jsda.or.jp`) — **HTTP 200 full CSV only** (≥ ~1.5 MiB):
   - Index **HTTP 403** (199 B); tip list not refreshed (prior tip still **`S260817`** / day **`2026-08-17`**).
   - Tip advance: `S260818`…`S260827` → **HTTP 404** (not on tip yet; 404 page ~46 KiB ≠ full CSV).
   - `S260828` + GW holes `S260504`…`506` + holiday-like `S260720`/`S260811` + Apr residual `S260430`…`S260424` + control sealed tip `S260817` → **connect/transfer timeout** (rc=28, http=000).
   - **FULL_OK full CSV this wave:** **0** → **no seal / no R2 put / no ledger write**.
   - Artifacts: `probe_summary.txt`, `download_ok.txt` (empty), `download_fail.txt`, `curl_index.out`.

3. **Optional `publish_jsda_hot_to_d1.py --apply-remote`:** **SKIP**
   - Local `jsda_repo_rates` tip max **`2026-08-10`** = D1 max **`2026-08-10`** (hot n=**252** already published).
   - Dry-run export only: hot rows **252**, cutoff `2026-07-01`.
   - Artifact: `.glm-logs/w0815s_g1_jsda/hot_d1_decision.json`

4. **Ops projection full publish:** **SKIP** (no local seal delta from this agent).  
   `ops_reeval_freshness.py` → FRESH; `coverage_segments_untouched=1`; mass=**NO-GO**.

5. **DEFER densify ban on empty history:** held — no densify of archive PARTIAL / empty pre-history shells this wave.

## OTC sealed this wave (**+0**)

_(none — empty-raw ban + timeout DEFER + official 404)_

Eligibility path held: would require HTTP **200** full CSV (~2.1–2.2 MiB) → parse/upsert → identity-matched `SignedReceiptAuthority` → ledger. Not exercised.

## POST OTC COMPLETE (**72** ledger; unchanged)

`2026-05-01`, `2026-05-07`…`2026-05-29` (GW hole `05-04`…`05-06`), then continuous trading days through `2026-08-17` except holiday-like holes `2026-07-20`, `2026-08-11`.

Dataset still **PARTIAL** (history target 2002-08-02; **8781** inventory days; observed_start **2026-05-01** → end **2026-08-17**).

## Corporate / Repo (COMPLETE skip)

- **Corporate:** segment COMPLETE **12/12** (`2015`…`2026`); dataset **COMPLETE** — unchanged (no seal).
- **Tokyo repo:** dataset **COMPLETE**, segment `jsda-era-timeseries` **COMPLETE** — unchanged (no seal). Local facts **30 303**; D1 hot tip **252**.

## DEFER (honest)

| Item | Why |
|------|-----|
| Tip advance past `2026-08-17` (`S260818+`) | Official **404** (not published yet; prior tip=`S260817`). |
| GW / holiday gaps `2026-05-04`…`05-06`, `07-20`, `08-11` | Timeout / official missing; **no invent COMPLETE**. |
| Apr residual densify (`S260430`…) | Connect timeout; **DEFER densify ban on empty history**. |
| Index listing this wave | **403 Forbidden**; tip list not refreshed. |
| Control re-fetch `S260817` | Timeout (site flake for CSV path) — **no invent / no re-seal**. |
| OTC full archive (~8709 PARTIAL remain) | Out of tip-only scope this wave. |
| COMPLETE without raw / empty shell | **Forbidden** (empty COMPLETE **0** on OTC) |
| Mass / READY / Phase7 | **NO-GO / OFF** |

Artifact: `.glm-logs/w0815s_g1_jsda/jsda_defer.json`

## Hot D1 / publish

```text
publish_jsda_hot_to_d1: SKIP
local_repo_max=2026-08-10 == d1_repo_max=2026-08-10 n=252
ops_projection --apply-remote: SKIP (no this-wave seal delta)
ops_reeval_freshness → FRESH projgen-69d0bd867f4a44768bf199bda2fcc349
OK coverage_segments_untouched=1 mass=NO-GO
```

Remote D1 check: OTC COMPLETE **72** / empty OTC COMPLETE **0**; corp+repo datasets **COMPLETE**; OTC dataset **PARTIAL**; platform COMPLETE **3457**; Dataset COMPLETE **11**; tokyo_repo hot facts n=**252**; raw manifests **15145**.

No `--force-apply-remote`. No hot D1 apply (local tip not ahead).

## Explicit non-claims

- This wave did **not** seal any OTC day (+0).
- OTC dataset-level COMPLETE **not** claimed (still PARTIAL vs 8781-day inventory).
- Platform Mass / READY / Phase7 **not** claimed.
- Corporate/repo seals **not** re-run (already COMPLETE).
- Official 404 / timeout days **not** sealed as COMPLETE.
- D1 full JSDA history backfill **NO-GO** by design (hot tip only).
- Empty-history densify **not** re-run (ban held).

## CF SoT honesty

| Plane | Role this wave |
|-------|----------------|
| **D1 hot** | Repo hot tip **252** held (`>=2026-07-01`…`2026-08-10`); OTC COMPLETE segs **72** receipt-projected |
| **R2 history** | Full JSDA history SoT remains R2 + local research mirror (not re-shipped) |
| **Receipt COMPLETE** | OTC tip days remain receipt-owned COMPLETE only; no empty-raw invent |

## Operator repro

```bash
# tip/gap probe (when market.jsda.or.jp accepts connections)
for code in S260818 S260819 S260820 S260821 S260824 S260825 S260826 S260827 \
            S260504 S260505 S260506 S260720 S260811; do
  curl -4 -sS --connect-timeout 12 --max-time 90 -o /dev/null \
    -w "$code %{http_code} %{size_download}\n" \
    "https://market.jsda.or.jp/shijyo/saiken/baibai/baisanchi/files/2026/${code}.csv"
done
# FULL_OK only if http=200 size>~1500000; else no seal

# optional hot tip D1 (only if local repo max > D1 max)
.venv/bin/python scripts/publish_jsda_hot_to_d1.py --dry-run
# .venv/bin/python scripts/publish_jsda_hot_to_d1.py --apply-remote

.venv/bin/python scripts/ops_reeval_freshness.py
```

## Logs

`.glm-logs/w0815s_g1_jsda/` — PRE/POST sha, verify skip, index 403, tip/gap probe (404 + timeout), download ok/fail, hot D1 decision + dry-run, defer, freshness, remote D1 probes, final_state.
